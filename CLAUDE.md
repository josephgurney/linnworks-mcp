# Linnworks MCP Server — Claude context

**Current version: 1.48.2** — 85 tools. See `pyproject.toml` for full metadata.

---

## ⚠️ Assumptions to check on ANY new Linnworks endpoint (read first)

Linnworks is a decade of accreted legacy — the same class of quirk resurfaces on
endpoint after endpoint. Before trusting a new endpoint, and ESPECIALLY before a
live write, run down this list. Every item here has already cost this project a
real debugging session; each is expanded with its evidence somewhere below.

1. **Current data or a stored/cached copy?** — The single highest-value question.
   Many endpoints operate on a *snapshot*, not live state: GLT `ProcessTemplates`
   pushes the template's stored fields (stale unless rebuilt in the UI — v1.27.1);
   `GetStockItemsFullByIds` returns empty `Suppliers[]` unless you pass
   `DataRequirements:[1]`; archived items are invisible to every list/search
   endpoint. Ask what the endpoint reads from before believing its output.
2. **2xx ≠ the right thing happened.** — A success status only means the call was
   accepted. GLT returned 2xx while pushing 5-month-old prices to the live store.
   Read back against the REAL surface (the Shopify listing, a fresh GET), not just
   the write's return value. This is why read-before-write + read-back is
   non-negotiable on write tools.
3. **Does UPDATE clear fields you omit?** — Many updates overwrite the whole record
   with what you send, nulling anything absent: `UpdateInventoryItem`,
   `Update_PurchaseOrderHeader`, `UpdateCategory`, `UpdateStockSupplierStat`. Read
   the existing row first and carry every field through, overlaying only changes.
4. **Wrapped `{"request":{…}}` or unwrapped?** — No consistent rule, and getting it
   wrong often FAILS SILENTLY: OpenOrders `GetOrdersLowFidelity` needs the wrapper;
   `SearchOrders` and `Search_PurchaseOrders2` must be UNWRAPPED (the wrapper is
   accepted but ignores all filters / 400s "must provide a search term").
5. **How is success signalled?** — Some endpoints return an empty 2xx body, some a
   204 (use `call_linnworks_void`), some a non-JSON 2xx string
   (`Create/UpdateInventoryItemExtendedProperties`). `call_linnworks` tolerates all
   three now, but confirm which you're dealing with.
6. **Query-string array shape.** — Repeated params, comma-join, and JSON-array-string
   are NOT interchangeable: `GetStockSupplierStatsBulk` only accepts
   `?inventoryItemIds=["guid","guid"]` (a JSON string); the other two 400.
7. **Active-only or includes archived?** — `GetStockItems`, `GetStockItemsFull` and
   friends silently return active stock only (~32k of ~93k here). "Not found" can
   mean "archived", not "doesn't exist".
8. **A 404 may be the wrong NAME, not a missing capability.** — `GetStockSupplierStat`
   existed for years while `GetInventoryItemSupplierStat`/`GetInventoryItemSuppliers`
   404'd. Before declaring something impossible, `grep -i <noun>` the relevant spec
   file (`inventory.json`, `stock.json`, …) for the real name.
9. **Does the server OVERRIDE the values you write?** — Distinct from "did the write
   land": the call succeeds, the field persists, but not with *your* value.
   `UpdateImages` pins the main image to `SortOrder` 0 and re-normalises everything
   else, so a requested order is silently rewritten (v1.28.0); a partial payload is
   re-sorted while the full main-first payload is honoured verbatim. Never let a
   dry-run manifest promise what the server will overrule — predict the override,
   then read back and compare against the plan.
10. **Is the "impossible" note in THIS file still true?** — Our own blocker notes go
   stale, and a stale one silently caps what the server can do. Issue #17 recorded
   "composite reverse lookup is impossible: `GetStockItemsFull` 400s"; a later note
   in the same file (7 Jul) recorded the working payload for that endpoint, and the
   sweep it declared infeasible turned out to cost 215 calls (issue #31, v1.33.0).
   Two of those halves were written months apart and never reconciled. Before
   accepting a documented dead end, re-probe it — especially one whose reasoning is
   "endpoint X doesn't work" rather than "the API has no such concept". Also check
   whether a **bulk** sibling exists (`…ByIds`): it can turn an "infeasible" N-call
   scan into N/100 calls.
11. **Does a `load*` flag LOAD detail, or FILTER the result set?** — They read like
   "also fetch this data", but some are include/exclude filters and silently shrink
   what you get back. `Stock/GetStockItemsFull`'s `loadCompositeParents` /
   `loadVariationParents` **omit those parents entirely** when false — the `false/false`
   payload this file called "the full active-item enumerator" for a month was missing
   every composite and variation parent (issue #32). Contrast the same call's
   `dataRequirements`, which genuinely is a load-more-detail option. A sweep that
   under-reports doesn't error; it just quietly answers a smaller question than you
   asked. Count a known family both ways before trusting a flag's name.
12. **Is that error actually about the DATA, or about the QUOTA?** — and note the
   fix for this shipped TWICE before it was right (#34 then #37): v1.37.0 made
   `RateLimitError` a `RuntimeError` subclass so old handlers "kept working", but
   55 bare `except RuntimeError` handlers bucket errors as per-item DATA failures,
   so they kept mislabelling. It now subclasses **`Exception`**, so a bare
   `except RuntimeError` cannot swallow it. **When you catch a broad exception and
   turn it into a per-item verdict, you are asserting a cause you did not check.** — Linnworks
   returns HTTP 429 `"API calls quota exceeded!"` per endpoint per minute (150 on
   most Inventory reads, 250 on `GetItemChangesHistory`). Until v1.37.0 nothing
   retried it, and `_resolve_sku_to_id` folded it into `"SKU not found"` — so
   `get_channel_listings_bulk` reported 4,804 SKUs as missing when it had simply
   run out of quota, under-reporting live listings by 88%. The helpers now retry
   (5/10/20/30s) and raise `RateLimitError`; bulk tools report `rate_limited`
   separately from `unresolved` and set `complete: false`. **When a per-item
   failure bucket says "not found", check it isn't really a 429** — and note which
   direction the mistake points: here it pointed at "safe to delete".
13. **Does that field describe the CHANNEL, or just what we last stored about it?** —
   Distinct from #1: the record is current, but a *field on it* is a stale echo. The
   channel-SKU table's **`ListedQuantity` is not live channel stock** — 11 zero-stock
   TikTok SKUs read `listed_quantity: 30` while **none had a live listing at all** (9 had
   no GLT template, 2 read `Status:"Not listed"` with an empty `ActiveListingId`). The
   tell was **`LastUpdate: 0001-01-01T00:00:00`**, the null date: that mapping has never
   synced, so every "live-looking" number on it is fiction. **Judge liveness on
   `OpenTemplatesByInventory` (`Status` + non-empty `ActiveListingId`), never on
   `ListedQuantity`** — and treat a null `LastUpdate` as "this row has never been
   confirmed by the channel". Reported as an urgent oversell exposure before it was
   checked; it pointed, as usual, at the destructive answer.
14. **Is it zero because it's DEAD, or zero because it's between deliveries?** —
   A stock level is a snapshot and carries no age. Of 16 zero-stock TikTok candidates,
   two had gone to zero within the past week; only 7 were ≥90 days. Retiring the fresh
   ones would have destroyed live listings for restocking products. Always run
   `get_stock_change_history` (`out_of_stock_since` / `days_out_of_stock`) **before**
   any delist/archive proposal — it changed the verdict on 9 of 16 here.
15. **Does the endpoint return the VALUE, or just a SUMMARY of it?** — A field
   can be present, named after real content, and still carry no content.
   `OpenTemplatesByInventory`'s `Info` returns real values for `Title`/`Price`
   but returns `Images: "1"` (a COUNT), `Attributes: "21"`, `MetaFields: "42"`
   and `Description: "Filled"` — all `Type:"Action"` summaries. So a
   "compare the snapshot to the item" check can compare title and price but is
   **structurally blind to image URLs and description text**, which is exactly
   what made #40 a silent no-op (image count 1 == 1, URL different). When a
   comparison comes back clean, ask whether the field you compared was the value
   or a tally of it — and report the blind spot instead of implying coverage.
   Related: a field can also be a CONSTANT wearing a decision's name —
   `NextSuggestedAction` read "Update"/allowed on 24/24 listed Shopify
   templates, so it never meant "a change is pending" as this file once claimed.
16. **Client-generated GUID or server-minted?** — Sub-entity `Create*` endpoints
   (prices, descriptions, titles, ext-props) need a client `pkRowId`; `AddInventoryItem`
   needs a client `StockItemId`; but `CreateCategory` and `CreateStockSupplierStat`
   mint the id server-side. Omitting a required one → PK-collision 400.

Prefer endpoint families already proven in this tenant (see the confirmed-endpoints
table). When in doubt, dry-run and read back before trusting anything.

---

> **v1.48.2 (issue #45) — `refresh_channel_listing`'s own runtime surface now agrees with the v1.47.1 write-up: Amazon reads "tried and shown ineffective", not "untried" (2 Sep 2026):** Docs-and-registry-only, additive. The two live Amazon revise proofs (24-25 Aug 2026, templates 32064 + 32239 — see v1.47.1 below) were already recorded in this file, but nothing on the code path itself reflected them: `GLT_CHANNELS["amazon"]` still only carried `revise_proven: False`, the tool docstring still said "NOT LIVE-PROVEN ON AMAZON OR TIKTOK" as if both were simply untried, and every unproven-channel warning used one "NOT yet live-proven" wording for both states. **New tri-state field `revise_attempted` on every `GLT_CHANNELS` entry**, alongside `revise_proven`: `False` = genuinely never attempted live (TikTok/Magento/Walmart), `True` + `revise_proven: False` = attempted live and shown ineffective (Amazon), `True` + `revise_proven: True` = attempted and proven (Shopify). The unproven-channel warning built inside `refresh_channel_listing` now branches on it — Amazon's message says the revise "has been fired live … and produced no observable change … TRIED AND SHOWN INEFFECTIVE, not merely untried"; TikTok's still says "has NEVER been attempted live" — both derived from the registry via the existing `_proven_revise_channels()`, so neither string is hard-coded and a future proof on either channel updates the wording for free. Both docstring passages carrying the stale claims were corrected: the top-level warning now states the Amazon/TikTok distinction plainly, and the `channel` arg's variation-shape note now says one Amazon family (bushings) was observed carrying a template per child — TikTok's shape, not Shopify's — and that this is one observation, not a rule (previously: "unestablished — nothing in this repo has observed it either way", which was false since the bushings proof). CLAUDE.md's tools-table entry for `refresh_channel_listing` and README.md's channels-and-listings row were reworded the same way, replacing "spec-based, not yet live-proven"/"NOT LIVE-PROVEN ON AMAZON OR TIKTOK" with the same tried-vs-untried distinction. **No behavioural change** — plan building, action selection, the child→parent variation fallback, and the staleness pre-flight are byte-identical; only prose (docstrings, README/CLAUDE.md, and the two warning-message branches) and the new registry field changed. `revise_proven` stays `False` for Amazon — accepted-with-no-observable-effect is not proof of a working route, and flipping it needs a push that demonstrably lands (still an external, human-run verification per the issue's post-merge steps: a manual GLT-UI revise on the art-deck listing, and/or a Linnworks support ticket, to establish whether the defect is in this tool's API call or in Linnworks' own Amazon integration). No live API call made from this build. New `test_docs_consistency.py` guard confirms README.md, CLAUDE.md and `GLT_CHANNELS` agree on which channels are `revise_proven`, generalising the existing per-doc/per-registry drift guards to this new fact. 522 tests + additions, 85 tools unchanged; versioned as a patch per the v1.44.1/v1.47.1/v1.48.1 precedent.

> **v1.48.1 — eBay revise live-proof: the WRAPPER question settled benign, the tool's push ACCEPTED BUT NEVER PROCESSED; `revise_proven` stays False, tracked as issue #47 (26 Aug 2026):** Docs-only. The v1.48.0 post-merge checklist was executed and its furniture gate fired exactly as designed — at the DRY RUN, before anything was written: the manifest showed the channel row's bare inner copy with **no shop furniture**, and the push was refused pending investigation. **The gate's premise turned out to be wrong for this tenant, in an informative way:** every template in the `EVRi - Single Image` configurator (~150, including ones backing live furniture-wrapped listings) stores INNER COPY ONLY — the design template applies the wrapper at push time (owner-confirmed). So furniture can never appear in a manifest built from channel data, and **the correct furniture check is the live description frame AFTER a push, not the manifest before it** — the checklist text in future briefs should say so. **The live proof, on `ven-grip-cleaner` (eBay 286493672322):** one provable sentence appended to the EBAY0 channel description, dry-run manifest verified carrying it, live push fired ~12:56Z → `unconfirmed` (accepted). The frame was then polled with fresh cache-busters for TWO HOURS: no change, furniture untouched. The owner pushed the identical change from the Linnworks listing UI at ~14:5xZ and the frame updated **within minutes — new sentence live AND both furniture blocks intact.** Two conclusions, cleanly separated: **(1) SETTLED BENIGN — a Linnworks revise recomposes the design-template wrapper around the pushed inner copy; the furniture-stripping risk is off the table for description revises.** (2) **The tool's own API-submitted push does not get processed on this tenant** — same verdict class as Amazon's #45, one layer over: Amazon's Revise is accepted by Linnworks with no observable effect; eBay's is accepted by Linnworks and demonstrably never forwarded, while a UI-initiated push of the same content processes in minutes. `revise_proven` therefore stays False on EVIDENCE, not caution. **Tenant traps found en route (throwaway attempt, item since archived):** `Listings/CreateEbayTemplates` REPLACES the item's template on every call — a new TemplateId each time — so polling status via repeated create calls silently destroys any in-flight operation (never use it as a query; page `GeteBayTemplates` instead); `Listings/GetEbayListingOperations` 400s with "Parameter 'request' has invalid value" under both body and `request.*` query serializations, so the operations queue is unreadable here; and API-initiated CREATE also never listed (the owner's UI create of the same junk item stalled too, so create-side evidence is suggestive only — possibly silent category-specifics validation — where the revise-side evidence is clean). **Until #47 resolves:** use the tool's dry-run manifest to review content, then propagate via the Linnworks listing UI; the manifest/staleness/threshold gates all behaved exactly to spec throughout. No runtime path touched; 522 tests and 85 tools unchanged; versioned as a patch per the v1.44.1/v1.47.1 precedent — a shipped caveat has hardened into a live-tested result.

> **v1.48.0 (issue #43) — `revise_ebay_listing_description`: the first eBay write, via a SEPARATE non-GLT API family the repo's own notes had missed (25 Aug 2026):** eBay was documented as having "no dedicated public listing API" (NON_GLT_SOURCES, `_resolve_glt_channel`, four other spots) — **that claim was stale, per assumption #10**. Live-probed 25 Aug 2026: `PublicApiSpecs/1.0/listings.json` (a spec file this repo had never referenced) defines a whole `Listings/` family for eBay and BigCommerce, entirely separate from `GenericListings/`: **`GET Listings/GeteBayConfigurators`** (no body — every EBAY configurator on the tenant, with `AssociatedTemplates`/`AssociatedVariations` counts), **`GET Listings/GetAllEbayConfigurators`** (source→subsource→configurator names, no body), **`POST Listings/GeteBayTemplates`** (paged `EbayListing` templates), **`POST Listings/ProcesseBayListings`** (push an updated template back, 204). **All three read endpoints fired live and confirmed working this build.** ~~`ProcesseBayListings` was never fired — deliberately out of scope for this issue, a human proves it live on one listing per the steps below.~~ **← SUPERSEDED the next day (v1.48.1, issue #47): the push WAS fired live 26 Aug 2026 against a real listing and was accepted but not observed to reach the channel — see that entry.** Confirmed on this tenant: `GetAllEbayConfigurators` → one source `EBAY`, one subsource `EBAY0`, 33 configurators, 9 with `AssociatedTemplates > 0` (largest: "(NEW) VARIATION - On Demand", 1049 templates/36 variations). **⚠️ `GeteBayTemplates` REQUIRES a non-zero `ConfigId` OR a non-empty `TemplateIds`** — body `{"parameters":{...}}` (the swagger body-parameter name `Listings_GeteBayTemplatesRequest` is NOT the wire key, same gotcha as every other GLT endpoint); an empty/zero `parameters` object, or `Source`+`SubSource` alone, returns **HTTP 400 `{"Message":"Parameter 'parameters' has invalid value"}`** — a genuinely well-formed `ConfigId` (e.g. `d9b672e0-69b8-4cb9-b9a0-7e4869aa82a3`) makes it work (`{"Items":[...],"TotalItems":319,...}`). **`InventoryItemIds` is documented in the `GetTemplatesParameters` schema but has NO server-side effect** — live-confirmed: passing a real `StockItemId` alongside a real `ConfigId` returns the exact same first page as omitting it. **So there is no direct by-SKU/by-listing-id lookup on this API** — the only route to "which template covers X" is to sweep `GeteBayConfigurators` (populated configs) → paginate `GeteBayTemplates` per config (no visible page-size cap; 500/page confirmed; past-the-end returns empty `Items` with a clean 200, not the `GetStockItemsFull`-style 400) and match on `ListingIds` — the same shape `find_composite_parents` (#31) used for composite parents. `_find_ebay_template_for_listing()` does exactly this; as of the QA round 1 fix below it is called during PLANNING (dry run included), not only right before a live push. **⚠️ eBay's variation shape (live-probed on `d516b0cf-1f32-40e7-a505-c435464ba87e`, "(NEW) VARIATION - EVRi - Size"): ONE TEMPLATE = ONE LISTING.** The template's top-level `SKU` is a group-style identifier for a multi-variation listing (e.g. `vnm-tc-V2-install-gold`, `JES-GRP-Master` — not always a real sellable SKU) and the real child SKUs live in `Variations[]`, each with its own `StockItemId`/`SKU` (live: `vnm-triplepads-yellowblack` → one template, one `ListingId` `285656695376`, covering both `vnm-triplepads-yellowblack-adt` and `-jnr`). `ListingIds` sits on the template and is **shared by every variation it covers**. **This is why dedupe does NOT need an eBay-API sweep at plan time**: the channel-SKU table's `ChannelReferenceId` is confirmed live to equal `ListingIds[0]` (`vnm_art_cruiser_deck`: `ChannelReferenceId "287277885799"` == `GeteBayTemplates` `ListingIds[0]`), so grouping input SKUs by their channel-SKU row's `ChannelReferenceId` IS the per-SKU→listing-id index already, for free. **⚠️ SUB-SOURCE NAMING MISMATCH, live-confirmed same tenant/same SKU:** the channel-SKU table's `SubSource` for eBay is **`"EBAY0"`** (matches the Listings API's `AccountId`), but `Inventory/GetInventoryItemDescriptions`' EBAY row for the exact same item (`vnm_art_cruiser_deck`) carries `SubSource` **`"EBAY0_UK"`** — a different string. An exact match (the plain `_effective_channel_value` rule) would silently miss the real per-channel description override on every single push and always fall back to the (usually absent) default row. `_ebay_effective_description()` tries the exact match first (reusing `_effective_channel_value`, "the same rule used elsewhere"), then falls back to a case-insensitive **prefix** match — mirroring the Amazon account-prefix precedent in `_resolve_glt_target`. **⚠️ Seller design-template preservation:** every `Description` read across ~1,600 live templates (9 populated configurators swept while building this) contained plain inner content with **no wrapper markers** ("Check out our eBay reviews", "Need Help Deciding?", etc. — none found), and for `vnm_art_cruiser_deck` the template's `Description` was **byte-identical** to the item's own `EBAY`/`EBAY0_UK` description row. So the tool sends **only** the `Description` field of the full, freshly-read template object — Title/Attributes/Categories/Price/etc. are carried through unchanged (the "nulls clear omitted fields" convention every other Linnworks update endpoint in this repo has already forced) — believed to preserve any seller wrapper, which appears to live outside anything Linnworks stores. **Not checked against a listing known to use a visible wrapper** — the post-merge steps below cover that. **The contradiction is resolved (criterion 12):** `NON_GLT_SOURCES`'s comment, `_resolve_glt_channel`'s ValueError, and the docstrings/inline messages of `unpublish_channel_listing` / `delist_all_channel_listings` no longer claim eBay has "no public listing-management API" — they now say it has no **GLT** template/API (still true — `GetConfiguratorsInfoPaged` still 400s "Invalid parameter request" for eBay, unchanged from v1.30.0/v1.32.0) and point at this tool for the one thing eBay's own API *can* do. Mirakl/Etsy/CDiscount are untouched, still fully manual, not re-probed. **New `EBAY_CHANNELS` registry** (mirrors `GLT_CHANNELS`'s shape) holds `revise_proven: False`; `_proven_ebay_revise_channels()` derives every warning from it, the same fix already applied twice to `GLT_CHANNELS` (`_proven_delete_channels`/`_proven_revise_channels`) so a hard-coded "unproven" string can't survive a future proof. New `WRITE_THRESHOLDS["revise_ebay_listing_description"] = 10` (the destructive tier shared with `unpublish_channel_listing`/`delist_all_channel_listings` — this writes to a live, unproven, customer-facing listing). Every result row/response carries a `verification_note` naming the **description-frame URL** (`https://vi.vipr.ebaydesc.com/itmdesc/<item_id>`) and warning that the item page will NOT show a fresh push (the description sits in a cross-origin frame there) — not independently verified against a live rendered page from this build, but the URL shape is a well-known, widely-documented eBay description-hosting pattern; confirm it resolves before relying on it. ~~**⚠️ NOT LIVE-PROVEN.** `ProcesseBayListings` was never called against a real listing — deliberately out of scope for this issue (see the brief's Post-merge verification).~~ **← SUPERSEDED the next day (v1.48.1, issue #47) — see that entry for the live proof and its outcome.** Every live-run row is reported `unconfirmed`, never `success`; a 204/accepted response only means Linnworks accepted the push. **Follow-up for the human proof (owner, in Claude Desktop), per the issue:** pick ONE low-risk eBay listing (ideally single-variation); dry-run first and confirm the manifest shows the expected description; run for real on that one listing only; verify on the description-frame URL for that item id (NOT the item page); if the shop furniture (e.g. "Check out our eBay reviews") is missing afterwards, STOP and restore from Seller Hub/the Linnworks GLT UI — that means the tool replaced the whole body rather than the inner content; only then flip `EBAY_CHANNELS["ebay"]["revise_proven"]` to `True` and record the listing id/timestamp/what was checked here; only then try the real multi-variation target and confirm ONE push, not one per SKU. 23 tests (`tests/test_revise_ebay_listing.py`, incl. the QA round 1 fix below), 516 total; 85 tools (84 → 85, first new tool since v1.44.0/#41).
>
> **QA round 1 fix, same build — two flagged traps were built but not addressed; both are now closed.** (1) **Stale-snapshot family** — the live push already sent the FULL stored `EbayListing` template back with only `Description` swapped, so `Title`/`Price`/everything else on it goes live exactly as Linnworks last stored it (the same hazard `ProcessTemplates Update` hit on Shopify in v1.27.1). The brief's own trap text required "whatever gets pushed must be shown in the dry-run manifest and read back afterwards" — neither half was met: the dry run never resolved the template (by design, to avoid the sweep cost) so a stale stored title/price was invisible until a live push, and there was no read-back at all. Fixed by resolving the template during PLANNING for every unblocked plan row — dry run included — via the existing `_find_ebay_template_for_listing()`, now cached per listing id within one call so a live run right after a dry run does not re-sweep. Each plan row gains `template_found` and a new `staleness` block (`stored_title`, `stored_price`, `title_stale`, `warning`) from the new `_ebay_template_staleness()` helper — title is compared against the item's own current base title (free, already resolved by `_resolve_bulk_inputs`); price is shown but deliberately NOT independently re-fetched (another read per row, and meaningless as a top-level value on a multi-variation template) — reviewing the shown `stored_price` is left to the human confirming the batch. A live push now also re-checks the template afterwards (`post_push_description_matches` on each result row, via a cheap CONFIG-SCOPED re-sweep using the new `config_id` parameter on `_find_ebay_template_for_listing()`/the extracted `_sweep_ebay_config_for_listing()` helper) — a read-back of Linnworks' own stored record, explicitly NOT proof the live eBay listing changed (that remains unconfirmable, per the verification-surface trap). (2) **A channel-SKU row proves a mapping, not a live listing** — the tool judged "is it listed" from a non-empty EBAY channel-SKU row alone, the exact v1.42.1 TikTok mistake (rows with `LastUpdate` `0001-01-01` mistaken for live channel state) this issue's own trap list named by number (assumption #13). Fixed: each planned SKU's `LastUpdate` is now carried through (`_is_never_synced_channel_sku()` treats the null date OR a missing value as never-synced — "unknown" must not read as "confirmed live"), and every plan row carries `never_synced_skus` (covered SKUs whose mapping has never been confirmed) plus a `never_synced_warning` naming them when non-empty. These SKUs are still planned rather than force-blocked, since a never-synced mapping is a caution for the human reviewing the manifest, not proof the listing is dead. Both fixes are additive to the plan-row shape only — no tool signature, threshold, or existing-field meaning changed. 5 new tests (store: `test_dry_run_manifest_shows_the_stored_template_title_and_flags_it_stale`, `test_dry_run_manifest_shows_the_stored_template_even_when_not_stale`, `test_live_push_is_read_back_afterwards_and_confirms_or_denies_the_match`, `test_never_synced_channel_sku_row_is_flagged_not_silently_trusted`, `test_synced_channel_sku_rows_carry_no_never_synced_warning`); the pre-existing `test_dry_run_never_sweeps_the_ebay_listings_api_either` was renamed to `test_dry_run_previews_the_stored_template_but_makes_zero_write_calls` and its mock updated, since a dry run now deliberately DOES sweep the eBay Listings API (a read, for the stale-snapshot preview) while still making zero calls to `ProcesseBayListings` (the write).
>
> **QA round 2 fix, same build — one criterion half-implemented, one trap not addressed at all; both now closed.** (1) **Criterion 10 half-implemented** — the not-yet-proven `revise_proven` flag was surfaced only on the top-level response, not on `plan` rows, even though the precedent this criterion is modelled on (`refresh_channel_listing`) puts it on both. `row_out` now carries `"revise_proven": EBAY_CHANNELS["ebay"]["revise_proven"]` alongside every other per-row field, so a reviewer looking at one plan row (rather than the response wrapper) still sees the channel is unproven. `test_revise_proven_flag_and_message_are_derived_from_the_registry` was extended (not just re-asserted at the top level) to check `row["revise_proven"]` on every plan row in both the default-False and `patch.dict`-flipped-True states, so the derived-not-hard-coded property is now proven at row level, matching how it was already proven at response level. (2) **"eBay refuses or partially applies a revise" trap was entirely unaddressed** — the tool already re-reads the pushed template afterwards (for `post_push_description_matches`) but discarded everything else on it, including the two fields (`Status`, `ErrorMessage`) that are exactly where eBay's own per-listing refusal (active bids, a recent sale, category restrictions) would show up, since it comes back as a stored condition rather than a transport error and the push call itself is a bodyless 204. Fixed: the same read-back object now also populates `post_push_status` and `post_push_error_message` (verbatim, `None` when the read-back itself failed) on every `unconfirmed` result row — no new API call, since the data was already being fetched and read for one field only. The tool still does not interpret or act on these fields (out of scope — v1 is description-only), it only stops throwing them away; the docstring, the module-level trap comment block, and this note all state that the human reviewing results must read them, since an `unconfirmed` outcome and a bodyless 204 given `Status:"Error"` would otherwise look identical. Both fixes are additive to the plan-row/result-row shape only — no tool signature, threshold, or existing-field meaning changed. 2 new tests (`test_ebay_side_refusal_status_and_error_message_reach_the_result_row`, `test_ebay_side_refusal_fields_are_none_not_missing_when_clean`), plus the extended `test_revise_proven_flag_and_message_are_derived_from_the_registry` — 25 tests in `tests/test_revise_ebay_listing.py`, 518 total.
>
> **QA round 3 fix, same build — criterion 11 violated on the tool's heaviest quota-consuming path.** `_sweep_ebay_config_for_listing()` and `_find_ebay_template_for_listing()` both caught `(RateLimitError, RuntimeError)` in the same `except` and collapsed either into the same `None` that means "no template covers this listing" — folding a genuine 429 into "not found" is exactly the #34/#37 mistake this repo has already fixed twice elsewhere (assumption #12). This sweep is the tool's single heaviest quota consumer (no by-listing-id lookup exists on this API — see the module note), it runs on **every** unblocked plan row, and — since the round 1 fix — on dry runs too, so it sits on the common path, not just the write path: a 10-row batch at the staging threshold can issue hundreds of `GeteBayTemplates` reads against a 150/min quota. Reproduced: with `GeteBayTemplates` raising `RateLimitError` after the retry ladder is exhausted, the dry run came back `complete: True`, `rate_limited: []`, and the row read `template_found: False` with a "could not locate the eBay template" message — the exact "not found" wording criterion 11 forbids for a quota failure, and a partial run reading as a complete one. Fixed: `RateLimitError` is no longer caught in either sweep helper — it now PROPAGATES to the caller (a `RuntimeError`, a genuine 400, still degrades to `None` as before). Three call sites now catch it explicitly: (1) the plan-loop template resolution (both when the config id must first be swept via `GeteBayConfigurators` and when `GeteBayTemplates` itself hits the quota) buckets the listing into `rate_limited` and marks the row `blocked` with the distinct reason `"rate_limited_locating_template"` (never `template_found: False`); (2) the live-run loop reports that specific reason as outcome `"rate_limited"`, not `"blocked"`, so it can't be confused with an ordinary block; (3) the post-push read-back (a second, config-scoped sweep) now also buckets a quota failure into `rate_limited` and sets a new `post_push_read_back_rate_limited: True` on the result row, so `post_push_description_matches: None` is never silently indistinguishable from "the quota ran out while checking". **Related fix, same site (per the QA note, not a separate finding):** a row whose template genuinely can't be located — no quota issue, just nothing found — was previously left `blocked: False` at plan level (only the live-run loop refused to push it), so a dry run's "N listing(s) would be revised" count included listings the live run would then refuse. That row is now also marked `blocked: True` with reason `"could not locate the eBay template serving this listing id"`, so the manifest's pushable count can't promise a push the tool won't make. Both fixes are additive to the plan-row/result-row shape only — no tool signature, threshold, or existing-field meaning changed. The pre-existing `test_never_synced_channel_sku_row_is_flagged_not_silently_trusted` needed its fixture updated (a locatable template) to isolate the never-synced assertion from the now-separately-blocking unlocatable-template case. 4 new tests (`test_rate_limit_while_sweeping_templates_is_bucketed_not_reported_as_not_found`, `test_rate_limit_while_listing_configurators_is_bucketed_not_reported_as_not_found`, `test_dry_run_marks_unlocatable_template_as_blocked_not_pushable`, `test_post_push_read_back_rate_limit_is_bucketed_not_silently_none`) — 29 tests in `tests/test_revise_ebay_listing.py`, 522 total.

> **v1.47.1 — Amazon Revise live-proof attempts: ACCEPTED BUT NO OBSERVABLE EFFECT, twice; `revise_proven` stays False, tracked as issue #45 (25 Aug 2026):** Docs-only. The v1.47.0 follow-up was run — two single-listing live proofs, both `processed: true`, neither changed anything Amazon-visible. **Proof 1 (title, FBA variation child):** `vnm_bushings_con_80a.FBA` (B07NQNKHT7), template 32064, 441-day snapshot stale on title; pushed 24 Aug ~20:10Z; title polled every 20 min for 12h — unchanged. **The observable turned out to be invalid:** the ASIN is a variation-family child whose displayed title is parent/contribution-driven — the live title ("Venom Skateboard**s** …") matches NEITHER the stored snapshot NOR the channel override, and search results render the parent's "… Soft/Medium/Hard - Full Set". Lesson for any future proof: **a variation child's TITLE cannot prove a revise on Amazon**; pick a seller-feed-controlled field. **Proof 2 (price, MFN standalone — the decisive one):** `vnm_artdeck_8.0` (B08QW9NJ2Y), template 32239, 441-day snapshot stale on price (£19.95 stored vs £22.95 live) and image_count (9 vs 12); pushed 25 Aug ~08:12Z; price polled every 3 min for 1h — **never moved off £22.95**, and offer price is per-SKU, seller-feed-controlled, immune to the contribution/variation merging that invalidated proof 1. No image-set reversion observed either. **Loopholes closed:** no Linnworks-side error on any readable surface (template status "Listed", `LastModificationTime` unchanged — no rebuild, per #27); the channel-SKU row's `last_update` ticks (18:56Z→02:27Z→09:04Z) are the ~7-hour periodic price/stock sync, not push reactions — which also means a landed stale-price push on a sync-managed (`ignore_sync: false`) listing would self-heal within hours; and Seller Central carries **NO feed log for these submissions** — Linnworks submits under its own SP-API app, so the processing report is visible only to Linnworks. **Conclusion: the #40 failure mode one channel over — on Amazon, ProcessTemplates Revise is accepted and, on every surface we can read, does nothing**, for both listing shapes tried (FBA child, MFN standalone). The #42 machinery behaved exactly as designed throughout: stale-snapshot warnings fired, acceptance-≠-proof messaging was accurate, and the unproven-channel warning is now evidence-backed rather than precautionary. **Two live structural findings worth keeping:** the bushings variation family resolves ONE TEMPLATE PER CHILD (each child SKU found its own template directly; the child→parent fallback never fired — first live evidence on the open variation-shape question, and it is TikTok's shape, not Shopify's, for this family), and `ven_ttool` holds TWO live templates (merchant + `.FBA`) on one item — the multi-template shape v1.47.0 planned for, confirmed in the wild. Investigation routes (Linnworks support with template ids/timestamps; `Update` vs `Revise`; a manual GLT-UI revise on the same art-deck listing to isolate the API path from the integration) are in **issue #45**. No runtime path touched; 493 tests and 84 tools unchanged; versioned as a patch per the v1.44.1 precedent, because the shipped "spec-based, not live-proven" caveat has now hardened into a live-tested negative.

> **v1.47.0 (issue #42) — `refresh_channel_listing` generalised beyond Shopify (Amazon first); spec-based, NOT live-proven on Amazon (24 Aug 2026):** Same extension `unpublish_channel_listing` got in v1.32.0 (issue #30), applied to the revise side: a new `channel` argument (default `"Shopify"`, back-compat unchanged) routes through the same `GLT_CHANNELS` registry and `_resolve_glt_target` — no new hard-coded channel strings, and a non-GLT channel (eBay/Etsy/Mirakl/CDiscount) raises the same clear error naming the supported channels. **Three Shopify-only assumptions had to go, each because they would have silently misbehaved on Amazon rather than errored:** (1) **the "is it listed?" check compared every channel-SKU row against `SHOPIFY`** — an Amazon SKU would always read `not_listed` regardless of channel; `_rows_on_store` now matches the resolved channel's `Source`. (2) **templates were keyed ONE PER STOCK ITEM ID, last-write-wins** — the same defect class already fixed once on the take-down tool (v1.32.0): Amazon can return a merchant AND an `.FBA` template for one item, and the old code would silently drop one, reporting a clean refresh while half the listing stayed on its stale snapshot. Templates are now collected as a **list per item**, and the plan is **one row PER TEMPLATE** (`templates_on_item` on each row), with a live run pushing each template separately. (3) **the staleness pre-flight's title/price override lookup was hard-coded to the SHOPIFY source** (`_effective_channel_value`, `_glt_template_staleness`) — an Amazon channel-title override would be invisible, so the check would compare the template against the *base* item title and misfire on exactly the channel-specific content this issue is about. Both functions now take a `channel_source` parameter threaded from the resolved channel. **New `revise_proven` flag on `GLT_CHANNELS`, alongside `delete_proven`, with a derived `_proven_revise_channels()` helper mirroring `_proven_delete_channels()`** — `True` on Shopify only (the catnip push, v1.27.1), `False` everywhere else. Every plan row and the top-level response carry `revise_proven`; an unproven channel gets an explicit warning naming the registry's proven list, so it can't go stale the way the old hard-coded delete warning did (that survived two proofs while still claiming neither had happened). The live-run message was also generalised: it now says plainly that `processed: true` is acceptance, not proof of a change, and to read the listing DATA back (`get_channel_listings`) rather than the channel's detail page — with an explicit callout that Amazon's detail pages can lag the catalogue by up to a day, so a green result plus an unchanged detail page proves nothing either way. Amazon's `NextSuggestedAction:"NotAllowed"` was confirmed (in the new test fixtures, mirroring the live shape recorded for the take-down tool) to still fall through correctly to `action="Revise"` via `IsAllowedToRevise`. **⚠️ SPEC-BASED, NOT LIVE-PROVEN ON AMAZON OR TIKTOK.** Only the Delete action has been fired live on those channels (v1.32.0/v1.42.0) — nothing in this repo proves a Revise/Update actually lands on an Amazon template, and Amazon carries hundreds of templates of unknown age with no API route to rebuild a stale one (#27), so a well-intentioned bulk refresh could silently revert live content the same way the Shopify catnip push did. No live push was fired from the build environment — that is explicitly out of scope for this build; the one-listing proof needs a human in Claude Desktop. Whether an Amazon variation family's template hangs off the parent (Shopify's shape) or each child (TikTok's shape) also remains unestablished — the existing child→parent fallback runs unchanged regardless of channel, and could open the wrong template, or none, on Amazon; flagged in the tool docstring rather than guessed at. **Follow-up for the single-listing live proof (owner, in Claude Desktop):** pick ONE low-risk Amazon listing; open it read-only first and check `staleness`/`LastModificationTime` to confirm the template snapshot is actually fresh (a stale push on Amazon carries the same overwrite risk the Shopify catnip incident proved); push it; then read the listing back via `get_channel_listings` (or Amazon's own reporting) — never the detail page, which can lag a day — to confirm the value actually changed; only then flip `GLT_CHANNELS["amazon"]["revise_proven"]` to `True`. 18 new tests (`tests/test_refresh_channel_amazon.py`), 493 total. No tool-count change (84) — this extends an existing tool rather than adding one. `test_refresh_staleness.py` and `test_refresh_variation_fallback.py` needed no assertion changes; only their mock patch target moved from the Shopify-scoped `_fetch_shopify_configurators` alias to the shared `_fetch_glt_configurators` the new code path actually calls.

---

> **v1.46.0 — `fileUpdate` detach LIVE-PROVEN on a throwaway product; and it exposed a filename bug that made the tool re-attach the SAME image forever (19 Aug 2026):** The one untested half of #41 is now proven, on an isolated fixture rather than a customer-facing page — a **DRAFT** product `ZZZ-MCP-TEST-DETACH-41` (`9500111962358`) created with 3 Linnworks-GUID images, then torn down. **Detach behaves exactly as documented:** `fileUpdate(files:[{id, referencesToRemove:[productId]}])` on 2 media returned no `userErrors`, the product dropped 3 → 1 media, the keeper and the featured image were untouched, and **both detached files still existed afterwards as `READY` MediaImage nodes serving HTTP 200** — so **detach ≠ delete is now VERIFIED, not assumed**, and a wrong removal is recoverable from Shopify Files. Also learned in teardown: **`productDelete` DOES destroy the media still attached to it** (the keeper's file id 404'd afterwards) while previously-detached files survive independently — so the two operations differ in exactly the way that matters for recovery. **⚠️ THE REAL FIND, and it is a live bug the fixture surfaced by accident: SHOPIFY UNIQUIFIES A FILENAME WHEN ONE OF THAT NAME ALREADY EXISTS IN FILES**, appending `_<uuid>` — `7385b433-….jpg` came back as **`7385b433-…_debe98f1-c8d5-4820-9178-bf41a2690ee3.jpg`**, because that image had been attached to `RS-102201` hours earlier. The v1.44.0 matching key required the WHOLE stem to be a GUID, so the suffixed form failed `_GUID_RE` — **and the failure is asymmetric in the worst way.** The media reads as `unmanaged`, so it is never detached (the safe direction), but the Linnworks image reads as **MISSING**, so the tool ATTACHES IT AGAIN — and again on every subsequent run, each new copy getting a fresh suffix. **Unbounded duplicate image growth on a live product page, from a tool reporting success.** This is not an edge case, and the sting is that **this tool manufactures its own trigger**: `fileUpdate` detaches without deleting, so the file stays in Files, and the next attach of that same Linnworks image collides with it. Sharing one image across products does it too. **Fix: `_linnworks_image_id()` extracts a LEADING GUID** (`^<guid>(_|$)`) instead of requiring an exact match, and every keying site — the diff, the featured-image lookup, the post-add poll, and the read-back — now runs off that resolved id (carried as `lw_image_id` on each normalised media row) rather than the raw stem. `prefix_<guid>` still resolves to None, so the GUID must lead and a hand-uploaded file is still `unmanaged`. **Verified against the fixture's real suffixed filename: matched 1, missing 0, unmanaged 0, `needs_repair: False`** — where before the fix it would have queued a duplicate attach. 11 new tests (47 in the file), 475 total. No tool-count change (84). **This retires the "NOT yet live-run" caveats on the detach path in the v1.44.1 and v1.45.0 entries.** Worth keeping: the v1.44.1 note recorded that the GUID key "holds for images Shopify fetches itself" — true, but only for a FIRST upload; the second upload of the same image is renamed, and one fixture found what 240 scanned products did not.

> **v1.45.0 — the hunt for a stale image found one, and it proved the detach half was WRONG BY DEFAULT: new net-loss guard on `repair_channel_listing_images` (19 Aug 2026):** Went looking for a genuinely stale listing to live-prove the one untested half of #41 (`fileUpdate` detach). **Scanned 240 products across three slices** — 25 `-vtk-c-rawBLT` composites, 150 broad catalogue, 65 eyewear/Venom — using a cheap `mediaCount` first pass before pulling media URLs. **Finding 1: this catalogue's drift is almost entirely MISSING images, not stale ones.** 49 of 50 in the first broad batch had FEWER media on Shopify than Linnworks; **zero** products anywhere had excess media except one. All 25 BLT items showed the same shape — 2 Linnworks images, 1 on Shopify — matching the v1.43.0 staleness finding. **Finding 2 — the one real candidate broke the tool's core assumption.** `luma-huxham-blue` (product `9072417374454`): 12 Linnworks images vs **14** Shopify media → 8 matched, 4 missing, **6 superseded**, standalone (so the variation gate correctly permitted removal). The tool planned `attach 4, detach 6`. **Looking at the actual images stopped it: the 6 "superseded" media are LIFESTYLE SHOTS (models wearing the sunglasses outdoors) and the 4 incoming Linnworks images are STUDIO PACKSHOTS.** Detaching would have stripped six pieces of marketing photography off a live product page and replaced them with product-on-white — a content downgrade produced by a tool behaving *exactly* to spec. **The root problem is that `superseded` is not the predicate anyone actually wants.** It means "Linnworks pushed this once and no longer has it", which cannot distinguish *replaced by a better version* (detach correct) from *removed from the catalogue while still earning its place on the storefront* (detach destructive). The `unmanaged` protection does not help — these images DO carry Linnworks GUIDs, because Linnworks put them there years ago. **Fix: a net-loss guard.** Removing MORE media than you add is the signature of a storefront richer than the catalogue, so `remove_superseded` now blocks when `len(superseded) > len(missing)`, reporting the counts in `removal_blocked_reason`, with **`allow_net_media_loss=True`** as the explicit override. An even swap (1 replaced by 1 — the actual #41 shape) and a net gain both stay allowed, so the ordinary repair is untouched. The variation gate still takes precedence, and its tests assert on the variation reason specifically so the two guards cannot be confused. **Live-verified on the real product: `to_detach` goes 6 → 0, blocked with the counts named, while the 4 missing packshots are still offered.** 4 new tests (36 in the file), 464 total. No tool-count change (84). ~~**⚠️ `fileUpdate` (detach) IS STILL NOT LIVE-PROVEN**~~ **← SUPERSEDED: proven on a throwaway product the same day, see v1.46.0 above.** The natural candidate in 240 products was still one that must NOT be detached, so nothing was fired against a live page. That is the honest state: the hunt produced a safety fix rather than a proof, and the proof still wants either a product whose superseded media are genuinely old versions, or an isolated throwaway Shopify product. **The wider lesson, and it is the #40 lesson again from the other side: a classifier can be correct on its own terms and still be the wrong question. `superseded` was measuring provenance when the decision needed value.**

> **v1.44.1 — the Shopify write path is now LIVE-PROVEN on `RS-102201`; and Shopify preserves the Linnworks GUID on images IT fetches, not just historical ones (19 Aug 2026):** Docs-only. v1.44.0 shipped `repair_channel_listing_images` with the write path validated against the live Admin schema but **never fired** — that warning is now wrong, and a stale "not yet proven" note is exactly the kind that silently caps later work (assumption #10). **Fired live, with the owner's explicit go-ahead, on the drifted deck the dry run found: `RS-102201` (product `9425839882486`), which had 4 Linnworks images but only 2 on Shopify, the Linnworks MAIN absent entirely and `be95aa80` featured instead.** All three mutations behaved as designed: `productUpdate(media:)` accepted both images with no `userErrors`; both reached **READY with empty `mediaErrors`**; `productReorderMedia` moved `7385b433` to position 0. Verified on the real surface — `featuredMedia` is now `38846347477238` (= the Linnworks main), 4 media all READY, and both new CDN URLs serve **HTTP 200 image/jpeg**. Re-running the shipped tool against the post-repair state returns **`repairable_count: 0`, `matched: 4 of 4`, `featured_is_correct: True`** — i.e. the tool's own diff agrees with the storefront. **⚠️ FINDING 1 — the GUID matching key HOLDS FOR IMAGES SHOPIFY FETCHES ITSELF.** The key discovery in v1.44.0 was observed only on historical media, leaving open whether Shopify would preserve the Linnworks GUID on a *new* upload or mint its own filename; it preserves it (`…/files/7385b433-6516-47e5-ae2c-53e37ae16750.jpg?v=1787148351`). That matters because the **read-back depends on it** — had Shopify renamed the file, every live repair would have reported `still_missing` for images it had just successfully attached. **⚠️ FINDING 2 — `productReorderMedia` IS ASYNCHRONOUS.** It returns a `Job` with **`done: false`**, so the featured image is not set when the mutation returns; the job read back `done: true` shortly after and the product then showed the new featured media. The tool's read-back re-reads the product (not the job), which is the right surface, but a caller polling too fast could see the OLD featured image and wrongly conclude the reorder failed. Adds to the "creates are async, deletes are sync" family (#38/#39). **⚠️ Note what was NOT exercised here: `fileUpdate` (detach)** — `RS-102201` had zero superseded and zero unmanaged media, so this run proved attach + featured only. **Detach was proven separately on a throwaway product (v1.46.0).** Docs-only; no runtime path touched (460 tests, 84 tools unchanged), versioned as a patch because the "not yet live-run" claim had already been committed and pushed.

> **v1.44.0 (issue #41) — `repair_channel_listing_images`: the image job the GLT cannot do; and Shopify's CDN filename turns out to carry the Linnworks image GUID (19 Aug 2026):** When an item's images are corrected in Linnworks there has been **no supported route to get the new picture onto an EXISTING Shopify listing**. `refresh_channel_listing` cannot: it pushes the template's STORED snapshot (#27/#40), so it re-sends the old — sometimes **deleted** — URL and returns `processed: true` having changed nothing; and re-listing is not an option on a live product. Every occurrence to date was fixed by hand through the Shopify Admin API. This tool does that repair, in bulk, and **deliberately bypasses the GLT**, so it stays useful whether or not #27 is ever unblocked. **⚠️ FIRST TOOL IN THIS SERVER THAT WRITES TO A SYSTEM OTHER THAN LINNWORKS** — it needs Shopify Admin credentials (scopes `read_products`, `write_products`, `read_files`, `write_files`) which Linnworks cannot supply, configured as `SHOPIFY_STORES` (JSON, per-SubSource — this tenant runs five Shopify stores) or the single-store `SHOPIFY_SHOP_DOMAIN`+`SHOPIFY_ADMIN_ACCESS_TOKEN`+`SHOPIFY_DEFAULT_SUB_SOURCE`; `SHOPIFY_API_VERSION` defaults to `2026-01`. **With none configured the tool writes nothing and returns setup instructions** — it never half-runs. **⚠️ THE HEADLINE DISCOVERY — Shopify's CDN filename PRESERVES the Linnworks image GUID** (live-confirmed across 3 products / 4 media): Linnworks `image_id` `88b7b1da-…` appears as `.../files/88b7b1da-….jpg?v=…`. That gives a **per-image identity**, which is exactly what #40 could not have — the GLT template exposes only an image COUNT, so "1 == 1" hid a wrong picture (assumption #15). Everything else follows from it: media whose stem is a GUID **not** on any item sharing the product is `superseded`; media whose stem is **not** a GUID was never put there by Linnworks (hand-uploaded photo, size chart, lifestyle shot) and is reported `unmanaged` and **NEVER auto-removed**. **⚠️ THE HAZARD THAT SHAPED THE DESIGN — on Shopify a variation group is ONE product shared by every variant (#26), but each variant is a SEPARATE Linnworks item with its OWN images.** So a sibling's live photo sits on the same product and, being a Linnworks GUID absent from *this* item's list, looks **exactly** like a superseded image — removing it would delete a selling variant's picture while the tool reported a clean repair. The owning set is therefore the **whole group** (parent + children, one variation lookup + one batched image read), a file is only superseded when it belongs to **none** of them, and removal is **disabled entirely** on a shared product unless every member is passed in `skus`. **Live-proven on the real 7-member catnip group (product `9280707657974`): one child passed → 2 sibling images classified `matched`/`belongs_to_sibling`, `to_detach` EMPTY, blocked with the group named; whole group passed → only the genuinely stale image queued.** A failed variation lookup degrades to "assume shared" (removal off), never to "assume standalone" — **and the test suite caught that this was documented but not implemented**, which is the exact "unknown must not read as safe" rule from `find_composite_parents`. **Mutations — all four validated against the LIVE Admin schema, and the manual procedure's two are now DEPRECATED:** `productCreateMedia` → **`productUpdate(product:{id}, media:[CreateMediaInput!])`**, `productDeleteMedia` → **`fileUpdate(files:[{id, referencesToRemove:[productId]}])`**, plus **`productReorderMedia`** (zero-based `MoveInput.newPosition`) for the featured image, since **Linnworks' `is_main` does not drive Shopify's featured image**. `fileUpdate` **DETACHES** and leaves the file in Shopify Files — strictly safer and recoverable, where `productDeleteMedia` destroyed it. **Ordering is load-bearing: nothing is detached until its replacement reaches READY**, because Shopify processes uploads asynchronously (same shape as GLT creates, #38 — a read-back taken straight after the write proves nothing) and detaching first is precisely how a product ends up with **no image at all**, an observed #41 failure mode. Each SKU is verified by **re-reading the product from Shopify** afterwards (the "verify against the REAL surface" rule), yielding `in_sync` / `still_missing` / `detach_failed` / `featured_is_correct`; a 2xx alone is never reported as a repair. Blocked SKUs carry a `blocked_reason` (`not_listed` · `not_found` · `no_linnworks_images` · `no_product_reference` · `shopify_product_missing` · `shopify_read_failed`), 429s go to a separate `rate_limited` bucket with `complete: false` (#34/#37), and Shopify's cost-based throttling is retried on the same 5/10/20/30s ladder. Threshold **10** (live customer-facing pages), `dry_run=True` default. **Live-verified 19 Aug 2026 with the Linnworks side fully live and REAL Shopify media captured from the Admin API — and it found genuine drift immediately: `RS-102201` has 4 Linnworks images but only 2 on Shopify, the Linnworks MAIN (`7385b433`) is absent entirely, and the featured image is `be95aa80`, i.e. NOT the main** → plan `attach 2 image(s)` + `set featured image (after attach)`. The two #41 SKUs, already repaired by hand, correctly return "nothing to repair". ~~**⚠️ The Shopify WRITE path is spec-based, NOT yet live-run**~~ **← SUPERSEDED THE SAME DAY: the write path was LIVE-PROVEN on `RS-102201`, see the v1.44.1 note above.** 32 new tests (`tests/test_repair_listing_images.py`), 453 total. 84 tools.

> **v1.43.1 — dependency caps: `mcp` pinned below 2.x, and the toolchain moved off Intel Homebrew Python (19 Aug 2026):** Packaging-only. **`requirements.txt` and `pyproject.toml` both declared an unbounded `mcp>=1.27.0`, which now resolves to `mcp 2.0.0` — and 2.0.0 REMOVED `mcp.server.fastmcp`, the module `server.py:26` imports.** So any fresh `pip install -r requirements.txt` produced a server that fails at import with `ModuleNotFoundError: No module named 'mcp.server.fastmcp'` (observed live while rebuilding the venv). Both files now declare **`mcp>=1.29.0,<2`**; a clean-install simulation resolves 1.29.0 and imports cleanly. **Context: Intel Homebrew Python was removed from this Mac (it sunsets this year), which broke the project `.venv` in three ways at once** — its interpreter symlinks pointed at the now-absent `/usr/local/opt/python@3.14`; its compiled wheels were **x86_64-only** (`_pydantic_core…so`) on an **arm64** machine, so merely repointing the symlinks would have swapped a missing-interpreter error for an `ImportError`; and `pyvenv.cfg` still recorded creation under the pre-rename path `Linnworks Apps/Linnworks Connector MCP/linnworks-mcp`. The venv was therefore **rebuilt**, not repaired, on `/opt/homebrew/opt/python@3.14` (3.14.3 → 3.14.7, arm64). Verified after the rebuild: 0 broken symlinks, arm64 wheels, `--list-tools` 83, `--check-auth` OK against `eu-ext.linnworks.net`, 428 tests green. **⚠️ If a future dependency bump wants mcp 2.x, `server.py`'s FastMCP import has to be migrated FIRST — the cap is load-bearing, not caution.** No runtime path touched; 428 tests and 83 tools unchanged.

> **v1.43.0 (issue #40) — `refresh_channel_listing` gains a pre-flight staleness check; and the honest finding is that the API CANNOT confirm a push changed anything (19 Aug 2026):** A live revise returned `processed: true` for two BLT templates (52731/52732) and left Shopify **byte-for-byte unchanged** — same media id, same `?v=` timestamp, after a 5-minute settle. Root cause is the #27 stored-snapshot hazard wearing a new hat: the template pointed at a Linnworks image URL that had since been **deleted** (HTTP 403), the item's images having been corrected afterwards. **`ProcessTemplates` returns an empty 2xx whether it changed the listing or not, so `processed: true` has always meant "accepted", never "changed" — assumption #2 on a write path that reports success.** The danger is directional, as usual: a silent no-op reads as "fixed", so the wrong image stays live indefinitely. **What the template actually exposes (live-probed, tpl 52731/39076): `Info` carries real VALUES only for `Title`, `Price` and `LastModificationTime`; `Images`, `Attributes`, `MetaFields` and `Description` come back as `Type:"Action"` SUMMARIES — a count ("1", "21", "42") or the literal "Filled".** So the check is **partial by construction** and says so: it compares **title · price (non-variation only) · image COUNT**, and always returns an explicit `undetectable_fields` list (image content/URL, description body, attributes, metafields). **Three traps, each of which produces a WRONG verdict and each now pinned by a test.** (1) **Title must be compared against the EFFECTIVE CHANNEL title, not the base `ItemTitle`** — the #40 items carry a channel-title override ("Zero Mega**death**…") differing from the base title ("Zero Mega**deth**…"), and the template correctly holds the override; comparing against the base would flag every override-carrying item stale. (2) **A VARIATION template reports `Price: 0.0`** because prices live per-variant (live: tpl 39076 → 0.0 against a real 79.95 channel price), so price comparison is SKIPPED there and reported in `skipped_comparisons` rather than flagged. (3) **MATCHING FIELDS ARE NOT A GUARANTEE** — the proven #40 no-op had title, price AND image count all matching. **The issue asked for `will_change: false`; that field is deliberately NOT implemented**, because on the very case that generated the bug it would have asserted freshness. The output is `comparable_fields_match` + `undetectable_fields` + a warning that names #40. **⚠️ THE SECOND REQUEST (post-push read-back) IS NOT ACHIEVABLE AND WAS NOT FAKED.** `Info.LastUpdateTime` advanced to `2026-08-18T15:48:36` on the **proven no-op push**, so it records that a push happened, not that anything moved; and `NextSuggestedAction` is **a constant** — "Update" with `IsNextSuggestedActionAllowed: true` on **24/24** sampled listed Shopify templates, including ones whose snapshots were seven weeks old. **This retires a wrong claim in this file and in the tool's own docstring, both of which called `NextSuggestedAction` "the action the GLT engine itself computed for pushing the pending change".** It is not evidence of a pending change; nothing the API returns can confirm the channel changed, so the message now says `processed: true` means ACCEPTED and to read the storefront. **The load-bearing signal is `LastModificationTime`** — the snapshot build time, free (already on the opened template) — and it is what would have caught #40: images corrected 18 Aug, snapshot built 12 Aug. It also dates the known-stale catnip template to **2026-02-16**, exactly matching the v1.27.1 incident (183 days). **Live-verified read-only 19 Aug 2026, and it found real damage waiting to happen: 9 of 9 planned Venom templates were genuinely stale** — all 9 holding 1 image against the item's 2, and **`1002079508`/`1002079509` (tpl 45491/45490) storing £137.95 against a current £139.95**, i.e. a push would have silently reverted the price by £2 on the live listing (independently confirmed against `GetInventoryItemPrices`). The #40 SKUs themselves correctly come back "no detectable difference" **with the no-op warning**, since their break is invisible to the API. New `check_staleness` param (default **True**, ~0.7s / 3 GETs per template; `False` skips the reads and says so). A failed read yields `checked: false` + `comparable_fields_match: None` — never a silent "matches" (the #37 lesson). 14 new tests (`tests/test_refresh_staleness.py`), 428 total. No tool-count change (83). **⚠️ Unchanged and still true: this does NOT fix the staleness itself (#27, blocked upstream) — a stale template still has to be rebuilt by opening the listing in the Linnworks GLT UI.**

> **v1.42.1 — RETRACTION: `ListedQuantity` is a STORED field, not live channel state; the "11 TikTok oversell listings" reported hours earlier in v1.42.0 do not exist (7 Aug 2026):** Docs-only. The v1.42.0 footprint note claimed 11 zero-stock TikTok SKUs showing `listed_quantity: 30` were "a live oversell exposure, and the obvious next cleanup batch". **They are not, and acting on it would have delisted live products.** Every one of those channel-SKU rows carries **`LastUpdate: 0001-01-01T00:00:00`** — the null date, i.e. the mapping has **never been synced** — so the 30 is whatever was written when the mapping was created and says nothing about what TikTok is serving. Checked against the authoritative surface (`OpenTemplatesByInventory`), **none of the 11 has a live TikTok listing**: **9 have NO GLT template at all** (mapping row only, unreachable from the API — the `no_glt_template` class from #35) and **2 (`vnm-gt-green`, `vnm-gt-red`) have a template reading `Status: "Not listed"` with an EMPTY `ActiveListingId`**. **⇒ Liveness must be judged on `OpenTemplatesByInventory` (`Status` + a non-empty `ActiveListingId`), NEVER on the channel-SKU table's `ListedQuantity`.** This is assumption #1 ("current data or a stored/cached copy?") missed on a field that *looks* live because it sits next to `IgnoreSync` and `LastUpdate` — and, as in #34, **the mistake pointed at the destructive answer**: "urgently take down 11 listings". **Second finding, equally load-bearing: ZERO STOCK ≠ DEAD — age before proposing any take-down.** Of the 16 remaining zero-stock TikTok SKUs, `vnm_tc_wheels_56mm` had been zero for **2 days** and `venom-helmet-Large` for **7**; only 7 were ≥90 days. Delisting a product that is merely awaiting restock destroys its listing history for something that is coming back. `get_stock_change_history(all_locations=True)` costs ~29 calls/SKU and **changed the verdict on 9 of 16 candidates here** — cheap at that scale, and it belongs in the chain *before* the delist, not after. **Net actually-retirable on TikTok: 4 templates, not 11** — 2 genuinely live dead listings (`ven-20-black-raw-core-complete-8.25` tpl 30010, `ven-20-natural-raw-core-complete-7.5` tpl 30001, both dead 207 days) plus the 2 orphaned `vnm-gt-*` templates; 3 more (`vnm_helmet_pads_combo_large_adult`/`_junior`, `vnm-triplepads-yellowblack-jnr`) correctly blocked as `variation_child_live_siblings` by the v1.41.0 gate. **Nothing was fired** — the owner held the batch after the retraction. Docs-only, no runtime path touched (414 tests unchanged, 83 tools); versioned as a patch because the wrong claim had already been committed and pushed, and a stale note in this file silently caps or misdirects future work (assumption #10). Two new entries added to the assumption list at the top of this file (#13 stored-vs-channel fields, #14 zero-stock vs dead) — that list is the thing actually read before trusting a new endpoint, so a finding that only lives in a version-log entry is a finding half-recorded.

> **v1.42.0 (issue #39) — TikTok `ProcessTemplates Delete` is now LIVE-PROVEN; and TikTok's variation shape is the OPPOSITE of Shopify's (7 Aug 2026):** TikTok has carried `delete_proven: false` since v1.32.0, so every plan and message warned about it and no bulk run could be trusted — while the tenant runs **5 TikTok configurators** (ChannelId 30, SubSource `SKATEWAREHOUSE_UK`), so the gap was real, not theoretical. **Proven the same way Shopify and Amazon were: ONE throwaway listing.** Target `ven-20-black-raw-core-complete-7.5` (template **30006**, configurator 112 "Skateboards - Completes", listing `1729486505080953421`) — dead since **12 Jan 2026** (~7 months), zero stock at all 29 locations, TikTok `listed_quantity` 0, `safe_to_retire: true` via `find_composite_parents` (0 composite parents). `ProcessTemplates` `Action="Delete"` with `ChannelType:"TikTok"`/`ChannelName:"TIKTOK"` → **verified on three independent surfaces**: the TIKTOK channel-SKU row disappeared (exactly one row), `OpenTemplatesByInventory` on ChannelId 30 went **1 → 0 templates**, and the Amazon + eBay + 2× Shopify rows and the stock levels were all untouched. **⚠️ FINDING 1 — ON TIKTOK THE VARIATION CHILD HOLDS ITS OWN TEMPLATE, which is the REVERSE of Shopify.** The proven item is a variation child of `ven-20-core-complete-skateboard`, yet it returned its own template with `templates_on_item: 1` and `via_variation_parent: null` — no parent fallback needed. On Shopify the template hangs off the PARENT and the children hold only channel-SKU rows (#26), which is the entire reason the v1.41.0 whole-group gate exists. **So a TikTok child can usually be retired individually, without endangering its siblings** — do NOT carry the Shopify mental model across. The gate is still correct and still fires for TikTok children that genuinely have no template: live-confirmed on `vnm-triplepads-yellowblack-jnr`, blocked as `variation_child_live_siblings` naming parent `vnm-triplepads-yellowblack` and its 1 live sibling. **⚠️ FINDING 2 — `Status: "Errors while updating"` does NOT block a Delete**, and neither did `NextSuggestedAction: "Update"` (we force Delete regardless). Same lesson as Amazon's `"NotAllowed"`: these fields describe the *suggested* action and the template's sync health, not permission to delete. A broken/erroring template is still deletable — which is useful, since erroring templates are exactly what cleanup targets. **⚠️ FINDING 3 — TikTok's duplicate channel-SKU rows are NOT the Amazon multi-template shape.** 8 of 93 TikTok SKUs carry two channel-SKU rows (helmets, Thunder Cloud wheels); re-opening two of them returned **`TotalEntries: 1` with an EMPTY `TemplatesInfo`** — i.e. two listings, zero GLT templates, unmanageable from the API. That also re-confirms the standing gotcha: **trust `TemplatesInfo`, never `TotalEntries`.** No multi-template TikTok item has been observed, so the per-template plan/read-back machinery (#36) is *defensive* here rather than load-bearing, unlike on Amazon. **The delete confirmed SYNCHRONOUSLY** (read-back straight after showed 0 templates), consistent with the v1.39.0 note that GLT deletes are synchronous while creates are async. **Also fixed: a stale in-code warning that survived two proofs.** `unpublish_channel_listing`'s unproven note hard-coded `"(only Shopify is)"` — it stayed wrong through the entire Amazon-proven period and would have gone wrong again here. It now derives the list from the registry via the new **`_proven_delete_channels()`** (→ "Amazon, Shopify, TikTok"), so it cannot drift from `GLT_CHANNELS` again — assumption #10 applied to our own strings. **Tenant TikTok footprint, measured this build:** a full sweep of 38,324 active items found **101 TikTok channel-SKU rows across 93 distinct SKUs**, of which **20 are zero-stock**; 11 of those 20 show `listed_quantity: 30` on **zero real stock**. ~~a live oversell exposure, and the obvious next cleanup batch~~ **← THIS CLAIM WAS WRONG AND WAS RETRACTED THE SAME DAY — see the v1.42.1 note above. Every one of those rows has `LastUpdate: 0001-01-01` (never synced) and NONE of the 11 has a live TikTok listing. `ListedQuantity` is a stored field, not live channel state.** **The test that used TikTok as the unproven-channel fixture now forces the flag instead of naming a channel**, so it stops silently becoming a no-op each time a channel is proven (it had already been re-pointed off Amazon once for exactly this reason). 2 new tests, 414 total. No tool-count change (83). **⚠️ Magento and Walmart remain `delete_proven: false` and have 0 configurators here — unproven AND untestable on this tenant.**

> **v1.41.0 (issue #35) — delist can now retire variation CHILDREN, but only when the whole group is going; and every block now says WHY (7 Aug 2026):** The first live dry run of the cleanup chain over the 42 oldest dead items planned **2 template deletions on 1 item** — **one item in 42 was retirable**, and the other 37 collapsed into a single opaque string, `"listed on the channel but no GLT template could be opened"`. **28 of those 37 were variation CHILDREN.** A child holds the channel-SKU rows but no template; the template hangs off the variation PARENT and serves every variant (the #26 shape), so ending one dead size means deleting a template that also owns its siblings. `refresh_channel_listing` got a child→parent fallback in v1.26.0; the take-down deliberately did NOT, and **that caution was right** — of the 21 parents behind those 28 children, **19 had live, in-stock siblings** (`Breezy-zyrsst-le`: 11 children, 1 dead; `SFR012WHITE`: 12, 3 dead), so the obvious workaround would have taken down 10 and 9 live selling listings to retire one variant. **Neither behaviour was correct: the tool couldn't retire the child, and the workaround was unsafe for 90% of groups.** **Fix: follow the parent only when it costs nothing.** A template-less child now resolves to its parent's template **iff every OTHER group member is either already un-listed on this store, or is itself being retired in the same operation** (`skus` + the new **`also_retiring_skus`**). Then the parent's template serves ONLY listings the caller asked to end. **⚠️ Liveness is judged on the channel-SKU ROW, not on stock** — a sibling listed at zero stock is still a customer-facing listing nobody asked to remove; and it is judged **per target store**, so a sibling live on Venom cannot block a SWH take-down (live-confirmed). New **`allow_variation_parent_takedown`** (default True, threaded through the fan-out and the Shopify wrapper) turns the whole behaviour off. **⚠️ THE READ-BACK SPLITS IN TWO, and getting this wrong would have been #36 again wearing a different hat:** on a parent take-down the TEMPLATE belongs to the parent but the channel-SKU rows belong to the CHILDREN — **the parent has none**, so re-reading the parent would return zero rows and score a clean `taken_down` with nothing having happened. Plan rows therefore carry **`template_stock_item_id`** (what to re-open) separately from **`listing_sids`** (whose rows must vanish), and a count is only trusted when it covers EVERY served item; a half-read now yields `unconfirmed`, never True. **Second fix — every block states its reason.** `unresolved[]` rows carry a **`blocked_reason`** code (`variation_child_live_siblings` — with `variation_parent_sku`, `live_sibling_count`, `live_siblings` — vs `no_glt_template` · `variation_parent_has_no_template` · `variation_child_parent_takedown_disabled` · `not_listed` · `template_locked` · `not_found` · `channel_read_failed`), plus `blocked_summary` / `blocked_count` / `retirable_sku_count`, and the message spells out "N of M requested SKU(s) CANNOT be taken down" so a 2-of-42 plan can never read as success. **The other 9 blocked SKUs (Triple Eight `337*`) are a genuinely different problem** — standalone, `variation_role=none`, listed on Shopify with NO template at all (listed outside the GLT), so nothing in the API can reach them; they now report `no_glt_template` pointing at the channel's own admin. **Also fixed: the fan-out delegated ONE SKU AT A TIME**, so a whole group handed to `delist_all_channel_listings` would have blocked itself — every sibling looked like a live listing nobody asked to end — and on a live run the first child's delete would succeed while the rest came back "no template". Delegation is now batched **per target (channel × account)**, letting the delegate dedupe by template (one delete `covers_skus` all 11), with `also_retiring_skus` carrying the rest of the batch on top for groups split across targets. Plan rows also gained `covers_skus`; a variation PARENT passed directly is now accepted (`listed_via_children`) with a warning naming every child listing it will end. **Live-verified read-only 7 Aug 2026 against the real groups from the issue:** one Breezy child → blocked, parent `Breezy-zyrsst-le` named, **10 live siblings**; all 11 children → **ONE** take-down (template 31351, `covers_skus` 11, `listing_sids` 11 = the children); `allow_variation_parent_takedown=False` → 11× `variation_child_parent_takedown_disabled`, not mislabelled; two real `337*` SKUs → `no_glt_template`; `RS-102201` → template 43285 unchanged (the same id recorded since v1.23.0); fan-out over the group → 1 take-down covering 11, and over one child → blocked with the reason in the summary. **The `ProcessTemplates` Delete push itself is unchanged and already live-proven (Shopify v1.25.0, Amazon v1.32.0); what is new is WHICH template gets selected, and that selection path is what was live-verified — no live delete was fired, since the issue did not authorise one.** 17 new tests (`tests/test_delist_variation_children.py`), 412 total. No tool-count change (83). **⚠️ Sizing note that has not changed: this fixes the 61 children in wholly-dead groups, NOT the 509 with live selling siblings** (BRAIN.md, 6 Aug) — those are dead VARIANTS inside live products and need Shopify-side variant deletion. The dominant cleanup failure mode is still not addressable from Linnworks.

> **v1.40.0 (issue #37) — `RateLimitError` no longer subclasses RuntimeError; the v1.37.0 fix was incomplete and I said it was done (7 Aug 2026):** v1.37.0 made `RateLimitError` inherit from `RuntimeError` with the stated reasoning that "existing `except RuntimeError` handlers keep working". **They did keep working — they kept working WRONGLY.** `server.py` has **55 bare `except RuntimeError` handlers and only 2 caught `RateLimitError` first**, and those handlers bucket a failure as a per-item DATA error (`unresolved`, "SKU not found"). So the exact defect #34 was meant to kill was still reachable through nearly every tool. **Observed live the same day:** `list_to_shopify` reported **116 of 266** SKUs as `unresolved` (all 429s, all real items); `get_stock_change_history` returned `"SKU 'X' not found in Linnworks: … HTTP 429"` **plus the hint "Archived items cannot be resolved by SKU — unarchive first"** — asserting a cause that had not happened; an ageing run lost **53 of 191** the same way, all fine on retry. **Fix (structural, and a deliberate breaking change): `class RateLimitError(Exception)`.** A bare `except RuntimeError` now *cannot* swallow it, so it propagates loudly instead of being mislabelled — the failure mode is impossible by construction rather than by discipline at 55 call sites. Tools that should degrade gracefully catch it **explicitly**: `get_stock_change_history`, `list_to_shopify`, `refresh_channel_listing` and `unpublish_channel_listing` now report a **`rate_limited`** bucket separate from `unresolved`, plus **`complete: false`** (joining `get_channel_listings_bulk` / `get_inventory_item_images_bulk` from v1.37.0), and the misleading archived hint is no longer attached to a quota failure. **Two `except Exception` swallows were also closed** — both silently turned a quota error into plausible-looking data: (1) in `get_stock_change_history` the stock-level read fell back to `{}`, and since **current level is what suppresses or produces `out_of_stock_since`**, a throttled read could make a stocked item look dead to the archive/delist chain; that SKU is now skipped into `rate_limited` instead. (2) `_supplier_map`'s best-effort resolve dropped items silently, which surfaces downstream as revenue landing in the **"No Supplier"** bucket — an under-attribution that looks like real data; it now re-raises. **11 `except Exception` sites remain by design** (JSON parsing, note-deletion errors, PO line errors) — those carry the exception text through, so a quota failure names itself rather than being relabelled. **Live-verified 7 Aug 2026:** `RateLimitError` MRO is `Exception → BaseException`; a bare `except RuntimeError` does not catch it; a genuine miss still reports `unresolved` **with** the archived hint (which is now only ever attached to real misses); and a real SKU still ages correctly (`venom-helmet-Large` → 2026-06-24, 43 days, `complete: true`). 5 new tests (24 in `tests/test_rate_limiting.py`), 395 total. No tool-count change (83). **The lesson worth keeping: "keeps existing handlers working" was the wrong goal — those handlers were the bug.** Backwards compatibility with a broken classification just preserves the break.

> **v1.39.0 (issue #38) — `list_to_shopify` gains a PRODUCT-level duplicate guard; the item-level dedupe worked perfectly and still created 177 duplicate Shopify products (7 Aug 2026):** The v1.19.0 dedupe asks *"is THIS StockItemId already listed on the target store?"* and answers correctly every time. It cannot see that a **different SKU for the same physical product** is already live, because the two SKUs are separate Linnworks items with separate StockItemIds. On 6 Aug 2026 a bulk run over the Surfboard Fins category (725 items, 459 correctly excluded, 266 listed) produced **177 duplicate product pairs on SWH Shopify**: the category carried the same fins under an old word-suffix scheme and a new code scheme (`21SP101411-black` vs `21SP101411-B`), and **184 of the 265 new listings had a byte-identical `ItemTitle` to a listing already live on the same store**. **This is the archetype of "the check passed and the outcome was still wrong" — the tool was asking a narrower question than the one that mattered.** Fix: a second dedupe layer matching on **normalised `ItemTitle`** (`_norm_title()` — lowercase + collapse whitespace, matched EXACTLY; deliberately not fuzzy, since exact match caught all 177 and anything looser starts blocking legitimately distinct sizes/colours). Matches go to a new **`possible_duplicates`** bucket and are **EXCLUDED from `CreateTemplates` by default**, with `allow_duplicate_titles=True` as the explicit override (which lists them but still reports them). The comparison set is the batch's own `already_listed` rows — **free, no extra API calls**, and correct whenever a whole category is passed, which is the normal usage; for batches containing only the new SKUs there is nothing in-batch to compare against, so **`known_listed_titles`** lets the caller supply live titles (e.g. from `list_inventory_items`). Each row reports `duplicate_of_skus`, `matched_on` and `listed_anyway`, and a `duplicate_warning` names the incident so the exclusion isn't mistaken for a bug. **Live-verified read-only against the real category 7 Aug 2026: 72 title collisions found in a 200-item sample** — same shape, now inverted after the cleanup (`21SP101411-S` live, `21SP101411` would-list), i.e. 72 SKUs the old code would have double-listed. 14 new tests (`tests/test_list_duplicate_guard.py`), 391 total. No tool-count change (83). **⚠️ Also recorded from the incident: `ProcessTemplates` CREATES ARE ASYNCHRONOUS** — templates sit at `Status: "Creating"` and drain at roughly **50 per 7 minutes**, so a 2xx means *queued*, not *listed*, and a read-back immediately after a create proves nothing. **Deletes are synchronous** and confirm immediately (which is why the v1.38.0 per-template read-back is meaningful for take-downs but would be premature for creates).

> **v1.38.0 (issue #36) — delist now verifies EVERY TEMPLATE individually; a per-item read-back was reporting success for templates that were never deleted (6 Aug 2026):** Found in the first LIVE take-down run (32 SKUs / 37 templates), not by reading code. `unpublish_channel_listing` confirmed take-downs with **one channel-SKU read per ITEM**, then applied that single result to **every template row on the item**. On a multi-template item the FIRST successful delete empties the channel-SKU table — so the second template's failure had nothing left to detect. Live proof: `304035-000-825` (tpl 8481 + 41666) and `304037-000-850` (tpl 12710 + 41653) each reported **`taken_down: true` for BOTH**, while one of each survived with Linnworks returning **`Status: "Not deleted"`**; re-firing Delete on those returned a clean 2xx and changed nothing (they are genuinely undeletable). **No customer impact that run** — the Shopify products were already gone, verified independently via the Shopify Admin API — **but only by luck**: this is exactly the hazard v1.32.0 was written to prevent, and on an item where the *surviving* template held the live listing it would have reported a clean take-down over a listing still selling. **Fix: two independent read-back surfaces that are allowed to disagree.** (1) **Per TEMPLATE** — after processing, the item's templates are re-opened via `OpenTemplatesByInventory` and each planned `template_id` is checked for absence, capturing `template_status_after` verbatim when it survives. (2) **Per ITEM** — the channel-SKU read still answers the separate question "is the listing row gone". Each result row now carries `template_deleted`, `template_status_after`, `still_listed` and a classified **`outcome`**: `taken_down` (template gone AND row gone) · `listing_gone_template_orphaned` (**the case that used to read as success** — listing down, template stuck, needs manual tidying) · `delete_failed` (**the dangerous one** — nothing removed, listing may still be selling) · `template_deleted_listing_row_remains` (usually channel-sync lag) · `unconfirmed` (read-back itself failed — `taken_down` is **None**, never defaulted to True). Response and the fan-out both gained `taken_down_count` / `orphaned_template_count` / `delete_failed_count`, and the message calls out failures explicitly so an aggregate can't hide them. **`Status: "Not deleted"` is now treated as the unambiguous negative it plainly is**, instead of being discarded. **Live-verified read-only against the two REAL orphaned templates**: both classify as `listing_gone_template_orphaned` with `status='Not deleted'`, while their successfully-deleted siblings classify as `taken_down` — where the old logic said `taken_down: True` for all four. 8 new tests (`tests/test_delist_readback.py`), 377 total. No tool-count change (83). **Costs one extra `OpenTemplatesByInventory` call per item on a live run** — worth it; the previous cost was a false success on an irreversible customer-facing write.

> **v1.37.0 (issue #34) — HTTP 429 is now retried everywhere and NEVER reported as "not found"; bulk reads accept StockItemIds (6 Aug 2026):** Found during the first end-to-end run of the cleanup chain, not by reading code. `get_channel_listings_bulk` on 5,391 zero-stock SKUs returned **310 listed / 4,804 "unresolved"** — and every one of those 4,804 was an **HTTP 429 `"API calls quota exceeded! Maximum admitted 150 per Minute."` laundered into a factual claim that the SKU didn't exist**. The true answer was **2,628 listed** (27 calls, 15.6s, via ids): an **88% under-report**. The same loop made `delist_all_channel_listings` silently skip **92 of 154 SKUs** while reporting success. **⚠️ THE DANGEROUS PART IS THE DIRECTION OF THE FAILURE: `unresolved` reads as "this SKU doesn't exist", so a transient quota error pointed straight at the destructive answer** — in a cleanup context that means archiving or ignoring an item that is in fact live on a sales channel. **Root cause was one line:** `_resolve_sku_to_id` wrapped **any** `RuntimeError` into `ValueError(f"SKU '{sku}' not found in Linnworks: …")`, and 15+ call sites bucket that as a per-item miss. **Fix, in three layers.** (1) **Retry/backoff moved into the HTTP helpers** (`call_linnworks`, `call_linnworks_get`, `call_linnworks_void`) rather than into one caller — new `_is_rate_limited()` (429 **or** a "quota exceeded" body, since the message has been seen on other statuses) plus `_rate_limit_pause()` walking a **5/10/20/30s ladder (~65s total, deliberately longer than one quota window)** and honouring `Retry-After` when present. **This fixes every endpoint at once**, including the autopaginating sweeps and `get_stock_change_history`, which hit the same wall at 8 items × 29 locations. Non-429 errors are NOT retried (a 400 is real; retrying wastes a minute). (2) **New `RateLimitError(RuntimeError)`** raised when the ladder is exhausted — subclassing RuntimeError keeps every existing `except RuntimeError` working, while callers that *classify* failures can now tell "ask again later" from "no such record". `_resolve_sku_to_id` re-raises it untouched instead of folding it into "not found". (3) **Both bulk read tools now take `stock_item_ids` as well as `skus`** (shared `_resolve_bulk_inputs()`), and report a **`rate_limited` bucket separate from `unresolved`** plus a **`complete`** flag that is False whenever anything was throttled — so a partial answer can never masquerade as a full one. The id path matters because **the batched read was never the bottleneck — the per-SKU resolve loop in front of it was**; anything chaining from `list_inventory_items` / `find_composite_parents` already holds the ids. **Live-verified 6 Aug 2026:** 2,000 items by id → **2.1s**, 0 rate-limited, complete; **250 SKUs by SKU** (which previously 429'd after ~150 and mislabelled the remainder) → **all 250 resolved, 0 mislabelled, complete:true** in 74s of honest backoff; and the id-path and SKU-path agree exactly (135/250 listed both ways). 20 new tests (`tests/test_rate_limiting.py`), 369 total. No tool-count change (83) — this is a correctness fix, not a new capability. **Still worth knowing:** the backoff makes big SKU-keyed batches *slow* rather than wrong, so prefer ids; and #36 (per-item read-back masking a per-template delete failure) is a separate, still-open defect.

> **v1.36.0 (issue #33) — stock change history / `out_of_stock_since`, fully live-tested; the spec is wrong about this endpoint in TWO places and omits the one field the issue needed (6 Aug 2026):** Added `get_stock_change_history(skus, location_id, all_locations, include_movements, max_movements, change_sources, exclude_change_sources, max_pages)` — the first stock-**history** tool in the server, and the missing piece of the cleanup chain. `get_stock_level` returns a point-in-time snapshot, so "has this been zero for 3 months or since this morning?" was unanswerable; the workarounds were a scheduled custom-SQL CSV drop (can't answer ad hoc) or logging our own daily snapshots (accumulates only going forward). **The issue's guessed endpoint `Stock/GetItemChangesHistory` was right** — GET `?stockItemId=&locationId=&entriesPerPage=&pageNumber=` → `{PageNumber, EntriesPerPage, TotalEntries, TotalPages, Data:[StockItemChangeHistory]}`. **⚠️ FINDING 1 — `locationId` is REQUIRED, despite the spec documenting it as optional ("If null then combined").** Omitting it → HTTP 400 "The request is invalid." **So there is NO combined-across-locations mode** and history is always location-scoped — the single biggest constraint on this build. **⚠️ FINDING 2 — `pageNumber=-1`, which the spec documents as "if -1 then will return all pages", returns HTTP 400 "Value must be at least 1."** Real paging is the only option (no 200 cap here though — `entriesPerPage=1000` works, unlike `GetStockItemsFull`; past-the-end returns an empty `Data` with a **200**, a clean stop, unlike `GetStockItemsFull`'s 400). **⚠️ FINDING 3 — THE MODEL HAS NO `ChangeSource` FIELD**, even though the underlying `StockChange` table does (which is why the tenant's custom-SQL export can filter on it). The source is only present inside the free-text `Note`, so `_classify_change_source()` derives it from ~6,100 live rows sampled across Default/FBA/supplier locations: `Order <n> ref:` → SALE · `Customer return for order` → RETURN · `PO … delivered` → PO_DELIVERY · `PO … to OPEN. Due` → PO_BOOKING · `PO … deleted` → PO_DELETED · `PO … purchase item line update` → PO_UPDATE · `Imported from file` → FILE_IMPORT · `FBA Sync …`/`No quantity returned from channel` → FBA_SYNC · `Stock Count Adjustment` → STOCKTAKE · `DIRECT ADJUSTMENT BY` → ADJUSTMENT · `SCRAPPED BY RMA` → SCRAP · else OTHER. The raw `note` is always returned so anything landing in OTHER can be reclassified. **⚠️ FINDING 4 — `Level` is the level AFTER the change** (the spec just says "Change level"), verified on 15 consecutive row pairs: `Level - ChangeQty` == the next-older row's `Level`, zero mismatches. Rows are newest-first. **THE CORE DESIGN CALL: `out_of_stock_since` is the OLDEST row in the current unbroken trailing run of `level_after == 0`, NOT the most recent row at zero.** A long-dead item still receives automated rows that rewrite 0, and "newest row at zero" would reset the dead-stock clock to today on exactly the items the cleanup job hunts. **Equally important, the level series is NOT source-filtered** — the issue proposed excluding `FBA Sync%`/`Imported from file%` (as its SQL export does), but live rows show FBA-sync entries are **genuine decrements** (observed walking one item 13 → 0), so excluding them from the series would produce a *wrong* date. Filtering therefore applies only to the returned `movements` and to `last_real_movement_date` — which is the field the noise actually distorts, and which is what the export's exclusions were really protecting. Also derived: `days_out_of_stock`, `last_movement_date` (non-zero qty), `last_sale_date`, `last_received_date` (PO delivery or return), `level_mismatch`. **Current level comes from `GetStockLevel_Batch`, never from the history** — live-confirmed an item can hold stock with ZERO history rows at a location (`RS-102201`: 0 entries at Default, 2 units at Rock Solid), and a `current_level > 0` always suppresses an out-of-stock date. Truncation is honest: if the zero run reaches the end of a capped read, `out_of_stock_since_is_lower_bound: true`. **`all_locations=True` returns a PER-LOCATION breakdown plus `zero_at_all_locations_since` = the LATEST of the per-location zero dates** (every location has only been at zero since the last one hit zero). **Quantities are deliberately NOT summed across locations** — most non-Default locations here are virtual dropship supplier mirrors showing the same availability, so a combined level would be meaningless (the same trap `get_stock_level` already warns about). This matters live: `venom-helmet-Large` is zero everywhere, but Default went zero **24 Jun** while FBA went zero **31 Jul** — reading Default alone would overstate the dead time by five weeks. **Fully live-tested 6 Aug 2026**: dead item (`venom-helmet-Large` → 2026-06-24, 43 days, matching the raw rows exactly); in-stock item → `out_of_stock_since: None`; source filtering; bogus SKU → `unresolved` without sinking the batch; invalid filter → ValueError; `all_locations` on both a dead and a live item (45 calls); truncation flags; and a real cleanup sample of zero-stock SKUs returning 135/197/204-day ages — including `10020ZZ061DA00-vtk-c-rawBLT` at **2026-04-30 / 98 days**, which matches the worked example in the issue body. **Honest caveat: every trailing zero run observed at Default in this tenant was length 1** (77 zero-stock items sampled), so the run logic is *defensive* rather than currently load-bearing — the stacked-zero cases are covered by synthetic unit tests, not by an observed live example. Read-only — no write-safety machinery applies. **Cost: one call per (SKU, location) page; `all_locations=True` is ~29 calls per SKU** — keep those batches small (rate limit 250/min). 41 new tests (`tests/test_stock_change_history.py`), 385 total. 83 tools.

> **v1.35.0 — the two order-address tools merged into one; `update_order_shipping_address` DELETED (5 Aug 2026):** `set_order_address` gains **`require_complete`** and is now the only address tool. **⚠️ BREAKING: `update_order_shipping_address` (issue #4, v1.4.0) is gone** — confirmed with the owner that nothing outside this repo called it. **Why it was safe: it was a strict SUBSET.** Reading both revealed the distinction previously documented here ("whole-address replacement vs field patch") was **wrong** — the deleted tool merged onto current state exactly like `set_order_address` (`address2 if address2 else current_address.get("Address2","")`), so it was never a replacement. Its only real difference was **argument enforcement** (five mandatory params), and on everything else it was worse: no `address3`, `saveToCrm` hard-coded `False`, and **no read-back after the write** (it returned straight from the POST, against the house read-back rule). **⚠️ It also carried a live bug: `country_code` was REQUIRED but never reached Linnworks** — the written country came from `country or current`, while `country_code` was only echoed into the response, so redirecting an order abroad with `country_code="FR"` and no `country` kept the OLD country while the response confirmed `"country_code": "FR"`. Nothing set `CountryId` either. That parameter is not carried over; use `country` (full name). The one thing worth keeping — the guard those mandatory args encoded — survives as **`require_complete=True`**: refuses unless full_name/address1/town/postcode/country are all non-blank, checking **what the caller supplied rather than the merged result** (a merged address can be complete and still wrong: new street, old town) and failing **before any API call**. Default `False`, so the by-line workflow is untouched. **Test coverage went UP, not down:** `set_order_address` had **zero** tests while the deleted tool had 7, so its cases were ported rather than dropped — `tests/test_update_order_shipping_address.py` → **`tests/test_set_order_address.py`, 15 tests** (dry-run default, before/after diff, write-then-read-back ordering, unsupplied-fields-preserved incl. internal `CountryId`, no-changes short-circuit, `save_to_crm` pass-through, numeric-id resolution, unknown order, processed-order refusal, four `require_complete` cases, error propagation, and a guard that the deleted tool is really gone). 308 tests total, all green. 82 tools (83 → 82 — the first tool ever removed from this server).

> **v1.34.0 (issue #32) — bulk active-inventory enumerator, fully live-tested; TWO of our own documented notes about this endpoint were wrong (5 Aug 2026):** Added `list_inventory_items(page, per_page, all_pages, include_stock_levels, location_id, zero_stock_only, include_composite_parents, include_variation_parents, flag_composite_parents, max_items)` — the "give me EVERYTHING" counterpart to `search_inventory_items` (keyword) and `find_inventory_item` (exact SKU), wrapping the long-documented-but-unwrapped **`Stock/GetStockItemsFull`**. Built for the catalogue-cleanup sweep: *"every active item with its quantity, so I can find what's out of stock across ALL locations"* → feed into `find_composite_parents` (archive gate) → `delist_all_channel_listings` → `archive_inventory_items`. The previous workaround was a `run_import`/`run_export` CSV round-trip outside the tool flow. **⚠️ FINDING 1 — `loadCompositeParents`/`loadVariationParents` are INCLUDE flags, NOT "load extra detail" flags.** The payload this file has recorded since 7 Jul 2026 as "the full active-item enumerator" passes **both `false`**, which silently makes every composite and variation parent **ABSENT** from the results — so that documented sweep was never the full catalogue. Live proof: the `vnm-catnip` family returns **8** items with both true, **7** with `lcp=True/lvp=False`, **2** with `lcp=False/lvp=True`, **1** with both false. Both therefore default to **True** here, so "every active item" is honest; set them False to exclude parents (what a cleanup sweep usually wants — a parent carries no stock of its own). This also explains a live count discrepancy: the sweep scans **38,684** items where the `Stock/GetStockItems` GET sweep (v1.33.0) walked **34,023** — the GET excludes variation parents, this endpoint (flags on) includes them. **⚠️ FINDING 2 — `dataRequirements` here is a STRING enum** (`StockLevels`/`Pricing`/`Supplier`/`ShippingInformation`/`ChannelTitle`/`ChannelDescription`/`ChannelPrice`/`ExtendedProperties`/`Images`), **not** the integer `[1]` that its sibling `GetStockItemsFullByIds` needs for `Suppliers[]` — the assumption-list entry about `DataRequirements` does not transfer between the two. `"StockLevels"` populates a **PER-LOCATION** `StockLevels[]` (Location{Id,Name}, StockLevel, Available, InOrders, Due, MinimumLevel), which is what makes *"out of stock EVERYWHERE"* answerable in ONE sweep instead of one sweep per location — the tenant holds stock at 29 locations, so a Default-only zero is NOT dead stock. **⚠️ FINDING 3 — this model has NO top-level `Quantity` and NO `TotalEntries`.** Quantities are **derived** by summing the per-location rows (cross-checked live against the GET: `1002079508` = Default 1 + Keen 6 == GET `Quantity` 7; `InOrders` == the GET's `InOrder`; scoping to Keen gives 6, matching the GET's server-side `locationId`). Paging past the end returns **HTTP 400 "No items found with given filter"** — an end-of-results marker, so `_fetch_full_stock_page` returns `None` on it and auto-paging stops cleanly rather than surfacing a spurious error (`entriesPerPage>200` → a real 400, so it's capped client-side). **⚠️ FINDING 4 — `"StockLevels": []` comes back BOTH when levels weren't requested AND when an item genuinely has no stock rows**, so loaded-ness is passed into `_format_full_stock_row` rather than inferred: unread → `None` ("not read"), read-but-empty → `0` (really no stock). Conflating them would let an unread figure pass the `zero_stock_only` cleanup filter as dead stock. **⚠️ FINDING 5 — the model carries `IsVariationParent` but NO `IsCompositeParent`** (as v1.33.0 noted), so `is_composite_parent` is `None` = "not determined" unless `flag_composite_parents=True`, which fills it from the **cached composite index** (~215 calls to build, then 15 min TTL) — cross-checked live against `GetInventoryItemCompositions` on 4 items, zero disagreement. **Also corrected: the "does NOT work" entry claiming this endpoint can't keyword-search is stale** — `keyword` works with empty `searchTypes` OR `["SKU"]`/`["Barcode"]` (a SKU string with `searchTypes:["Title"]` correctly finds nothing). That's the **third** stale blocker in two builds (cf. assumption #10). Response reports `scope` (active_only, location, parent-flag settings), `pages_fetched`, `scanned_count`, `matched_count`, `count`, `truncated`, `complete`; `max_items` (default 5000) truncates only the returned detail while the **counts always span everything scanned** — a 38k-item dump would otherwise swamp the context. **⚠️ ACTIVE ITEMS ONLY** — same blind spot as every list/search endpoint; archived items are unreachable. **AUTOPAGINATING** (~194 calls / ~132s for `all_pages=True`) — never run in parallel with the other sweep tools. **Fully live-tested 5 Aug 2026**: full sweep 194 pages/38,684 items/132s finding **11,836 items at zero across every location**; sum + location-scope cross-checks vs the GET; truncation; `include_stock_levels=False` → None-not-zero; both guards; past-the-end termination; composite flagging verified against the compositions endpoint. Read-only — no write-safety machinery applies. 21 new tests (`tests/test_list_inventory_items.py`), 289 total. 83 tools.

> **v1.33.0 (issue #31) — composite component → parent REVERSE lookup, fully live-tested; a "no endpoint exists" blocker turns out to have been a stale one (5 Aug 2026):** Added `find_composite_parents(skus, include_listing_status=True, max_parents_listed=25, rebuild_index=False)` — the archive-safety gate: *"before I retire this dead-looking SKU, is it a component of a bundle that is still live?"*. Until now nothing in the toolset could answer that: `get_item_relationships` covers parent→components but declared the reverse **impossible**, on the grounds (issue #17, Open Q2) that "no endpoint maps a component back to its parents, and the only fallback — scan every composite — is infeasible because `Inventory/GetInventoryItems` and `Stock/GetStockItemsFull` both 400". **The reporter was right: the second half was stale.** The first half still holds (there genuinely is no component→parent endpoint), but the enumerate-and-invert fallback works fine — **not** via `GetStockItemsFull`/`loadCompositeParents` as the issue proposed (that model has **no `IsCompositeParent` field at all**, so it can't tell parents from ordinary items), but via the **`Stock/GetStockItems` GET sweep already used by the category tools**, whose rows DO carry `IsCompositeParent`. **⚠️ FINDING 1 — `IsCompositeParent` is RELIABLE, unlike its sibling `IsVariationParent`.** That flag's twin has burned this project before (observed `false` on a genuine variation parent, hence #17's "never trust the flag" rule), so it was cross-checked here rather than assumed: on a 200-item page, 96 flagged / 96 with components / **zero disagreement in either direction**. This matters asymmetrically — a false negative would silently clear a component that still feeds a live bundle. **⚠️ FINDING 2 — `Inventory/GetInventoryItemsCompositionByIds` is the bulk composition resolver** (found by sweeping `inventory.json` for the noun, the same trick that cracked the supplier family in #25): POST **wrapped** `{"request":{"InventoryItemIds":[…]}}` → a **map** of parent id → `StockItemComposition[]`. It **caps at 100 ids** (HTTP 400 "The maximum items count for this call is 100" at 200 — the spec doesn't say so) and **OMITS** items with no compositions rather than returning empty lists. That turns ~4.3k per-parent GETs into 44 calls. **Measured live on this tenant: 171 sweep pages + 44 composition calls = 215 API calls, ~105s, over 34,023 active items → 4,343 composite parents → 2,908 distinct components.** Because that cost is real, the index is **built once and cached for the process (15 min TTL)**, the tool is **batch-first** (one build serves the whole screening run — a second call returned in 1.9s, `from_cache:true`), and it reports `index.built_at`/`age_seconds`/`from_cache` so a stale answer is visible rather than implicit. The sweep also yields a **SKU→id map for free**, so input SKUs resolve at zero extra cost (with a per-SKU `GetInventoryItem` fallback for items newer than the cached index). **⚠️ Some components are enormous:** `buildmyboard` sits in **3,056** composites, `blackgrip-9x33` in 2,986 — so `parents[]` is capped (`max_parents_listed`, default 25) with **listed parents sorted first**, while `parent_count`/`listed_parent_count`/`safe_to_retire` are always computed across **ALL** parents. The verdict is never truncated, only the detail. `parent_is_listed` comes from `BatchGetInventoryItemChannelSKUs` over the batch's distinct parents (~1 call/200); if that read fails the tool degrades to the reverse answer plus a `listing_status_error` and returns `safe_to_retire: None` — it never guesses "safe". Same for `include_listing_status=False`. **⚠️ ACTIVE ITEMS ONLY** — the sweep never returns archived items (the same blind spot as the category sweep), so an ARCHIVED composite parent is invisible; safe for the archive-gate use (an archived parent isn't selling) but not a complete history. **Fully live-tested 5 Aug 2026**: index build (215 calls/105s); the issue's own Venom case round-tripped forward↔reverse (`vnm-catnip-MID-bundle-adult-large` → components → `vnm-catnip-MID` back to **6 parents, all listed, `safe_to_retire:false`**); `venom-helmet-Large` 244 parents/240 listed and `venom-triplepads-adult` 374/368; a genuine non-component → `safe_to_retire:true`; truncation, cache hit, listing-status-off and bogus-SKU paths all confirmed. `get_item_relationships`'s composite block now carries `reverse_lookup_tool: "find_composite_parents"` and its "cannot be determined" note is retired. 19 new tests (`tests/test_composite_parents.py`), 268 total. Read-only — no write-safety machinery applies. 82 tools.

> **v1.32.0 (issue #30) — delist generalised beyond Shopify to ALL GLT channels; two Amazon-shaped assumptions broken (5 Aug 2026):** `unpublish_channel_listing` gains a **`channel`** parameter ("Shopify" default / "Amazon" / "TikTok" / "Magento" / "Walmart") and the Shopify-only fan-out becomes **`delist_all_channel_listings(skus, channels=None, …)`**, with `delist_all_shopify_listings` kept as a thin Shopify-pinned wrapper (back-compat, same behaviour). New shared plumbing: a **`GLT_CHANNELS` registry** (channel_type / channel_name / channel-SKU `source` / `delete_proven`), `_resolve_glt_channel()`, `_fetch_glt_configurators(channel)` (with `_fetch_shopify_configurators()` as an alias), `_glt_channel_id_for()` and `_resolve_glt_target()`. **Live channel probe re-run this build:** Shopify 67 configurators · **Amazon 10 (ChannelType "Amazon" + ChannelName "AMAZON", ONE account — ChannelId 2, SubSource "The Warehouse Group")** · TikTok 5 (ChannelId 30, "SKATEWAREHOUSE_UK") · Magento 0 · Walmart 0; eBay/Etsy 400 "Invalid parameter request", External 400 "Failed to create a channel", CDiscount_OBSOLETE 400 null-ref — so eBay/Etsy/Mirakl/CDiscount stay permanently manual and are reported under `skipped_channels` with a reason. **⚠️ FINDING 1 — ONE ITEM CAN HAVE SEVERAL TEMPLATES ON ONE CHANNEL.** `vnm_bearings_gold` returns **TWO** Amazon templates for a single StockItemId (32115 = the `.FBA` channel SKU, 32381 = merchant). The old code keyed `templates_by_sid[sid] = t` — **last-write-wins, silently dropping the other template**, so a "successful" take-down would have left a live Amazon listing up. Templates are now collected as a **list per item**, the plan is **one row per template** (`templates_on_item`), and `EntriesPerPage` was widened from `len(chunk)` to `len(chunk)*4`. **⚠️ FINDING 2 — AMAZON REGIONS SHARE ONE ACCOUNT.** The channel-SKU table carries seven Amazon SubSources ("The Warehouse Group", "… - Germany/France/Italy/Spain/Netherlands/Sweden") but the configurator catalogue has only the account — so a regional sub_source has NO ChannelId of its own. `_glt_channel_id_for()` resolves it by **longest account-prefix** (reported as `sub_source_resolution: "account-prefix"` vs `"exact"`), anchored to that channel's own account names so a Shopify store never cross-matches onto Amazon's single ChannelId. The fan-out **collapses the regions into ONE take-down per (item, account)** with `covers_sub_sources` listing them — deleting once per region would just re-delete the same templates. Also fixed: **read-back is now once per ITEM after all its templates are processed** (a per-template read-back reports `still_listed` for rows the next template is about to remove), and the fan-out adds an honest cross-region **`still_listed_sub_sources`** re-read, since a delegated call only ever checks its own representative sub-source. Amazon's `Info.ActiveListingId` is the **channel SKU** (`vnm_bearings_gold.FBA`), not a numeric product id. **✅ AMAZON DELETE IS NOW LIVE-PROVEN (5 Aug 2026)** — fired on ONE real listing, `MOB-GRP-3080` (template 31703, ASIN B0B311TFG3, qty 2, status "Listed"), exactly the way Shopify was proven in v1.25.0. `ProcessTemplates` `Action="Delete"` with `ChannelType:"Amazon"`/`ChannelName:"AMAZON"` → empty 2xx; **verified against three independent surfaces**: the AMAZON channel-SKU row disappeared (only EBAY + SHOPIFY remain), `OpenTemplatesByInventory` now returns **0 templates** for that item on ChannelId 2 (so a linked-template Delete removes the template AND ends the listing — same semantics as Shopify), stock untouched at 2/2, and eBay + Shopify listings unchanged. Re-running the tool is idempotent (→ "not listed on Amazon — nothing to take down"). **⚠️ KEY FINDING: the Delete succeeded even though the template read `NextSuggestedAction:"NotAllowed"`** — that field describes the action GLT *suggests*, it does NOT gate a forced Delete, so a "NotAllowed" template is still deletable. `GLT_CHANNELS["amazon"]["delete_proven"]` is therefore now `True`. ~~**TikTok remains unproven** (`delete_proven: false`, warned about in every message) — prove it on one throwaway TikTok listing the same way.~~ **← SUPERSEDED by v1.42.0: TikTok Delete was live-proven 7 Aug 2026 and is now `delete_proven: true`.** **Read/selection path also fully live-validated** (Amazon 2-template plan; regional prefix fallback → ChannelId 2; Shopify regression → tpl 43285 unchanged; cross-channel fan-out on a 7-listing item → Amazon×1 + Shopify×2 + TikTok×1 targets, 5 templates, eBay/CDiscount/Magento skipped; non-GLT channel → ValueError; wrong-channel sub_source → error + available list). Thresholds unchanged at 10; `dry_run=True` default. 20 new tests (`tests/test_delist_channels.py`), 249 total. 81 tools.

> **v1.31.0 — `set_order_status` order-state tool (lock/unlock/paid/unpaid); PARK IS NOT API-ACCESSIBLE (28 Jul 2026):** Added `set_order_status(order_ids, action, confirmed_count=None, dry_run=True)` — one tool to change an order's workflow state, built to support a Shopify→Linnworks cancellation-hold workflow (lock an order so a pickwave can't dispatch it while a customer cancellation request is confirmed). Actions: **lock/unlock** via `Orders/LockOrder` (`{"orderIds":[guid],"lockOrder":bool}`), **paid/unpaid** via `Orders/ChangeStatus` (`{"orderIds":[guid],"status":int}`). **The `ChangeStatus` enum is documented in the endpoint description: `0=UNPAID, 1=PAID, 2=RETURN, 3=PENDING, 4=RESEND`** — so paid→1, unpaid→0. This enum is **`GeneralInfo.Status`** on `GetOrdersById` (a live PAID order reads back 1 — confirmed in-process 28 Jul) and is **DISTINCT** from the GetOrdersLowFidelity display codes in `_ORDER_STATUS_LABELS` (0=Draft/1=Pending Dispatch/2=Paid/…) — do NOT conflate them; the tool uses a separate `_PAYMENT_STATUS_LABELS` map for read-back. **⚠️ PARK/UNPARK ARE NOT SUPPORTED — no public endpoint exists.** Swept every spec file: parking is only a **RulesEngine action (`ChangeOrderParkStatus`)** with no standalone API; `GeneralInfo.IsParked` is **read-only**. The tool rejects `park`/`unpark` with a message pointing to `lock` (which stops dispatch anyway — a locked order stays in the queue but is barred from processing). **⚠️ Operational difference that makes this API gap matter (not just a naming one): LOCKING an order RELEASES its allocated stock back to available** (so that stock can be re-allocated/sold on other orders while the order is held), whereas **PARKING KEEPS the stock allocated** — so for a hold where you want to keep the stock ring-fenced, park is the correct state and it's exactly the one the API can't reach. Lock still stops the dispatch; just know the stock is no longer reserved. **⚠️ Lock has NO read-back:** the order model exposes no lock-status field (`lockOrder` is the only "lock" token in the whole spec), so a lock/unlock CANNOT be verified by re-reading — the tool reports `lock_readback:"unavailable"` and says to check the padlock in the UI; **paid/unpaid ARE read-back verified** against `GeneralInfo.Status`. Bulk-capable (both endpoints take an id list → one call for the whole resolved batch), GUID or numeric ids (a bare string is accepted), read-before-write manifest (current status/label, is_parked, processed, customer, ref + per-action intent/warning), unresolvable id → `resolve_errors` row (never aborts the batch), `_write_guard` threshold **25** (all four actions are reversible), `dry_run=True` default. **Read/manifest path live-validated in-process** (order 607046 → resolved, `Status:1`=PAID, `is_parked:false`, processed-warning; park/unknown-action rejections). **`Orders/LockOrder` (lock + unlock) is now LIVE-PROVEN (28 Jul 2026)** — reversible round-trip on open order 606371 (lock → empty 2xx → unlock → empty 2xx, order restored to unlocked). **`Orders/ChangeStatus` (paid/unpaid) is spec + enum confirmed but NOT yet live-fired** (deliberately — it flips accounting-relevant payment state; test on one order when needed, it's reversible 1↔0). 10 new tests (`tests/test_set_order_status.py`), 229 total. **Context:** this was built for a self-serve-cancellation → Linnworks-hold pipeline that was then parked — Shopify Flow can't trigger on a customer self-serve cancellation request, and a tag added post-checkout wouldn't reach Linnworks anyway (no order re-sync after import), so the store disabled self-serve cancellations instead. The tool stands on its own. 80 tools.

> **v1.30.0 (issue #29) — `add_variation_group_items` + `create_variation_group` FIXED & FULLY LIVE-TESTED (28 Jul 2026):** Added `add_variation_group_items(child_skus, parent_sku=None, group_name=None, dry_run=True)` — the write path that was missing: attach one or more child SKUs to an **existing** variation group. `create_variation_group` was create-only (no-oped on an existing name, never diffing/adding), so a supplier feed that adds a new size/colour weeks later left the child orphaned (an item, not a group member) until someone opened the product in the Linnworks UI. **Endpoint: `Stock/AddVariationItems`** (POST `{"pkVariationItemId":<group id>,"pkStockItemIds":[<child guids>]}`, empty 2xx). The group is identified by **parent_sku OR group_name** (the parent's StockItemId **IS** the group's `pkVariationItemId`, #17); idempotent (already-linked SKUs → `already_present`, skipped), unresolvable child → `child_errors` row that never sinks the batch, read-before-write manifest + read-back (`added: true/false` per SKU). **Two big live findings this build surfaced (both now baked in):** (1) **`Stock/CreateVariationGroup` was BROKEN and is now fixed + FIRST-EVER LIVE-TESTED.** The old tool resolved `parent_sku` to an *existing* item — but Linnworks **requires the variation parent SKU to be BRAND-NEW** (`HTTP 400 "Chosen variation SKU already exists as a normal stock item"`). Corrected: it now pre-checks with **`Stock/CheckVariationParentSKUExists`** (→ `NotExists`/`Exists`/`AlreadyVariation`), passes the **zero-GUID** `ParentStockItemId` (server mints the real id, returned as `pkVariationItemId`), and children must already exist. (2) **`Stock/GetVariationGroupByName` is UNRELIABLE — it returns HTTP 200 `null` even for a group that demonstrably exists** (live-proven seconds after creating one). Both tools' name lookups now go through a shared `_find_variation_group_by_name()` helper using **`Stock/SearchVariationGroups` (searchType=VariationName, substring)** with an EXACT case-insensitive match. **Fully live-tested 28 Jul 2026** against isolated `ZZZ-MCP-TEST-VG-*` SKUs: create (new parent, 2 children) → add 3rd child (read-back member count 3) → idempotent no_op → group_name path → bogus-child error → non-parent/no-identifier/name-mismatch guards → teardown (group + items deleted, tenant clean). 14 new tests (`tests/test_add_variation_group_items.py`), 219 total. `Stock/AddVariationItems` sent **unwrapped** (like `CreateVariationGroup`'s `{"template":{…}}`). `DeleteVariationItem(s)` exist but are **not wrapped** (no remove request in the issue). 79 tools.

> **v1.29.0 — `delete_purchase_order`, fully live-tested (24 Jul 2026):** Added `delete_purchase_order(purchase_ids, confirmed_count, dry_run=True)` — the whole-PO counterpart to `remove_purchase_order_item` (single line). Wraps the long-confirmed-but-unwrapped `PurchaseOrder/Delete_PurchaseOrder` (`{"pkPurchaseId":"guid"}` unwrapped, empty 2xx; a deleted PO then 400s "Purchase Order does not exist"), which was previously only used for test-PO teardown. Bulk-capable (list of `pkPurchaseID`), gated like `delete_inventory_item`: **threshold 10**, `dry_run=True` default, read-before-write manifest (status, supplier, line_count, total_cost, `delivered` flag), per-PO delete + read-back (`Get_PurchaseOrder` 400 → `deleted:true`). **DELIVERED/PARTIAL POs are flagged with a `warning`** (deleting loses received-stock history) but NOT auto-blocked — Linnworks itself allows the delete; the intent is clearing mistaken/duplicate/abandoned **PENDING/OPEN drafts**. Unresolvable id → error row, never aborts the batch. **Fully live-tested** (create throwaway PENDING PO → dry-run manifest → live delete → independent confirm-gone → bogus-id error row). No new endpoints (reuses `Get_PurchaseOrder` + `Delete_PurchaseOrder`). 78 tools.

> **v1.28.0 (issue #28) — image delete + reorder, fully live-tested; ⚠️ the MAIN image is PINNED to sort_order 0 (17 Jul 2026):** Added `delete_inventory_item_images(sku, image_ids, …)` and `set_inventory_item_image_order(sku, image_ids, main_image_id, …)`, completing the image suite (`add` v1.11.0 + `get` #19 + delete/reorder here). Until now a wrong image could only be fixed by hand in the UI. **Endpoint choice matters:** the issue proposed `DeleteImagesFromInventoryItem`, but that keys off image **URLs** (`{"inventoryItemImages":{itemId:[url,…]}}`); the tool instead uses **`Inventory/DeleteInventoryItemImageBulk`**, which keys off image **IDs** — matching what `get_inventory_item_images` returns — and returns a `BatchedAPIResponse` (200 + body, NOT 204). **⚠️ THE HEADLINE FINDING (live-proven, 3 probes): `Inventory/UpdateImages` PINS THE MAIN IMAGE TO SortOrder 0 and you cannot override it.** The resulting order is always `[main] + [requested order minus main]`: a FULL set submitted main-first is honoured **exactly**; ask for the main anywhere else (or submit a PARTIAL payload) and the server re-normalises — asked `[img2,img3,img4,img1]` with img1 main → got `[img1,img2,img3,img4]`; asked `[img1,img2,img3,img4]` with img3 main → got `[img3,img1,img2,img4]`. So **the issue's model ("first is main, sort freely") is backwards — reordering can NEVER put a non-main image first; to change the storefront hero you must set the main image.** `Inventory/SetInventoryItemImageAsMain` (204) sets the flag but does **NOT** reorder (a main image can transiently sit at sort 2 — any later `UpdateImages` yanks it to 0). Consequences baked into the tool: it sets main FIRST then submits the **full set main-first** (the only honoured shape); it **predicts** the server's override into the dry-run manifest and flags `main_forced_first` + a `warning` rather than promising an order Linnworks won't deliver; and the read-back returns `order_matches_plan` so a 2xx is never mistaken for the right outcome. `UpdateImages` also **clears any omitted field** (same nulls-clear family as UpdateInventoryItem/UpdateStockSupplierStat) — `_image_simple_row()` carries the full row, incl. the checksum, which the GET spells `CheckSumValue` while the Simple model wants `ChecksumValue`. Thresholds: delete **10** (irreversible), reorder **25**. Both `dry_run=True` default, read-before-write manifest, read-back after. **Fully live-tested** against isolated `ZZZ-MCP-TEST-IMG-*` SKUs (17 checks: order honoured, override predicted, promote-to-hero, no-op, bogus-id refusal/skip, delete + read-back, teardown). 19 new tests (`tests/test_image_delete_reorder.py`), 205 total. NB: `Inventory/UploadImagesToInventoryItem` (raw file upload, vs URL) exists but is NOT wrapped — no request in the issue.

> **v1.27.1 — ⚠️ `ProcessTemplates Update` LIVE-RUN for the first time: it pushes a STALE TEMPLATE SNAPSHOT, not current item data (14 Jul 2026):** The first live revise (catnip parent tpl 39076, the issue-#26 fix) **updated the live Shopify listing with the template's STORED field values, frozen at its `LastModificationTime` (2026-02-16)** — it overwrote the current £89.95 selling price with the template's stale £79.95 on all 6 variants and left compare-at at the stale £94.95 (the `special_price`=99.95 edit never reached the template body). **GLT's `NextSuggestedAction:"Update"` does NOT mean the template body is fresh** — it only means GLT knows a change is pending; the UI listing screen evidently rebuilds template fields on open, but the API path (`OpenTemplatesByInventory` → `ProcessTemplates`) does NOT refresh them. **Remediation (same session):** selling price £89.95 + compare-at £99.95 restored on all 6 variants directly via the Shopify Admin API (`productVariantsBulkUpdate` — the SWH Shopify MCP), verified by read-back; exposure window ~15 min at £79.95. A Linnworks-side re-push via `set_inventory_item_prices` was also queued (same value, harmless when it lands). **Net state: catnip bundle now shows exactly what issue #26 wanted** (89.95 / was 99.95) — but achieved via Shopify direct, not the GLT. **Consequences:** `refresh_channel_listing`'s docstring + live-run message now carry a stale-snapshot hazard warning (only live-run a template known to be fresh; read the live listing back after EVERY push); the variation-fallback selection logic from v1.26.0 is UNCHANGED and correct (right template found, action accepted, listing updated — the defect is upstream in what GLT pushes). **Open lead:** `GenericListings/SaveTemplateFields` (`{"request":{ChannelType,ChannelName,TemplateId,"FieldsToSave":{key:value}}}`) exists and could write fresh values into the template before processing, but its field-key format is undocumented — deliberately NOT probed against a live template; filed as a follow-up issue. No tool count change; tests unaffected (186 green).

> **v1.27.1 addendum — #27 investigation: NO public-API path can refresh a variation template's fields (live-probed 14 Jul 2026):** Four experiments, all against the catnip test bed: (1) **`ProcessTemplates Revise` behaves identically to `Update`** — it pushed the same stale Feb-2026 snapshot (price → 79.95 again); no rebuild semantics. (2) That second push also **proved compare-at IS included in a revise** (the template's stale 94.95 overwrote the live 99.95) — so a FRESH template would deliver the strikethrough correctly; template freshness is the sole blocker, not the `IsProductCreationOnly` flag on the configurator's `compare_at_price` attribute. (3) **`CreateTemplates` on an already-listed item does NOT adopt/rebuild** — it mints a NEW UNLINKED template (Status "Not listed", no ActiveListingId, NextSuggestedAction "Create"; pushing it would duplicate the listing — the #18 dedupe assumption is now empirically confirmed). The orphan was safely removed with `ProcessTemplates Delete` (on an unlinked template it deletes just the template; live listing untouched — verified). (4) **`SaveTemplateFields` validates `FieldsToSave` keys against the `ShopifyTemplateProperties` model** — top-level scalars `Price`/`Title` are accepted, but `Variations`/`Attributes`/`Description`/`compare_at_price`/`special_price` are all rejected ("Property is not found by info key …"), so **per-variant price/compare-at cannot be written into a template via the public API.** Also useful: `GetConfiguratorData` `DataKey:"Attributes"` dumps a configurator's field mappings (confirmed conf 129 maps `compare_at_price` ← `special_price`, per-variant). ⇒ **#27 is blocked for variation listings**; the working fixes for stale-template variants are the Shopify Admin API (proven: catnip repaired twice, final state 89.95/compare-at 99.95) or the Linnworks UI (which rebuilds fields on open). Listing was re-repaired and the sandbox template cleaned up in-session; net tenant state is correct. **Outcome (same day): ALL 84 bundle variants across all 14 Shopify products were fixed via the Shopify Admin API** (13 products in one aliased `productVariantsBulkUpdate` sweep + catnip) — every variant read back at price £89.95 / compare_at £99.95; the #26 repricing is fully live. **⚠️ STANDING WARNING: those 14 GLT templates (incl. catnip tpl 39076) STILL hold the stale Feb-2026 snapshot (price 79.95 / compare-at 94.95).** Any future `ProcessTemplates Update`/`Revise` on them — via `refresh_channel_listing` OR the GLT bulk actions — will revert the live listings to those values. Before any GLT revise of a `vnm-*-bundle` listing, open it in the Linnworks GLT UI first (which rebuilds the template fields), or re-check the live listing immediately after.

> **v1.27.0 (issue #25) — per-item supplier links, fully live-tested (14 Jul 2026):** Added `get_inventory_item_suppliers(sku)` + `set_inventory_item_suppliers(links, …)` — the first tools for the **StockItemSuppliers table** (which supplier(s) an item can be bought from, at what code/cost, and which is primary). This is the table `get_sales_by_supplier` keys off via `IsDefault`, so an item with no default supplier link is invisible to supplier-keyed exports/restock tooling (the issue's ~9 pooled J&R/Rock Solid master SKUs). **Endpoint family cracked:** `Inventory/GetStockSupplierStat` (GET `?inventoryItemId=<guid>` → array of `StockItemSupplierStat` — NB the previously-404 probe `GetInventoryItemSupplierStat` was simply the wrong name); `Inventory/GetStockSupplierStatsBulk` (GET — **`inventoryItemIds` must be a JSON-array STRING** in the query, `?inventoryItemIds=["guid","guid"]`; repeated params and comma-joins both 400 "Invalid parameter"); `Create`/`UpdateStockSupplierStat` (POST `{"itemSuppliers":[StockItemSupplierStat,…]}` → **204 No Content**, so `call_linnworks_void` like RunNow*; **no client GUID needed** — rows are keyed by (StockItemId, SupplierID), unlike the pkRowId sub-entity Creates). **Two live-proven behaviours shape the write tool:** (1) **UpdateStockSupplierStat CLEARS any omitted field** (a cost-only update wiped `Code` to null — same nulls-clear gotcha as UpdateInventoryItem/PO header), so the tool reads the existing row and carries every field through, overlaying only the requested changes; (2) **creating a new `IsDefault:true` row auto-flips the previous default to false server-side** (no second write needed). Upsert keyed by (item, supplier); supplier matched case-insensitively by name against `get_suppliers` (GUID also accepted; unknown → per-item error row, never sinks the batch). Threshold 50 (purchasing metadata), `dry_run=True` default, read-before-write manifest (old vs changes), read-back after write. `DeleteStockSupplierStat` exists but is NOT wrapped (no delete request in the issue). **Fully live-tested** against isolated `ZZZ-MCP-TEST-SUP-*` SKUs (create→read-back→cost-only-update-preserves-code→second-supplier-default-flip→unknown-supplier-error→teardown) + real read on RS-102201 (Rock Solid, code RS-102201, £52.50, default). 9 new tests (`tests/test_supplier_links.py`), 186 total.

> **v1.26.0 (issue #26) — variation-group fallback in `refresh_channel_listing`; the "no GLT template" compare-at mystery solved (14 Jul 2026):** Issue #26 asked for a way to push Shopify **`compare_at_price`** (the strikethrough) for "listings without a GLT template" — ~84 repriced Venom bundle variants where `set_inventory_item_prices` moved the selling price fine but `refresh_channel_listing` returned every SKU `unresolved` ("no GLT template could be opened"). **Root cause found live: those listings ARE GLT-managed — via the variation PARENT.** GLT variation listings hang off the parent item: the **parent holds the template** (e.g. `vnm-catnip-MID-bundle` → tpl 39076, configurator 129 "Master - Helmet & Pad Size", Status "Listed", `ActiveListingId` = the live Shopify product id, `NextSuggestedAction` "Update") **but has NO channel-SKU row**, while the **children hold the channel-SKU mappings** (each variant = `product:variant:inventory` ref on the SAME Shopify product) **but have NO templates**. `refresh_channel_listing` was blind in both directions — child SKUs opened zero templates, and the parent SKU failed the "is it listed?" channel-SKU check. **Fix: extended the EXISTING tool — no new tool, no new endpoints** (the issue's other suggestion, a `compare_at_price` field on `set_inventory_item_prices`, is a dead end: `StockItemPrice` has no such field, the public specs contain no "compare" anywhere, and compare-at-via-direct-price-sync is an unfulfilled Linnworks feature request). New behaviour: (1) a template-less **child** falls back to its variation parent's template (child→parent resolved via the issue-#17 `SearchVariationGroups`+`GetVariationItems` path, with a **per-call sibling cache** so N children of one group cost ONE search, not N); (2) a **parent** SKU counts as listed when any child is mapped on the store (children checked via `BatchGetInventoryItemChannelSKUs`); (3) the plan is now **deduped by template** — rows carry `covers_skus` (all inputs sharing the template), `via_variation_parent`, `listed_via_children`; 6 catnip children → ONE tpl-39076 push. `_resolve_variation` now also returns `parent_stock_item_id` (the group's `pkVariationItemId` IS the parent's StockItemId, confirmed #17). **Live-verified (dry-run) 14 Jul 2026:** 6 catnip children dedupe to one parent plan row (action "Update"), dragon parent resolves via `listed_via_children`, plain per-item template (RS-102201 → tpl 43285) unchanged, bogus SKU/store still error cleanly. **The `ProcessTemplates` Update/Revise push remains spec-based, NOT yet live-run** — first live run should be ONE parent template (e.g. catnip, which would fix its stuck compare-at 94.95→99.95; NB a revise pushes the configurator's FULL field mapping — title/price/description — not just compare-at, so read the listing back after). 6 new tests in `tests/test_refresh_variation_fallback.py`; suite 177 green (incl. the #23 stale-test fix, shipped separately, no version bump).

> **v1.25.0 — archive/unarchive + delist-all-Shopify; `ProcessTemplates Delete` now LIVE-PROVEN (8 Jul 2026):** Three new tools for **retiring archived products' channel listings**, born from a real task: "de-list every Linnworks-archived item that's still live on a channel." **Two hard API walls surfaced (both now documented):** (1) *archived items can't be enumerated* (established v1.24.1), and (2) *an archived SKU can't be resolved to its StockItemId* — `Inventory/GetInventoryItem` returns HTTP 400 "Could not determine inventory item id from SKU", and no SKU→id resolver sees archived items (all 404). So the only handle on an archived item is its **StockItemId GUID**, obtainable only from a Linnworks UI/Data export of the Archived view (with the "Stock Item ID" column). The intended fully-automated `unarchive→delist→re-archive` pipeline is therefore **not** feasible from SKUs; the practical workflow is **bulk-unarchive in the Linnworks UI → delist (by SKU, now active) → re-archive (by SKU)**. New tools: **`delist_all_shopify_listings(skus, …)`** — thin wrapper over `unpublish_channel_listing` that discovers EVERY Shopify store an (active) item is listed on via the channel-SKU table and takes each down; non-Shopify listings (Mirakl/eBay/Amazon/Magento) are reported under `skipped_channels` and **left up** (the GLT delete path is Shopify-only). **`archive_inventory_items(skus, …)`** (`Inventory/ArchiveInventoryItems`, SKU-driven, resolve→archive→read-back that the SKU stops resolving) and **`unarchive_inventory_items(stock_item_ids, …)`** (`Inventory/UnarchiveInventoryItems`, **GUID-driven** since archived SKUs can't resolve). Both wrap the shared `_set_archive_state()` worker; body is `{"parameters":{"InventoryItemIds":[…],"SelectedRegions":[],"Token":"0000…"}}`. **`GenericListings/ProcessTemplates` with `Action="Delete"` was run LIVE for the first time** (sample `luma-harlyn-frosted`, StockItemId `6ae030a4-…`) and **confirmed working** — 2 Shopify listings (Icarus Eyewear tpl 20894 + TWG B2B tpl 41159) taken down, read-back `taken_down:true`; then the item was re-archived live via `ArchiveInventoryItems` (empty 2xx, SKU stopped resolving). `ArchiveInventoryItems` + the delist delegate are now live-proven; `UnarchiveInventoryItems` is spec-based (same `InventoryParametersRequest` shape as archive). Thresholds: `delist_all_shopify_listings` 10 (destructive), `archive`/`unarchive` 25 (reversible). All `dry_run=True` default.

> **v1.24.1 — category "empty" ≠ deletable: the archived-item blind spot (live-confirmed 7 Jul 2026):** The v1.24.0 emptiness sweep is **active-items-only** and this materially overstates what can be deleted. `Stock/GetStockItems` (the sweep endpoint) returns ONLY active stock — archived items are invisible to it, and there is **NO** endpoint that lists or counts archived items per category (`Stock/GetStockItemsFull` is also active-only; `GetInventoryItemsCount` has no category filter; no `GetArchivedItems` exists — all probed 7 Jul). On this tenant **32,125 items are active but 92,816 exist including archived (~60k archived)**. So a category holding only archived stock reads as "0 items"/empty in the sweep, yet `Inventory/DeleteCategoryById` refuses it with **HTTP 400 "Category is in use"**. That server-side guard is the **authoritative** emptiness check (it sees archived items + channel references the sweep can't). **Real run that surfaced this:** deleting the 10 "junk" empties returned only **1 deleted** (`- POS`, truly empty) + **9 "Category is in use"** (`#REF!`, `Archived`, `AUCTION`, `Boardmasters`, `Carver Complete Composites - Archived`, `OUTLET`, `Packaging`, `Scooter Completes - Archived`, `SHOWS` — all hold archived items). **Fix:** both `delete_categories` and `delete_empty_categories` now detect the "Category is in use" 400 via `_is_category_in_use()` and report those categories as **`skipped_in_use`** (a clean, expected outcome — NOT a `delete_error`), and return a `skipped_in_use` count alongside `deleted`. `force=True` does **not** override this (it only skips the sweep; the server-side guard still applies). To actually delete such a category, unarchive/reassign its items in the Linnworks UI first. Docstrings for `get_categories(with_counts)`, `delete_categories`, `delete_empty_categories`, and `_count_items_per_category` now state "empty = zero ACTIVE items". The v1.24.0 note's "62 of 181 empty" is an **upper bound**, not a deletable count. No new tools; no new working endpoints (only the confirmation that no archived-per-category endpoint exists).

> **v1.24.0 — categories management suite, fully live-tested 7 Jul 2026:** Added a proper inventory-**category** CRUD suite (5 tools) — the first category tools in the server. `get_categories(with_counts=False)` lists all categories (one fast GET); pass `with_counts=True` to also tally items-per-category and flag the empty ones (triggers a full catalogue sweep, see below). `create_category(name, dry_run=True)` (dup-name guarded), `rename_category(category_id, new_name, dry_run=True)` (carries the full record through — UpdateCategory clears omitted fields; Default protected), `delete_categories(category_ids, force=False, confirmed_count, dry_run=True)` (delete specific ids; **refuses non-empty unless `force=True`**, which reassigns their items to Default), and `delete_empty_categories(confirmed_count, dry_run=True)` — the **headline cleanup**: sweeps the catalogue, finds every zero-item category (Default always excluded), and bulk-deletes on confirm. **Emptiness has no dedicated endpoint** (`GetInventoryItemsCount` is a global total only), so it's derived by sweeping the whole catalogue via `Stock/GetStockItems` (`keyWord=""`, ~161 pages / 32k items / ~2 min) and tallying `CategoryId` — the shared `_count_items_per_category()` helper **throttles to <150/min and backs off on HTTP 429** (a 161-page sweep trips the limit otherwise), and always runs to completion (a partial scan could misreport a used category as empty → wrongful delete). Both delete tools use the tightest staging **threshold 10** (destructive; shared with `delete_inventory_item`), `dry_run=True` default, read-back-after-delete, and hard-protect the zero-GUID **Default** category (never rename/delete). **Fully live-tested** (unlike the GLT write paths): create→rename→delete cycled against throwaway `ZZZ-MCP-TEST-CAT*` categories with read-back; the sweep confirmed **62 of 181 categories empty** (junk like `#REF!`/`AUCTION`/`SHOWS` + archived ranges). CreateCategory mints the `CategoryId` server-side (**no** client GUID needed, unlike the `pkRowId` sub-entity Create* endpoints); Update/DeleteCategoryById return an empty 2xx on success. No new broken endpoints.

> **v1.23.0 (issue #22) — read/selection path live-tested 7 Jul 2026:** Added `unpublish_channel_listing(skus, sub_source="SWH Shopify", …)` — the **destructive take-down counterpart** to `list_to_shopify` (creates) and `refresh_channel_listing` (revises). Where those keep a listing alive, this **ends** it: the GLT `ProcessTemplates` **`Action="Delete"`** against the item's existing template retires the live Shopify listing so it stops selling. Built for the issue's duplicate-item cleanup — after a SKU-scheme migration leaves an orphaned Linnworks item still live on Shopify at a frozen quantity (silently able to oversell), this takes those listings down in bulk instead of doing it by hand in the Shopify admin. **The delete primitive is the SAME pair as the revise path** — `GenericListings/OpenTemplatesByInventory` opens the item's EXISTING template (never creates → can't duplicate), then `ProcessTemplates` with the Delete action. The tool mirrors `refresh_channel_listing`'s selection path 1:1 (resolve → confirm SHOPIFY+`sub_source` channel-SKU mapping → OpenTemplatesByInventory → skip locked templates) but **forces `Action="Delete"`** (no auto-action selection — delete is delete). **Read-before-write** captures each listing's current `channel_reference_id` + `listed_quantity` into the plan/manifest so you can confirm you're retiring the orphan and NOT the good listing (with reviews/ranking); **read-back-after** re-reads the channel-SKU table per SKU and reports `taken_down`/`still_listed` (the channel can lag sync, so a `still_listed:true` row may simply not have propagated — re-check with `get_channel_listings`). **Tightest staging tier — threshold 10** (shared with `delete_inventory_item`), lower than list/refresh's 25, because taking a live listing down is destructive. `_check_injection` on `sub_source`; `dry_run=True` default. **Read/selection path live-confirmed 7 Jul 2026** (RS-102201 on SWH Shopify → plan row: template_id 43285 / active_listing_id 9425839882486 / status "Listed" / ref + qty captured / action "Delete" — the SAME template_id `refresh_channel_listing` opened for that SKU; bogus SKU → unresolved; bad store → error + available list; real SKU on a store it's not listed on → "nothing to take down"). The **`ProcessTemplates` Delete push is spec-based, NOT yet live-run** — it retires a real customer-facing listing and the issue did not authorise a live delete, so start with a single orphan SKU you're certain about. Uses the `Delete` value already in the `ProcessTemplates` action enum (confirmed in `genericlistings.json`: `NotAllowed/ChannelSpecific/Create/Update/Relist/Delete/Revise`). No new endpoints — reuses `OpenTemplatesByInventory` + `ProcessTemplates` + `GetInventoryItemChannelSKUs`. NB: v1 ends the storefront listing (the issue's primary ask); it does **not** separately delete the channel-SKU link row — the Delete action removes the mapping as a side effect of ending the listing.

> **v1.22.0 (issue #21) — fully live-tested 23 Jun 2026:** Added channel-specific **title** read+write plus a **description getter**, filling the last gap in the per-channel content suite (descriptions + prices already had setters; titles had neither, and descriptions had no getter). Three tools: `get_inventory_item_titles(sku)`, `set_inventory_item_titles(titles, confirmed_count, dry_run=True)`, and `get_inventory_item_descriptions(sku)`. Linnworks stores per-channel listing **titles** that can diverge from the base `ItemTitle` (set via `create_or_update_inventory_item`) — keyed by `(StockItemId, Source, SubSource)`, exactly like descriptions/prices. The endpoints mirror descriptions 1:1: `Inventory/GetInventoryItemTitles` (**GET**, `?inventoryItemId=<guid>` → array of `StockItemTitle` {pkRowId, Source, SubSource, Title, StockItemId}), `Inventory/CreateInventoryItemTitles` / `UpdateInventoryItemTitles` (**POST**, body `{"inventoryItemTitles":[...]}`). `set_inventory_item_titles` follows `set_inventory_item_descriptions` exactly — read-before-write manifest (old/new + create/update decision per row), client-generated `pkRowId` GUID on create, `_check_injection` on title text, `_write_guard` threshold 50, `dry_run=True` default. **Fully live-tested** (unlike the description write, which was the live template): read path on a real SKU (RS-102201 → 3 channel titles AMAZON/EBAY/SHOPIFY, 2 descriptions); write path create→read-back→update→read-back→delete cycled against an isolated `ZZZ-MCP-TEST-TITLE-*` SKU (create + update both verified, then SKU deleted). `Inventory/DeleteInventoryItemTitles` (`{"inventoryItemTitleIds":[guid,...]}`) exists but is **not wrapped** by a tool (no delete request in the issue). NB: these set the channel-title *override* only — they do NOT touch the base `ItemTitle`; correct both when fixing product data, then push live with `refresh_channel_listing`.

> **v1.21.0 (issue #20) — read path live-tested 18 Jun 2026:** Added `refresh_channel_listing(skus, sub_source="SWH Shopify", action=None, …)` — the **revise counterpart to `list_to_shopify`**. Where `list_to_shopify` *creates* listings, this *revises* existing ones so edited item data (extended properties, title, price, description) propagates to the live Shopify listing — the API form of the UI "open template → revise" flow. It **never creates** a listing: a SKU not already live on the target store lands in `unresolved`. **Key discovery — the revise primitive is `GenericListings/OpenTemplatesByInventory`, NOT `CreateTemplates`** (the issue hypothesised reusing `CreateTemplates`, but that *creates* templates and would risk a duplicate — `OpenTemplatesByInventory` *opens the existing* template). It takes the same `InventorySearchParameters` (`ChannelId` + `InventoryItemIds`) and is a **safe read** (live-probed against a real listed SKU with no side effects). Each returned `TemplatesInfo[]` row carries top-level (un-wrapped) `Id` (= the `TemplateId` for ProcessTemplates), `StockItemId`, `ConfiguratorId`, `IsLocked`, `IsAllowedToRevise`, `NextSuggestedAction` (the action GLT itself computed — **"Update"** for a Shopify item with a pending change), `IsNextSuggestedActionAllowed`, plus a nested wrapped `Info` (`ActiveListingId` = the Shopify product id, `Status` = "Listed"). A non-listed SKU returns empty `TemplatesInfo`. **Action is data-driven:** `action=None` (auto) uses the template's `NextSuggestedAction` when allowed, else falls back to `"Revise"`; pass `action="Revise"/"Update"/…` to force one. Flow per SKU: resolve → confirm SHOPIFY+`sub_source` mapping (channel-SKU table) → `OpenTemplatesByInventory` → (live) `ProcessTemplates` `Action=Revise/Update`. **Read/selection path live-confirmed** (listed SKU → plan row with template_id 43285 + action "Update"; non-listed/bogus → unresolved; bad store → error; action override + validation); the **`ProcessTemplates` revise push is spec-based, NOT yet live-run** (it changes a real customer-facing listing — start with one SKU). Shared `_write_guard` (threshold 25) + `_check_injection` + `dry_run=True` default. `sub_source`→`ChannelId` resolved from the configurator catalogue (confirmed 1:1: 18=SWH Shopify, 21=Venom, 26=Icarus, 29=Lobster, 34=TWG B2B).

> **v1.20.0 (issue #19) — live-tested 18 Jun 2026:** Added `get_inventory_item_images(sku)` and `get_inventory_item_images_bulk(skus)` — the **read-side companion** to `add_inventory_item_images` and the third piece of the safe-bulk-listing workflow alongside `get_channel_listings` (#18) and `list_to_shopify` (#16). They answer "does this SKU have a product image, how many, and is a main image set?" so a brand's in-stock items can be filtered to the genuinely list-ready ones before pushing to Shopify. Single = `Inventory/GetInventoryItemImages` (**GET**, `?inventoryItemId=<guid>`); bulk = `Inventory/GetImagesInBulk` (**POST**, payload **WRAPPED** `{"request":{"StockItemIds":[...]}}` — sending it unwrapped returns HTTP 400 "request is empty"; chunked at 200). Both return `has_image`, `image_count`, `has_main_image` (spot items that have *a* picture but no main set), and per-image rows (`image_id`, `is_main`, `sort_order`, `full_url`, `thumbnail_url`) sorted by sort_order. **Field-shape gotcha:** the single GET returns the thumbnail under `Source`, the bulk POST under `FullSourceThumbnail` — both use `FullSource` for the full image; `_format_image_row` normalizes both. The bulk response carries `StockItemId` per image but **not** `SKU`, so the bulk tool resolves SKU→StockItemId first (also yielding titles + an `unresolved` split, mirroring `get_channel_listings_bulk`) then maps images back by id; an item that exists but has zero images is `has_image:false` (a real item to fix), distinct from an `unresolved` SKU (not in catalogue). All 5 scenarios live-verified (item with 2 images/1 main, bogus SKU → error/unresolved, bulk mixed real+bogus, and 3 real zero-image items → `has_image:false`). No new broken endpoints.

> **v1.19.0 (issue #18) — live-tested 18 Jun 2026:** Added `get_channel_listings(sku)` and `get_channel_listings_bulk(skus)` — the **read-side companion** to `list_to_shopify`. They answer "is this SKU already listed, and on which channel/store?" by reading the channel-SKU link table: single = `Inventory/GetInventoryItemChannelSKUs` (**GET**, `?inventoryItemId=<guid>`), bulk = `Inventory/BatchGetInventoryItemChannelSKUs` (**POST**, payload **unwrapped** `{"inventoryItemIds":[...]}`, chunked at 200 for the 150/min rate limit). A non-empty row set = the item is mapped/listed on that `Source`+`SubSource`; both tools return `is_listed`, `listing_count`, distinct `channels`/`sub_sources`, and per-channel `listings` (source, sub_source, channel_sku, channel_reference_id, update_status, listed_quantity, …). **Live-confirmed across SHOPIFY (per store: SWH Shopify / Icarus Eyewear / The Warehouse Group B2B / Lobster Eyewear), EBAY (EBAY0), AMAZON (per region), Mirakl MP, MAGENTO.** No clean listing-URL field exists — `channel_sku` + `channel_reference_id` identify the listing (Shopify ref = `product:variant:inventory` id triple). **Bonus — `list_to_shopify` now dedupes:** before grouping, every resolved item is checked against the channel-SKU table and any that already has a SHOPIFY listing on its target store (the configurator's `sub_source`) is moved into a new `already_listed` output and **excluded from `CreateTemplates`** — so a bulk run (dry **or** live) can't silently create a duplicate customer-facing listing. A lookup failure degrades safely (dedupe skipped, `dedupe_warning` surfaced). The read endpoints + the dedupe split were live-verified (listed-on-SWH-Shopify SKU → `already_listed`; unlisted SKU → stays in `plan`); the `list_to_shopify` **write** path remains spec-based as before. No new broken endpoints — the all-caps `GetInventoryItemChannelSKUS` in the issue is actually `…ChannelSKUs` (lowercase final s).

> **v1.18.0 (issue #17) — live-tested 18 Jun 2026:** Added `get_item_relationships(sku)` — one read call that resolves a SKU's parent/child links across BOTH relationship types and BOTH directions. **Variation reverse lookup (Open Q1) resolved POSITIVE:** `Stock/SearchVariationGroups?searchType=ItemSKU` substring-matches member SKUs, and exact membership is then confirmed via `Stock/GetVariationItems`, so a child SKU returns its parent group + siblings (`reverse_lookup_confirmed: true`). Forward variation: `Stock/GetVariationGroupByParentId` returns the group for a parent (HTTP 200 `null` for non-parents; the parent's `StockItemId` == the group's `pkVariationItemId`) → `GetVariationItems` lists children (parent NOT included). Forward composite: `Inventory/GetInventoryItemCompositions?getFullDetail=true` returns components (`LinkedStockItemId` = component id; the row's own `StockItemId` is the PARENT's id; `[]` for non-composites). **Composite reverse (Open Q2) confirmed UNSUPPORTED** — no component→parent endpoint and no working catalogue-list endpoint to scan, so `composite.reverse_lookup_supported` is always `false` and a non-parent can't be positively classified as a "component". **Gotcha:** the `IsVariationParent` flag on `GetInventoryItem`/`GetStockItems` is **unreliable** (observed `false` on a genuine variation parent), so role is derived from the relationship endpoints, never the flag. Companion fix: `find_inventory_item` now also returns `IsVariationParent` for parity with `search_inventory_items`. All 6 scenarios live-verified (variation parent/child, composite parent, standalone, composite component, not-found). No new broken endpoints — all candidates worked.

> **v1.17.0 (issue #16) — read path live-tested 18 Jun 2026:** Added `list_to_shopify` — the first **listing/channel** tool, wrapping the Generic Listing Tool (GLT) to push EXISTING Linnworks inventory to Shopify. Per-item configurator selection is data-driven: `configurator` override → the item's `Shopify Configurator` extended property → `default_configurator` fallback, then validated against the live Shopify configurator catalogue (a name that doesn't match becomes a per-item error row, never sinks the batch). **Channel-identity gotcha cracked live:** GLT identifies Shopify by `ChannelType="Shopify"` + `ChannelName="SHOPIFY"` — the uppercase **Source** string, NOT a SubSource store name (`"SWH Shopify"`, `"Venom Skateboards"` etc. all 400 with "Channel types mismatch on channel factory creation"). The individual stores are distinguished by each configurator's `ChannelId`/`SubSource` (18=SWH Shopify, 21=Venom Skateboards, 26=Icarus Eyewear, 29=Lobster Eyewear, 34=The Warehouse Group B2B). Configurator names are **not unique across stores** (12 names duplicated), so a shared name is reported as ambiguous unless `sub_source` scopes it. **Read/selection path live-confirmed** (catalogue fetch = 67 configurators, SKU→GUID, property read, override/default/validation/ambiguity/scoping/staging/injection all verified); the **write path (`CreateTemplates`+`ProcessTemplates`) is spec-based, NOT yet live-exercised** — a live run creates real customer-facing Shopify listings, so start with a single SKU. The issue named these endpoints under `Listings/` but they live under `GenericListings/`, and the configurator is applied at `CreateTemplates` time via `ConfiguratorId` (not a separate `SaveConfiguratorData` step — those modify a configurator's own definition).

> **v1.16.0 (issue #15) — live-tested 16 Jun 2026:** Fixed PO line-cost write — Linnworks `Cost` is the **tax-inclusive line total** (`unit × qty × (1+rate)`), not the ex-VAT unit cost. `create_purchase_order`, `add_purchase_order_item`, and `update_purchase_order_item` now convert via `_po_line_cost_inc_tax()` before writing (was sending the bare unit cost → wrong unit prices and PO totals). Read-back paths (`get_purchase_order`, the update diff) expose a derived `unit_cost_ex_tax` via `_po_line_unit_ex_tax()` so reads reconcile with the ex-VAT unit the tools accept. Also fixed `create_purchase_order`'s return `total_cost`, which read a non-existent `GrandTotal` header key (→ `None`) instead of `TotalCost`. **Verified end-to-end against a live test PO** (create 3×£10@20% → £36; add 15×£17.50@20% → £315; update qty 3→5 → £60; update unit £10→£12.34 → £74.04; all header totals/tax correct), then deleted via `Delete_PurchaseOrder`.

---

## Brain — load at session start

This project has a private `.brain/` intelligence layer that tracks change history
and known patterns. Load it at the start of every session:

1. Read `.brain/HOOK_LOG.md` — recent commit annotations (what changed, why it matters, risk flags)
2. Read `.brain/BRAIN.md` — known fragile areas, patterns, decisions, carry-forwards

Confirm both are loaded before writing any code. If either file is missing, note it and continue.

**At version release:** follow the rollup instructions in `.brain/rollup.md` to update
`BRAIN.md` from the log, then write the version marker into `HOOK_LOG.md`.

---

A local stdio MCP server that exposes Linnworks data to Claude Desktop. This is **Phase 1** of a two-phase plan:

- **Phase 1 (current)**: Single-tenant stdio server, runs on your machine, no OAuth, no hosting. Used to iterate on tool design with a fast feedback loop.
- **Phase 2 (later)**: Same tool functions wrapped as a remote MCP server with OAuth 2.1 + Dynamic Client Registration, deployed to a public host, registered as a custom connector in claude.ai.

---

## Write-safety framework

Every bulk write tool (any tool that takes a list of items) must use two shared helpers defined near the top of `server.py`:

### `_write_guard(operation, items, confirmed_count, dry_run, threshold=None)`

Staged-manifest gate. Call this at the top of any write tool before touching the Linnworks API.

- **Batch ≤ threshold** → returns `None`; standard `dry_run` logic applies.
- **Batch > threshold, `confirmed_count` is None** → returns a blocking dict with `staged=True`. The caller should build the per-item manifest preview and merge it into this dict before returning. No writes happen.
- **Batch > threshold, `confirmed_count` ≠ `len(items)`** → returns an error dict. Prevents injection from bypassing staging by guessing a wrong count.
- **Batch > threshold, `confirmed_count` == `len(items)`** → returns `None`; execution proceeds.

The `confirmed_count` echo-back is the key protection: an injected instruction can't predict the exact count without first seeing the manifest.

Thresholds (defined in `WRITE_THRESHOLDS`):

| Operation | Threshold | Reason |
|---|---|---|
| `set_stock_levels` | 25 | Immediate channel availability impact |
| `set_inventory_item_prices` | 25 | Immediate channel price impact |
| `create_or_update_inventory_item` | 50 | Channel sync is async |
| `set_extended_properties` | 50 | Metadata, lower blast radius |
| `set_inventory_item_descriptions` | 50 | Content, lower blast radius |
| `set_inventory_item_titles` | 50 | Content, lower blast radius |
| `set_inventory_item_suppliers` | 50 | Purchasing metadata |
| `add_inventory_item_images` | 100 | Additive only, no overwrites |
| `delete_inventory_item_images` | 10 | Irreversible — removes images from an item |
| `set_inventory_item_image_order` | 25 | Reorders images; the main image is the storefront hero |
| `delete_inventory_item` | 10 | Irreversible deletion |
| `delete_purchase_order` | 10 | Irreversible — deletes whole PO (header + all lines) |
| `delete_categories` | 10 | Irreversible; non-empty categories reassign items to Default |
| `delete_empty_categories` | 10 | Irreversible bulk delete |
| `set_order_status` | 25 | Reversible, but lock RELEASES allocated stock |
| `archive_inventory_items` | 25 | Reversible (unarchive) |
| `unarchive_inventory_items` | 25 | Reversible (archive) |
| `list_to_shopify` | 25 | Creates live customer-facing channel listings |
| `refresh_channel_listing` | 25 | Re-pushes live listings — may push a STALE template snapshot |
| `unpublish_channel_listing` | 10 | Destructive — ENDS a live customer-facing listing |
| `repair_channel_listing_images` | 10 | Writes to live customer-facing product pages via the Shopify Admin API |
| `delist_all_channel_listings` | 10 | Destructive — ends listings across every GLT channel |
| `delist_all_shopify_listings` | 10 | Destructive — ends listings across every Shopify store |
| `revise_ebay_listing_description` | 10 | Writes to a live eBay listing; fired live 26 Aug 2026 but accepted with no observed channel effect — `revise_proven` stays False on that evidence |
| `default` | 25 | Fallback for unlisted operations |

This table is hand-maintained against `WRITE_THRESHOLDS` in `server.py` (and duplicated in README.md) — `tests/test_docs_consistency.py` now fails the build if any of the three drift apart. It had fallen 11 rows behind between v1.22.0 and v1.34.0.

There is **no hard cap** — any batch size works once confirmed. The threshold is a staging gate, not a refusal.

### `_check_injection(field_name, value)`

Last-resort prompt injection tripwire. Call this on every free-text write parameter (titles, descriptions, notes, extended property values) before forwarding to Linnworks.

Raises `ValueError` naming the offending field if the value matches a known injection signature (e.g. `"ignore previous instructions"`, `"system:"` prefix, `[INST]` tags, `<|im_start|>` markers, `"act as"`, `"forget everything"`, etc.).

**This is not a comprehensive defence** — it fails loudly on obvious attacks and passes everything else. The primary safety nets remain `dry_run=True` default, read-before-write, and per-item result reporting.

### Usage pattern for a bulk write tool

```python
@mcp.tool()
def set_stock_levels(updates: list[dict], confirmed_count: int | None = None, dry_run: bool = True) -> dict:
    # 1. Injection check on all text fields
    for u in updates:
        _check_injection("note", u.get("note", ""))

    # 2. Build manifest (always — needed for both staging and dry_run)
    manifest = [_preview_stock_change(u) for u in updates]

    # 3. Write guard — may return a blocking staged dict
    guard = _write_guard("set_stock_levels", updates, confirmed_count, dry_run)
    if guard is not None:
        return {**guard, "manifest": manifest}  # merge in the preview

    if dry_run:
        return {"dry_run": True, "item_count": len(updates), "manifest": manifest}

    # 4. Execute in chunks of 25, collect per-item results
    results = _execute_in_chunks(updates, chunk_size=25)
    return {"dry_run": False, "item_count": len(updates), "results": results}
```

---

## Conventions

- **Python 3.10+**, type hints throughout. Tool docstrings become the descriptions Claude sees — write them carefully, that's the UX.
- **Secrets stay in env vars.** Never hardcode, never log, never commit. `.env` is gitignored; in production credentials come from Claude Desktop's config `env` block.
- **Authorization header is the raw token — no `Bearer ` prefix.** This is the most common auth bug.
- **Open Orders endpoints require the `{"request": {...}}` wrapper.** Without it: `'request' parameter is missing`.
- **Read before write.** Every write tool must (a) fetch current state first, (b) log old vs new, (c) read back after, and (d) default to `dry_run=True`. No exceptions.
- **Surface Linnworks errors verbatim** in tool exceptions — the response body almost always contains the real reason.
- **Cache the auth token for the process lifetime, re-auth once on 401.** Don't auth on every call (rate limits) and don't pretend tokens never expire (they do).

---

## Tools

85 tools. `python server.py --list-tools` is the authoritative count; see `server.py` for full docstrings and parameter details.

> **v1.11.0:** Inventory write suite — `create_or_update_inventory_item`, `set_stock_levels`, `set_inventory_item_prices`, `set_extended_properties`, `set_inventory_item_descriptions`, `add_inventory_item_images`, `create_variation_group`. All protected by `_write_guard` + `_check_injection`. **Live-tested 15 Jun 2026** against an isolated test SKU (create/update/stock/price/description/extended-property all verified by read-back) — this surfaced and fixed the `StockItemId`/`pkRowId`/empty-body/default-price gotchas; see the Inventory writes section. `add_inventory_item_images` and `create_variation_group` not yet exercised live.
>
> **v1.10.0:** Write-safety framework (`_write_guard`, `_check_injection`, `WRITE_THRESHOLDS`). `run_import` and `run_export`.
>
> **v1.5.0:** `get_order` now returns `delivery_address` and `billing_address` dicts with all address fields. New `set_order_address` write tool.

### Orders & stock (read)

| Tool | Endpoint | Key notes |
|---|---|---|
| `get_open_orders(location_id, limit, overdue_only)` | `OpenOrders/GetOrdersLowFidelity` | Always returns `overdue_count`; `overdue_only=True` filters past-deadline; `StatusLabel` decoded |
| `find_inventory_item(sku_or_title)` | `Inventory/GetInventoryItem` | **Exact SKU only** — no fuzzy/title search; use `search_inventory_items` for keyword/name lookup. Returns `IsCompositeParent` + `IsVariationParent` (the latter added in v1.18.0 for parity with `search_inventory_items` — but see the unreliability note under `get_item_relationships`) |
| `search_inventory_items(keyword, page, per_page)` | `Stock/GetStockItems` (GET) | **Keyword discovery** — matches title, SKU, and barcode (the UI search box endpoint). Paged (`per_page` capped at 200); returns `total_entries`/`total_pages` + per-item `sku`, `stock_item_id`, `title`, `stock_level`, `available`, `barcode`, `retail_price`, `is_composite_parent`, `is_variation_parent`, etc. so results chain straight into stock/price/write tools. **Live-tested 16 Jun 2026** (closes #14) |
| `list_inventory_items(page, per_page, all_pages, include_stock_levels, location_id, zero_stock_only, include_composite_parents, include_variation_parents, flag_composite_parents, max_items)` | `Stock/GetStockItemsFull` (POST) [+ the composite index when `flag_composite_parents`] | **Bulk enumerator — "give me EVERYTHING"**, the counterpart to `search_inventory_items` (keyword) and `find_inventory_item` (exact SKU). Built for the cleanup sweep: every active item with its quantity, to find what's dead across ALL locations, then hand to `find_composite_parents` → `delist_all_channel_listings` → `archive_inventory_items`. **Per-location stock is the point:** `include_stock_levels=True` (default) gives a `locations[]` breakdown and derives `quantity`/`available`/`in_order`/`due` by **summing every location** (this model has no top-level Quantity) — so "zero everywhere" is answerable in one sweep; `location_id` scopes to one instead. `zero_stock_only=True` filters to the cleanup candidates. **`include_composite_parents`/`include_variation_parents` default True** (they are INCLUDE flags server-side — False makes those parents ABSENT). `is_variation_parent` is real; **`is_composite_parent` is `None` = "not determined"** unless `flag_composite_parents=True` (fills it from the cached composite index, ~215 extra calls first time). Levels unread → `quantity: None` ("not read"), never 0. `max_items` (default 5000) truncates only the returned detail — `scanned_count`/`matched_count` always span everything swept. **⚠️ ACTIVE items only** (archived unreachable). **AUTOPAGINATING** with `all_pages=True` (~194 calls / ~132s here) — don't run beside another sweep tool. **Fully live-tested 5 Aug 2026** (closes #32) |
| `get_item_relationships(sku)` | `Inventory/GetInventoryItem` → `Stock/GetVariationGroupByParentId` + `Stock/GetVariationItems` + `Stock/SearchVariationGroups` (variation) and `Inventory/GetInventoryItemCompositions` (composite) | **Parent/child relationship lookup** for one exact SKU, both directions, both types. Returns `variation` {role parent/child/none, group_name, parent_sku, children[], siblings[], reverse_lookup_confirmed} and `composite` {role parent/none, components[] (sku, linked_stock_item_id, qty, purchase_price), belongs_to[], reverse_lookup_supported, reverse_lookup_tool}. Top-level `is_variation_parent`/`is_composite_parent` are **derived from the live endpoints, not the raw item flags** (the `IsVariationParent` flag is unreliable). Variation reverse (child→parent+siblings) works via `searchType=ItemSKU` + exact-membership confirmation. Composite reverse (component→parents) is **not resolved here** — it needs a catalogue-wide index, so `belongs_to` stays empty and `reverse_lookup_supported` stays False, meaning "not answered by this call" — **use `find_composite_parents` (v1.33.0, issue #31)**, which does answer it. **Live-tested 18 Jun 2026** (closes #17) |
| `find_composite_parents(skus, include_listing_status=True, max_parents_listed=25, rebuild_index=False)` | `Stock/GetStockItems` (sweep) + `Inventory/GetInventoryItemsCompositionByIds` (invert) + `Inventory/BatchGetInventoryItemChannelSKUs` (listing status) | **Component → composite parents** — the REVERSE of `get_item_relationships`, and the **archive-safety gate**: "is this dead-looking SKU still a component of a live bundle?" Run before `archive_inventory_items` / `delete_inventory_item` / `delist_all_channel_listings`. Per SKU: `is_component`, `parent_count`, `listed_parent_count`, `has_listed_parent`, **`safe_to_retire`**, `parents[]` (parent_sku, parent_title, quantity, parent_is_listed, parent_channels), `parents_truncated`. **Batch-first by design** — Linnworks has no component→parent endpoint, so it enumerates every composite parent (`IsCompositeParent` on the sweep, live-verified reliable) and inverts their component lists: **~215 calls / ~105s** here (34,023 active items → 4,343 parents → 2,908 components), **built once and cached 15 min**, so pass the whole candidate list in ONE call. Counts always span ALL parents even when `parents[]` is capped (some components sit in 3,000+ composites) — the verdict is never truncated. `safe_to_retire` is `None` (not `True`) whenever listing status wasn't read. **⚠️ ACTIVE items only** — archived composite parents are invisible. Don't run in parallel with the other autopaginating tools. **Fully live-tested 5 Aug 2026** (closes #31) |
| `get_order(order_id)` | `Orders/GetOrdersById` or `GetOrderDetailsByNumOrderId` | GUID → POST; numeric ID → GET; returns `customer_name`, `customer_email`, `delivery_address`, `billing_address`, `notes` list, `totals` (subtotal/postage/tax/total/currency), `fulfilment_location_id`; each item now also includes `row_id` (OrderItemRowId — required by `refund_order_lines`), `price_per_unit`, `cost_inc_tax` |
| `set_order_address(order_id, ..., require_complete=False, dry_run=True)` | `Orders/SetOrderCustomerInfo` | **The only order-address tool** (v1.35.0 absorbed `update_order_shipping_address`). Open orders only; GUID or numeric order_id; every field defaults to None = keep current, so ONE call covers both a one-line amendment and a whole-address change. **`require_complete=True`** refuses the call unless full_name/address1/town/postcode/country are all supplied non-blank — use it whenever the destination itself changes, so a partly-supplied address can't merge with the old one and ship to a hybrid (new street, old town). It checks what the CALLER passed, not the merged result, and fails before any API call. Read-before-write diff, read-back after the write, blocks on processed orders |
| `get_order_notes(order_id)` | `Orders/GetOrderNotes` | Fetch all notes on an order (open or processed); returns note_id, text, internal flag, timestamp, creator; GUID or numeric order_id |
| `add_order_note(order_id, note, internal=True, dry_run=True)` | `Orders/AddOrdersNote` | Add a note to any order; internal=True by default (staff-only); dry_run default; works on open and processed orders |
| `update_order_note(order_id, note_id, note, internal=None, dry_run=True)` | `ProcessedOrders/DeleteOrderNote` + `Orders/AddOrdersNote` | No dedicated update endpoint — deletes old note then adds replacement; preserves internal flag if not supplied; before/after diff |
| `delete_order_note(order_id, note_id, dry_run=True)` | `ProcessedOrders/DeleteOrderNote` | Permanently removes a note; read-before-write confirms existence and captures text; dry_run default |
| `delete_order_notes_by_text(order_id, text, match, case_sensitive, max_to_delete, dry_run=True)` | `Orders/GetOrderNotes` + `ProcessedOrders/DeleteOrderNote` | Delete notes by content match — no note_id needed; match modes: exact (default), contains, starts_with; case_insensitive by default; max_to_delete guard prevents accidental bulk deletion; zero matches = success |
| `find_open_orders_for_sku(sku, location_id)` | `GetOrdersLowFidelity` + `GetOrdersById` | Finds all open orders containing a SKU; searches composite children too; enriches with customer name + email; use for "who's waiting on this item?" |
| `find_orders_by_reference(reference, include_processed=False, location_id)` | `OpenOrders/SearchOrders` + `GetOrdersById` | Look up orders by channel reference (Shopify "#11177274", Amazon "202-...", eBay etc.); strips leading #; returns customer name + email + external_reference; include_processed=True extends to dispatched orders; **live-tested 15 Jun 2026** — payload must be sent **unwrapped** (see endpoints table) |
| `cancel_order(order_id, note=None, dry_run=True)` | `Orders/CancelOrder` | Cancel an open (unprocessed) order; refuses if already processed; dry_run shows items that would be cancelled; `fulfilmentCenter` taken from `FulfilmentLocationId` on the order; no `return_to_stock` param in API — controlled by workspace settings |
| `set_order_status(order_ids, action, confirmed_count=None, dry_run=True)` | `Orders/LockOrder` (lock/unlock) · `Orders/ChangeStatus` (paid/unpaid) | Change order workflow state in bulk. `action` ∈ **lock/unlock/paid/unpaid**. `lock` holds an order from picking/dispatch (padlock) — the intended "hold" action (e.g. pending a cancellation). paid→status 1, unpaid→status 0 (enum `0=UNPAID,1=PAID,2=RETURN,3=PENDING,4=RESEND`). GUID or numeric ids (bare string accepted); one API call for the whole resolved batch; read-before-write manifest; unresolvable id → `resolve_errors` (non-fatal); threshold 25; `dry_run=True` default. **⚠️ `park`/`unpark` NOT supported** (no endpoint; rejected → use lock). **⚠️ lock RELEASES allocated stock** (park would keep it, but park is unreachable) and lock has **no read-back** (verify padlock in UI); paid/unpaid ARE read-back verified. **lock/unlock LIVE-PROVEN 28 Jul 2026 (order 606371 round-trip); paid/unpaid not yet live-fired** |
| `refund_order(order_id, note=None, push_to_channel=True, dry_run=True)` | `ReturnsRefunds/GetRefundOptions` + `ReturnsRefunds/CreateRefund` + `ReturnsRefunds/ActionRefund` | Full refund of all items + postage on a **processed** order; `push_to_channel=True` calls ActionRefund to push to Shopify/Amazon/eBay; **spec-based, not yet live-tested** |
| `refund_order_lines(order_id, lines, refund_postage=False, note=None, push_to_channel=True, dry_run=True)` | `ReturnsRefunds/GetRefundOptions` + `ReturnsRefunds/CreateRefund` + `ReturnsRefunds/ActionRefund` | Partial refund of specific lines; each entry needs `row_id` from `get_order` items; optional `amount` override and `quantity`; **spec-based, not yet live-tested** |
| `get_stock_level(sku, location_id, include_empty_locations)` | `GetInventoryItem` → `Stock/GetStockLevel_Batch` | Zeros filtered by default; warns on virtual dropship duplicate rows |
| `get_stock_change_history(skus, location_id, all_locations, include_movements, max_movements, change_sources, exclude_change_sources, max_pages)` | `Stock/GetItemChangesHistory` (GET) + `Stock/GetStockLevel_Batch` | **Stock movement history + "when did this SKU actually go out of stock?"** — the audit trail behind a stock level, and the missing ageing step in the cleanup chain (`list_inventory_items` finds what's at zero; this says since when). Per SKU/location: `out_of_stock_since`, `days_out_of_stock`, `last_movement_date`, `last_real_movement_date`, `last_sale_date`, `last_received_date`, plus `movements[]` (date, change_source, level_after, quantity, note). **`out_of_stock_since` is the TRANSITION to zero** — the oldest row in the current unbroken run of `level_after == 0`, NOT the newest zero row (a dead item still gets automated rows rewriting 0, which would reset the clock to today). **⚠️ PER LOCATION — `locationId` is REQUIRED** (the spec's "if null then combined" is wrong); `all_locations=True` gives a per-location breakdown + `zero_at_all_locations_since` (the **LATEST** per-location zero date), and never sums quantities across locations (supplier mirrors duplicate them). **⚠️ `change_source` is DERIVED from the free-text note** — Linnworks returns no such field. Filters apply to `movements`/`last_real_movement_date` **only**, never to the level series (FBA-sync rows are genuine decrements here). Current level comes from stock levels, not history (an item can hold stock with zero history rows). Truncated tail → `out_of_stock_since_is_lower_bound`. Cost ~1 call/(SKU, location) page; `all_locations` ≈ 29 calls/SKU. **Fully live-tested 6 Aug 2026** (closes #33) |
| `get_processed_orders(from_date, to_date, date_field, page, page_size)` | `ProcessedOrders/SearchProcessedOrders` | Flat response; min page_size 20; overflows context at 500 — use aggregation tools for wide ranges |
| `get_locations()` | `Inventory/GetStockLocations` | Returns all physical + virtual locations |
| `get_extended_properties(sku)` | `Inventory/GetInventoryItemExtendedProperties` | API typo in field name: `ProperyName` not `PropertyName` |
| `get_inventory_item_titles(sku)` | `Inventory/GetInventoryItemTitles` (GET) | Channel-specific listing titles keyed by (Source, SubSource); returns `base_title` (the `ItemTitle`), `title_count`, `default_title`, and per-row `titles` (source, sub_source, title, pk_row_id). Read companion to `set_inventory_item_titles`. **Live-tested 23 Jun 2026** (issue #21) |
| `get_inventory_item_descriptions(sku)` | `Inventory/GetInventoryItemDescriptions` (GET) | Channel-specific descriptions keyed by (Source, SubSource); returns `description_count`, `default_description`, and per-row `descriptions` (source, sub_source, description, pk_row_id). Read companion to `set_inventory_item_descriptions` (which previously had no getter). **Live-tested 23 Jun 2026** (issue #21) |
| `get_inventory_item_suppliers(sku)` | `Inventory/GetStockSupplierStat` (GET) | Per-item **purchasable supplier links** (StockItemSuppliers table): supplier, supplier_id, code, cost, lead_time, min_order_qty, pack_size, currency, is_default + `default_supplier`. The table `get_sales_by_supplier` keys off (IsDefault); zero rows = invisible to supplier-keyed exports/restock tooling. Read companion to `set_inventory_item_suppliers`. **Live-tested 14 Jul 2026** (issue #25) |
| `get_processed_order_items(from_date, to_date, ...)` | `SearchProcessedOrders` + `GetOrdersById` batched | Line-item detail per order; use `get_top_skus` instead for aggregated SKU analysis |

### Reporting (read, autopaginating)

| Tool | Notes |
|---|---|
| `get_revenue_summary(from_date, to_date, date_field)` | Totals + breakdown by channel and country. Best for "what was revenue in April?" |
| `get_top_skus(from_date, to_date, date_field, top_n, rank_by, supplier_name)` | Rank SKUs by revenue or units; optional supplier filter (case-insensitive partial match) |
| `get_category_report(from_date, to_date, date_field, top_n)` | Revenue + units by product category |
| `get_period_comparison(current_from, current_to, prior_from, prior_to, date_field)` | Side-by-side totals with % deltas for orders, revenue, AOV |
| `get_sales_by_supplier(from_date, to_date, date_field, top_n, rank_by)` | Aggregates revenue, units, orders, SKU count by supplier |
| `get_component_sales(from_date, to_date, date_field, top_n, sku)` | **Units only.** Explodes composite order lines (bundles, custom completes, multipacks, option/linking SKUs) to component (child) level so hidden component demand becomes measurable. Each row splits `composite_units` vs `standalone_units` + `from_composite` flag. Optional `sku` restricts to the children of one composite parent. Relies on `CompositeSubItems` (processed-order detail), **not** `CompositeChild` (open orders only). Child `Quantity` is already resolved — never multiply by parent qty. Confirmed against tenant 13 Jun 2026 |

### Purchase orders

| Tool | Notes |
|---|---|
| `search_purchase_orders(status, from_date, to_date, ...)` | `PurchaseOrder/Search_PurchaseOrders2`; payload **unwrapped** — wrapping silently ignores all filters |
| `get_purchase_order(purchase_id)` | Returns header + items (with `outstanding` = qty − delivered) + delivery records; deleted items filtered |
| `get_suppliers()` | `Inventory/GetSuppliers` GET — not in public OpenAPI specs but confirmed working |
| `create_purchase_order(..., dry_run=True)` | `DateOfPurchase` always required (SQL rejects null); `SupplierReferenceNumber` always required (send `""` if none). `cost` is the **ex-VAT unit cost** — converted to a tax-inclusive line total on write (issue #15) |
| `update_purchase_order_header(..., dry_run=True)` | Carry all existing header fields — missing fields are cleared; blocks on DELIVERED status |
| `add_purchase_order_item(purchase_id, sku, quantity, cost, tax_rate, dry_run=True)` | Add a new line to a PENDING/OPEN/PARTIAL PO; resolves SKU → GUID automatically |
| `update_purchase_order_item(purchase_id, purchase_item_id, quantity, cost, tax_rate, dry_run=True)` | Edit a line item; only fields you provide change; shows before/after diff; use `get_purchase_order` to find `purchase_item_id` |
| `remove_purchase_order_item(purchase_id, purchase_item_id, dry_run=True)` | Delete a line item; confirms what will be removed; read-back after write |
| `open_purchase_order(purchase_id, dry_run=True)` | PENDING → OPEN only |
| `deliver_purchase_order(...)` | Marks items as delivered |
| `add_purchase_order_note(...)` | Use for carrier tracking numbers — the "Add delivery" dialog in the UI has no public API equivalent |
| `delete_purchase_order(purchase_ids, confirmed_count, dry_run=True)` | **IRREVERSIBLE — deletes whole POs** (header + all lines + notes); the whole-PO counterpart to `remove_purchase_order_item`. Bulk-capable list of `pkPurchaseID`; read-before-write manifest (status, supplier, line_count, total_cost, `delivered` flag), staging **threshold 10**, per-PO delete + read-back (a deleted PO makes `Get_PurchaseOrder` 400 "does not exist" → `deleted:true`). DELIVERED/PARTIAL POs are flagged with a `warning` in the manifest (deleting loses received-stock history) but NOT auto-blocked. Unresolvable id → error row, never aborts the batch. `PurchaseOrder/Delete_PurchaseOrder`. **Fully live-tested 24 Jul 2026** (create throwaway PENDING PO → dry-run → delete → confirm-gone → bogus-id error row) |

### Rules Engine

| Tool | Notes |
|---|---|
| `get_rules()` | Flat list of all rules with pkRuleId, RuleName, Enabled, RunOrder |
| `get_rule(rule_id)` | Full IF/THEN/subrule tree for one rule |

Rule types seen in practice: `Orders` (fires on incoming orders), `Test` (sandbox). Action types include: `AssignShippingService`, `AssignToFolder`, `AssignTagToOrder`, `AssignOrderExtendedProperty`, `ChangeOrderLockStatus`, `ChangeOrderParkStatus`, `BlockOrderFromMerging`, `ExecuteMacro`.

### Import / Export

| Tool | Notes |
|---|---|
| `get_import_list()` | Use this for status triage — `ImportStatus` on the detail endpoint is unreliable |
| `get_export_list()` | Same shape as import list |
| `get_import(import_id)` | Config inspection only (feed URL, column mappings, schedule) — `ImportStatus` returns `null` even for erroring imports |
| `get_export(export_id)` | Config inspection for exports |
| `run_import(import_id, dry_run=True)` | Queue an import for immediate execution; read-before-run shows config preview; refuses if already executing/queued; spec-based, not yet live-tested |
| `run_export(export_id, dry_run=True)` | Queue an export for immediate execution; same pattern as `run_import`; spec-based, not yet live-tested |

### Inventory writes (v1.11.0 — **live-tested 15 Jun 2026** against an isolated test SKU)

> **Write-endpoint gotchas confirmed live 15 Jun 2026** (all now handled in code):
> - **`AddInventoryItem` needs a client-generated `StockItemId` GUID** — omitting it returns HTTP 400 "StockItem StockItemId could not be empty". The tool generates one with `uuid.uuid4()`.
> - **`Create*` sub-entity endpoints (prices, descriptions, extended properties) need a client-generated `pkRowId` GUID** — omitting it collides on the table PK (HTTP 400 "Violation of PRIMARY KEY ... duplicate key (00000000-...)"). The tools generate one per create row.
> - **The default price row (empty Source+SubSource) exists implicitly as the zero-GUID row but is NOT returned by `GetInventoryItemPrices`** — so a default-price write must UPDATE pkRowId `00000000-...`, not create. `set_inventory_item_prices` special-cases this; genuine channel rows are created with a fresh `pkRowId`.
> - **Most write endpoints return a 2xx with an EMPTY body on success** (AddInventoryItem, UpdateInventoryItem, the Create/Update sub-entity calls, UpdateStockLevelsBulk). `call_linnworks` now treats an empty 2xx body as `{}` instead of raising a JSON-decode error. `UpdateStockLevelsBulk` does not echo `Items`, so `set_stock_levels` infers per-item success from the 2xx and recommends a `get_stock_level` read-back.
> - **`Create`/`UpdateInventoryItemExtendedProperties` return a 2xx with a NON-empty, NON-JSON body on success** — `response.json()` rejects it with "Expecting value: line 1 column 1 (char 0)", which the empty-body guard alone doesn't catch. `call_linnworks` now wraps `response.json()` in a `try/except ValueError` and falls back to `{"raw": text}` on any 2xx, so a successful write is never mis-reported as a failure. This fixed `set_extended_properties` live writes, which previously failed on every `dry_run=False` call (issue #13, 15 Jun 2026).

All write tools use `_write_guard` (staged-manifest gate) and `_check_injection` (injection tripwire). All default to `dry_run=True`. See the **Write-safety framework** section above for the protection design.

The helper `_resolve_sku_to_id(sku, cache)` is shared across all write tools — it resolves SKU → StockItemId GUID via `Inventory/GetInventoryItem`. Results are cached within a single tool call to avoid redundant API calls for the same SKU.

| Tool | Endpoint(s) | Threshold | Notes |
|---|---|---|---|
| `create_or_update_inventory_item(items, confirmed_count, dry_run=True)` | `Inventory/AddInventoryItem` / `Inventory/UpdateInventoryItem` | 50 | Upsert by SKU — probes existence with GetInventoryItem, then creates or updates. On update, reads existing fields and merges so unsupplied fields are preserved. Returns per-item action (created/updated/error). Fields: sku, title, barcode, retail_price, purchase_price, tax_rate, category_name, weight, height, width, depth, metadata. |
| `set_stock_levels(updates, confirmed_count, dry_run=True)` | `Stock/UpdateStockLevelsBulk` | 25 | Absolute stock level overwrite. Read-before-write captures current levels for diff. Per-item `Errors[]` in response reported individually. Fields: sku, stock_level, location_id. |
| `set_inventory_item_prices(prices, confirmed_count, dry_run=True)` | `Inventory/CreateInventoryItemPrices` / `Inventory/UpdateInventoryItemPrices` | 25 | Price upsert keyed by (StockItemId, Source, SubSource). Reads existing price rows first; creates new rows or updates existing by pkRowId. Fields: sku, price, source, sub_source. |
| `set_extended_properties(properties, confirmed_count, dry_run=True)` | `Inventory/CreateInventoryItemExtendedProperties` / `Inventory/UpdateInventoryItemExtendedProperties` | 50 | Property upsert keyed by ProperyName (deliberate API typo). Reads existing props first; creates or updates. `_check_injection` on property_name and property_value. Fields: sku, property_name, property_value, property_type. |
| `set_inventory_item_descriptions(descriptions, confirmed_count, dry_run=True)` | `Inventory/CreateInventoryItemDescriptions` / `Inventory/UpdateInventoryItemDescriptions` | 50 | Description upsert keyed by (StockItemId, Source, SubSource). `_check_injection` on description. Fields: sku, description, source, sub_source. |
| `set_inventory_item_titles(titles, confirmed_count, dry_run=True)` | `Inventory/CreateInventoryItemTitles` / `Inventory/UpdateInventoryItemTitles` | 50 | Channel-title (override) upsert keyed by (StockItemId, Source, SubSource). Mirrors `set_inventory_item_descriptions` exactly. Writes the per-channel title override only — NOT the base `ItemTitle` (use `create_or_update_inventory_item` for that). `_check_injection` on title. Fields: sku, title, source, sub_source. **Live-tested 23 Jun 2026** (create + update verified by read-back on an isolated test SKU). |
| `set_inventory_item_suppliers(links, confirmed_count, dry_run=True)` | `Inventory/CreateStockSupplierStat` / `Inventory/UpdateStockSupplierStat` (both 204 void) | 50 | Supplier-link upsert keyed by **(StockItemId, SupplierID)** — attach/update "buy this item from supplier X at code Y, cost Z". Supplier matched case-insensitively by name against `get_suppliers` (GUID accepted; unknown → per-item error row). **Update carries the FULL existing row** (Linnworks clears omitted fields — live-proven) and overlays only the passed fields; **a new `is_default=True` row auto-flips the previous default server-side** (live-proven). Read-before-write manifest + read-back after. Fields: sku, supplier, supplier_code, cost, is_default, lead_time, min_order_qty, pack_size, barcode, currency. **Fully live-tested 14 Jul 2026** (issue #25). |
| `add_inventory_item_images(images, confirmed_count, dry_run=True)` | `Inventory/AddImageToInventoryItem` | 100 | Additive only — existing images not removed. One API call per image (no bulk endpoint). `_check_injection` on image_url. Fields: sku, image_url, is_main. |
| `delete_inventory_item_images(sku, image_ids, confirmed_count, dry_run=True)` | `Inventory/GetInventoryItemImages` (read) → `Inventory/DeleteInventoryItemImageBulk` (write) → read-back | 10 | **IRREVERSIBLE.** Removes images from ONE item by **image id** (the `image_id` from `get_inventory_item_images`) — the corrective counterpart to `add_inventory_item_images`. Uses `DeleteInventoryItemImageBulk` (id-keyed, 200+`BatchedAPIResponse`), **not** `DeleteImagesFromInventoryItem` (url-keyed, 204). Read-before-write manifest shows which image each id is (url + main/sort flags); an id not on the item → `unresolved`, skipped, never blocks the valid ids. Read-back splits `deleted` / `still_present` (a 2xx alone doesn't prove removal). **Fully live-tested 17 Jul 2026** (issue #28). |
| `set_inventory_item_image_order(sku, image_ids, main_image_id, confirmed_count, dry_run=True)` | `Inventory/GetInventoryItemImages` (read) → `Inventory/SetInventoryItemImageAsMain` + `Inventory/UpdateImages` (write, both 204) → read-back | 25 | Reorders an item's images and/or sets the main/hero image. **⚠️ The main image is PINNED to position 0 by Linnworks — final order is always `[main] + [your order minus main]`, so reordering can NEVER put a non-main image first; pass `main_image_id` to change the hero.** `image_ids` may be partial (names lead, unnamed keep relative order behind them) and only controls the order BEHIND the main. The tool predicts the server's override into the manifest and flags `main_forced_first` + `warning` instead of promising an order that won't land; read-back returns `order_matches_plan`. Sets main first, then submits the **full set main-first** (the only shape honoured verbatim — partial payloads get re-normalised). Carries the full row (UpdateImages clears omitted fields). Does NOT push to a live listing — follow with `refresh_channel_listing` and verify the storefront. **Fully live-tested 17 Jul 2026** (issue #28). |
| `create_variation_group(group_name, parent_sku, child_skus, dry_run=True)` | `Stock/CheckVariationParentSKUExists` + `Stock/SearchVariationGroups` + `Stock/CreateVariationGroup` | — (single op) | **⚠️ `parent_sku` must be a BRAND-NEW variation SKU** (not an existing item — Linnworks mints it). Pre-checks the parent is free via `CheckVariationParentSKUExists`; child SKUs must already exist. Existing-group name guard via `_find_variation_group_by_name` (SearchVariationGroups — `GetVariationGroupByName` is unreliable). Passes the zero-GUID `ParentStockItemId`; read-back confirms members. `_check_injection` on group_name. **Fixed + first-ever live-tested 28 Jul 2026 (issue #29).** |
| `add_variation_group_items(child_skus, parent_sku=None, group_name=None, dry_run=True)` | `Stock/GetVariationGroupByParentId` / `Stock/SearchVariationGroups` (identify) + `Stock/GetVariationItems` (read-before-write) + `Stock/AddVariationItems` (write) + read-back | — (single op, one group) | **Add child SKUs to an EXISTING variation group** — the write path `create_variation_group` (create-only) lacks. Identify by `parent_sku` OR `group_name` (parent's StockItemId == group's `pkVariationItemId`, #17). Idempotent: already-linked SKUs → `already_present` (skipped); unresolvable child → `child_errors` (never aborts). Read-back returns `added: true/false` per SKU. Built for the supplier-feed case where a new size/colour is created but stays orphaned from the live group. **Fully live-tested 28 Jul 2026 (issue #29).** |
| `delete_inventory_item(skus, confirmed_count, dry_run=True)` | `Inventory/DeleteInventoryItems` | 10 | **IRREVERSIBLE.** Read-before-write resolves each SKU → StockItemId and captures title + total stock into the manifest; unresolvable SKUs become error rows (don't abort the batch). Lowest staging threshold of the suite (10). Single delete call with the resolved GUID list; per-SKU read-back via GetInventoryItem confirms each is gone (`deleted: true/false`). Any Linnworks delete error surfaced verbatim in `delete_error`. **Live-tested 15 Jun 2026** — create → dry-run → delete → confirm-gone cycle verified against a throwaway `ZZZ-MCP-TEST-*` SKU (closes #12). |

---

### Inventory categories (v1.24.0 — **fully live-tested 7 Jul 2026**)

Category CRUD + empty-cleanup. Categories are a flat list keyed by `CategoryId` (GUID); the zero-GUID **Default** category is built in and is hard-protected from rename/delete. **Emptiness has no dedicated endpoint** — `_count_items_per_category()` sweeps the whole catalogue via `Stock/GetStockItems` (`keyWord=""`, ~161 pages / ~2 min for 32k items), **throttling to <150/min with a 429 backoff**, and always runs to completion (a partial scan could misreport a used category as empty → wrongful delete). Both delete tools default `dry_run=True`, stage at **threshold 10**, and read back after deleting.

> **⚠️ "Empty" = zero ACTIVE items (v1.24.1).** The sweep endpoint `Stock/GetStockItems` returns active stock only — **archived items are invisible to it**, and no Linnworks endpoint counts archived items per category (`GetStockItemsFull` is active-only too; `GetInventoryItemsCount` has no category filter; no `GetArchivedItems` exists). This tenant: 32,125 active vs 92,816 incl. archived (~60k archived). So a category of only archived stock reads as empty but `DeleteCategoryById` blocks it with **HTTP 400 "Category is in use"** — the authoritative emptiness check. Both delete tools now classify that 400 as **`skipped_in_use`** (expected, not a `delete_error`) and return a `skipped_in_use` count. `force=True` does NOT bypass it. Unarchive/reassign the items in the UI to make such a category deletable. Treat `empty_count`/`is_empty` as an **upper bound**, not "deletable".

| Tool | Endpoint(s) | Threshold | Notes |
|---|---|---|---|
| `get_categories(with_counts=False)` | `Inventory/GetCategories` (+ sweep when `with_counts`) | — (read) | Lists all categories (one fast GET) → `category_id`, `category_name`, `product_category_id`, `is_default`. `with_counts=True` adds `item_count` + `is_empty` per category and top-level `empty_count`/`used_count`/`total_items` — but triggers the full ~2 min catalogue sweep. |
| `create_category(name, dry_run=True)` | `Inventory/CreateCategory` | — (single op) | Creates a category (Linnworks mints the id). Case-insensitive duplicate-name guard. `_check_injection` on name. |
| `rename_category(category_id, new_name, dry_run=True)` | `Inventory/GetCategories` + `Inventory/UpdateCategory` | — (single op) | Renames via read-before-write, carrying the full record through (UpdateCategory clears omitted fields). Refuses Default + duplicate names; read-back confirms. `_check_injection` on new_name. |
| `delete_categories(category_ids, force=False, confirmed_count, dry_run=True)` | `Inventory/DeleteCategoryById` (+ sweep unless `force`) | 10 | **IRREVERSIBLE.** Deletes specific ids. **Refuses non-empty categories** (marks them `blocked`) unless `force=True` — which skips the sweep and deletes regardless (their items → Default). Default id always blocked; unknown ids → `error`. Per-id manifest (name, item_count, status). **A category empty of ACTIVE items but holding archived items is blocked by Linnworks ("Category is in use") → reported `skipped_in_use`, not deleted (v1.24.1); `force` does NOT override this.** Returns `deleted` + `skipped_in_use` counts. |
| `delete_empty_categories(confirmed_count, dry_run=True)` | `Stock/GetStockItems` (sweep) + `Inventory/DeleteCategoryById` | 10 | **IRREVERSIBLE — headline cleanup.** Sweeps the catalogue, finds every zero-**active**-item category (Default excluded), shows the manifest, and bulk-deletes on confirm. Sweep re-runs on the confirmed call to re-verify emptiness. **"Empty" = zero ACTIVE items only; archived items are invisible to the sweep (v1.24.1)** — categories of only archived stock come back `skipped_in_use` on the live run (not deleted). `empty_count` is an **upper bound** on what deletes; response reports `deleted` + `skipped_in_use`. Live sweep found **62 of 181 categories with no active items** (junk like `#REF!`/`AUCTION`/`SHOWS` + archived ranges — but only genuinely-empty ones actually delete). |

---

### Listings — Generic Listing Tool (GLT)

| Tool | Endpoint(s) | Threshold | Notes |
|---|---|---|---|
| `list_to_shopify(skus, configurator, default_configurator, sub_source, confirmed_count, dry_run=True)` | `GenericListings/GetConfiguratorsInfoPaged` (read) → `GenericListings/CreateTemplates` + `GenericListings/ProcessTemplates` (write) | 25 | Lists **existing** inventory to Shopify via a saved configurator — the API form of "select items → apply configurator → create listings". Per-item configurator chosen by: `configurator` override → item's `Shopify Configurator` extended property → `default_configurator` fallback; the chosen name is validated against the live catalogue (typo/blank → per-item `unresolved` row, never fatal). Items grouped by resolved configurator; each group = one CreateTemplates (returns `AllCreatedIds`) + one ProcessTemplates (`Action:"Create"`). **Read/selection path live-confirmed 18 Jun 2026; write path spec-based, not yet live-run** (creates real Shopify listings — start with one SKU). Channel = `ChannelType="Shopify"` + `ChannelName="SHOPIFY"` (the Source, **not** a SubSource); store derived from the configurator's `ChannelId`/`SubSource`. Pass `sub_source` to disambiguate the 12 configurator names shared across stores. **Dedupe (issue #18):** before listing, every resolved item is checked against the channel-SKU link table; any already live on its target store's Shopify is moved into an `already_listed` output and **excluded from `CreateTemplates`** (dry + live), so a bulk run can't duplicate a customer-facing listing. Lookup failure → dedupe skipped + `dedupe_warning`. |
| `refresh_channel_listing(skus, sub_source="SWH Shopify", channel="Shopify", action=None, check_staleness=True, confirmed_count, dry_run=True)` | `GenericListings/GetConfiguratorsInfoPaged` + `Inventory/GetInventoryItemChannelSKUs` + `GenericListings/OpenTemplatesByInventory` (read) → `GenericListings/ProcessTemplates` (write) | 25 | **Revise counterpart to `list_to_shopify` (issue #20)** — re-pushes EXISTING listings so edited item data (extended properties, title, price, description) reaches the live channel. **Generalised beyond Shopify in v1.47.0 (issue #42), reusing the `GLT_CHANNELS` registry and `_resolve_glt_target` from `unpublish_channel_listing`** — `channel` selects "Shopify" (default), "Amazon", "TikTok" (also Magento/Walmart where GLT-managed); a non-GLT channel (eBay/Etsy/Mirakl/CDiscount) raises naming the supported channels. **Never creates** — a SKU not already live on `sub_source` → `unresolved` (use `list_to_shopify`). Per SKU: resolve → confirm the channel's Source+`sub_source` mapping (channel-SKU table) → `OpenTemplatesByInventory` opens the item's **existing** GLT template(s) (a safe read; returns top-level `Id`=TemplateId, `IsLocked`, `IsAllowedToRevise`, `NextSuggestedAction`, nested `Info.ActiveListingId`/`Status`) → `ProcessTemplates` with the revise action, per template. **Plans one row PER TEMPLATE (v1.47.0)** — an item can have several templates on one channel (live: 2 on Amazon, merchant + `.FBA`), and reviving only one would leave the other on its stale snapshot while reporting a clean refresh; `templates_on_item` on each row. **Action is data-driven:** `action=None` (auto) uses the template's `NextSuggestedAction` when allowed (typically "Update" for Shopify), else `"Revise"` (Amazon templates have been observed reporting `NextSuggestedAction:"NotAllowed"` while `IsAllowedToRevise` still permits a forced "Revise" — the fall-through is exercised in the Amazon test fixtures); force with `action="Revise"/"Update"/…`. Locked / no-allowed-action templates → `unresolved`. **Variation-group aware (v1.26.0, issue #26):** GLT variation listings hang off the variation PARENT (parent = template, no channel-SKU row; children = channel-SKU rows, no templates), so a template-less CHILD falls back to its parent's template (`via_variation_parent`), a PARENT counts as listed when any child is mapped (`listed_via_children`), and the plan **dedupes by template** (`covers_skus`) — 6 variants of one group = ONE push. This is how Shopify `compare_at_price` (mapped from `special_price`) is pushed for variant listings. One Amazon variation family has been observed live (bushings, issue #45, 25 Aug 2026): every child SKU resolved its own template directly, TikTok's shape rather than Shopify's — that is one observation, not a rule, and other families may still hang off the parent; the fallback runs unchanged regardless of channel and could open the wrong template, or none, on an unobserved Amazon variation SKU. **PRE-FLIGHT STALENESS (v1.43.0, issue #40; channel-scoped in v1.47.0):** `check_staleness=True` (default) compares each template's STORED snapshot against the item's CURRENT values and reports `staleness` per plan row (`template_last_modified`, `snapshot_age_days`, `compared{title,price,image_count}`, `stale_fields[]`, `comparable_fields_match`, `skipped_comparisons[]`, `undetectable_fields[]`, `warning`), plus top-level `stale_plan_count` / `no_detectable_change_count` / `staleness_unchecked_count`. **PARTIAL BY CONSTRUCTION** — the template exposes values only for Title/Price/LastModificationTime; Images/Attributes/MetaFields/Description are counts or "Filled", so **`comparable_fields_match: true` means "no difference I can SEE", NOT "this push will change something"** (the #40 no-op matched on all three). Title/price are compared against the **effective override row for THIS channel** (override → base) — hard-coded to Shopify before v1.47.0, which would have compared an Amazon override against the base item title. Price is **skipped on variation templates** (snapshot reports 0.0), and a failed read gives `checked: false` / `None`, never "matches". There is deliberately **no `will_change`** and **no post-push read-back**: `LastUpdateTime` advances on every push including the proven no-op, and `NextSuggestedAction` is a constant ("Update"/allowed on 24/24 listed Shopify templates), so nothing the API returns can confirm the channel changed — the live-run message says so and points at reading listing data back (not the detail page; Amazon's can lag the catalogue by up to a day). Costs 3 GETs/template. Response carries `target_channel`/`target_source`/`sub_source_resolution`/`revise_proven`, derived from `GLT_CHANNELS` via `_proven_revise_channels()` so an unproven-channel warning can't go stale. **Read/selection path live-confirmed 18 Jun 2026 + variation fallback dry-run-confirmed 14 Jul 2026 + staleness live-verified 19 Aug 2026 (9/9 real Venom templates genuinely stale, two storing £137.95 vs a current £139.95). ⚠️ `ProcessTemplates Update` LIVE-RUN 14 Jul 2026 (v1.27.1) on SHOPIFY: it pushes the template's STORED (possibly stale) field snapshot, NOT current item data — a stale template overwrote the live selling price and the listing had to be repaired via the Shopify admin. Only live-run templates known to be fresh; read the live listing back after every push.** ⚠️ **`revise_proven: False` ON AMAZON AND TIKTOK, but the two are NOT the same state (issue #45, see the v1.47.1 entry above for the full write-up):** Amazon's Revise/Update has been FIRED LIVE TWICE (24-25 Aug 2026, templates 32064 + 32239) and PRODUCED NO OBSERVABLE CHANGE on either listing — tried and shown ineffective, not merely untried (`GLT_CHANNELS["amazon"]["revise_attempted"] is True`). TikTok's has never been attempted live at all (`revise_attempted` is `False`). Both facts are on the registry and surfaced on every plan row and warning message, worded differently per state. `sub_source`→`ChannelId` from the configurator catalogue, with Amazon's regional sub-sources resolving via the account-prefix rule (`sub_source_resolution: "account-prefix"`). |
| `unpublish_channel_listing(skus, sub_source="SWH Shopify", channel="Shopify", allow_variation_parent_takedown=True, also_retiring_skus=None, confirmed_count, dry_run=True)` | `GenericListings/GetConfiguratorsInfoPaged` + `Inventory/GetInventoryItemChannelSKUs` + `GenericListings/OpenTemplatesByInventory` (read) → `GenericListings/ProcessTemplates` (write) → `Inventory/GetInventoryItemChannelSKUs` (read-back) | 10 | **Destructive take-down counterpart to `list_to_shopify`/`refresh_channel_listing` (issue #22; generalised beyond Shopify in v1.32.0 / issue #30)**. **`channel`** selects any GLT channel — Shopify, Amazon and TikTok (all three **live-proven**), Magento/Walmart (unproven, and 0 configurators here); a non-GLT channel (eBay/Etsy/Mirakl/CDiscount) raises. Plan/results carry `delete_proven`, and an unproven channel is warned about in every message. **Plans one row PER TEMPLATE** — an item can have several templates on one channel (live: 2 on Amazon, merchant + `.FBA`), and deleting only one leaves the other listing live. `sub_source` accepts an Amazon **regional** sub-source ("The Warehouse Group - Germany"), which resolves to the account's ChannelId (`sub_source_resolution: "account-prefix"`). Read-back is done **once per item** after all its templates are processed — ENDS an existing Shopify listing so it stops selling (the GLT `Action="Delete"`). Built to retire an orphaned/duplicate listing left live after a SKU-scheme migration. Same selection path as `refresh_channel_listing` (resolve → confirm SHOPIFY+`sub_source` channel-SKU mapping → `OpenTemplatesByInventory` opens the **existing** template → skip locked) but **forces `Action="Delete"`** (no auto-action). Read-before-write captures each listing's `channel_reference_id` + `listed_quantity` into the plan so you can confirm you're retiring the orphan, NOT the good listing (reviews/ranking). Read-back-after uses **TWO surfaces (v1.38.0, issue #36): each TEMPLATE is re-opened by id to confirm it is actually gone, AND the channel-SKU table is re-read per item** — a per-item check alone reported success for templates that were never deleted on multi-template items. Each row carries `template_deleted`, `template_status_after`, `still_listed` and an `outcome` (`taken_down` / `listing_gone_template_orphaned` / `delete_failed` / `template_deleted_listing_row_remains` / `unconfirmed`); `Status:"Not deleted"` is treated as a failure, and an unverifiable read-back yields `taken_down: None`, never True (channel sync may lag). **VARIATION-AWARE (v1.41.0, issue #35):** a template-less CHILD is retired via its parent's template **only when no other group member would lose a listing** — every other member already un-listed on this store, or itself in `skus`/`also_retiring_skus`; otherwise it is blocked as `variation_child_live_siblings` naming the parent and the live siblings. Liveness = has a channel-SKU row on THIS store (not "has stock"). A PARENT passed directly is accepted (`listed_via_children`) with a warning naming every child listing it ends. Plan rows split `template_stock_item_id` (what to re-open) from `listing_sids` (whose rows must vanish) — **a parent has no channel-SKU rows, so reading the parent would score a false `taken_down`**. Deduped by template: 11 children → ONE delete with `covers_skus`. Every `unresolved` row carries a **`blocked_reason`**, and `blocked_summary`/`blocked_count`/`retirable_sku_count` + the message make "2 of 42 retirable" impossible to read as success. **Tightest threshold 10** (shared with `delete_inventory_item`). Not listed on `sub_source` / bogus / locked → `unresolved`. **Read/selection path live-confirmed 7 Jul 2026; `ProcessTemplates` Delete LIVE-PROVEN on Shopify 8 Jul 2026** (`luma-harlyn-frosted` → Icarus tpl 20894 + TWG B2B tpl 41159 taken down) **and on Amazon 5 Aug 2026** (`MOB-GRP-3080` tpl 31703, ASIN B0B311TFG3 — verified against the channel-SKU table, a 0-template re-open, and untouched stock/other channels) **and on TikTok 7 Aug 2026** (v1.42.0 — `ven-20-black-raw-core-complete-7.5` tpl 30006, same three surfaces; **on TikTok the variation CHILD holds its own template**, the reverse of Shopify, so a child usually needs no whole-group gate). Still destructive to a real customer-facing listing — start with one orphan SKU. |
| `delist_all_channel_listings(skus, channels=None, allow_variation_parent_takedown=True, confirmed_count, dry_run=True)` | per sku: `Inventory/GetInventoryItem` + `Inventory/GetInventoryItemChannelSKUs` (discover) + `GenericListings/GetConfiguratorsInfoPaged` per channel → delegates to `unpublish_channel_listing` → `Inventory/GetInventoryItemChannelSKUs` (cross-region read-back) | 10 | **Take down EVERY GLT-manageable listing for each item, across ALL channels and stores/accounts (v1.32.0, issue #30)** — the general fan-out; `channels=None` (default) = every GLT channel with configurators in this tenant. **Shopify, Amazon AND TikTok deletes are all live-proven** (TikTok as of v1.42.0); Magento/Walmart are not, and have 0 configurators here anyway. **Amazon's regional sub-sources collapse into ONE take-down per (item, account)** (`covers_sub_sources`) since they share one template set. Non-GLT listings (eBay/Etsy/Mirakl/CDiscount) and channels with no configurators go to `skipped_channels` **with a reason** and are LEFT UP — "delisted everywhere" is never literally true. Live run adds **`still_listed_sub_sources`**, an explicit per-(item, channel) re-read across every covered sub-source. **Delegation is batched PER TARGET (channel × account) since v1.41.0** — one SKU at a time meant a whole variation group blocked itself (every sibling looked like a live listing nobody asked to end) and, live, the first child's delete succeeded while the rest reported "no template"; the batch also passes `also_retiring_skus` so a group split across targets still counts. Surfaces `blocked_summary` / `blocked_count` / `retirable_sku_count` — on real dead stock most SKUs are dead VARIANTS of live products and are **not** retirable here, so never read the take-down count as a completion rate. Built for the dead-product cleanup chain: unarchive → delist → wait for channel sync → `archive_inventory_items` |
| `repair_channel_listing_images(skus, sub_source="SWH Shopify", remove_superseded=True, set_featured=True, allow_net_media_loss=False, confirmed_count, dry_run=True)` | `Inventory/GetInventoryItemChannelSKUs` + `Inventory/GetInventoryItemImages` (Linnworks) → **Shopify Admin GraphQL**: `productUpdate(media:)` · `productReorderMedia` · `fileUpdate(referencesToRemove:)` | 10 | **Gets corrected Linnworks images onto an EXISTING Shopify listing (issue #41) — the one image job the GLT cannot do.** `refresh_channel_listing` re-pushes the template's STORED snapshot, so it re-sends the old (sometimes deleted) URL and no-ops silently (#40); re-listing is not an option on a live product. This writes to Shopify DIRECTLY, so it needs Admin credentials (`SHOPIFY_STORES`, or `SHOPIFY_SHOP_DOMAIN`+`SHOPIFY_ADMIN_ACCESS_TOKEN`; scopes read/write_products + read/write_files) — **with none configured it writes nothing and returns setup instructions**. Per SKU: read Linnworks images → find the product via the channel-SKU `product:variant:inventory` reference → diff → attach missing → wait for READY → set featured → detach superseded. **Images match on the Linnworks GUID that Shopify preserves in the CDN filename**, so it compares PICTURES, not counts (#40's blind spot). **Never removes `unmanaged` media** (non-GUID filename = hand-uploaded), and **blocks removal that would take away MORE media than it adds** (v1.45.0 — the signature of a storefront richer than the catalogue; live-caught on `luma-huxham-blue`, whose 6 "superseded" media were lifestyle shots against 4 incoming packshots). Override with `allow_net_media_loss=True`. **⚠️ Variation-safe: one Shopify product is shared by every variant while each keeps its own Linnworks images**, so the whole group's images are read first, a sibling's photo is `matched` not superseded, and removal is disabled on a shared product unless every member is passed. **Nothing is detached until its replacement is READY** — detaching first is how a listing ends up with no image. Read-back re-reads the product from Shopify (`in_sync` / `still_missing` / `featured_is_correct`). **Live-verified 19 Aug 2026 — read path AND write path.** The read found real drift on `RS-102201` (2 of 4 images missing, wrong featured image) and the repair was then FIRED live on that product: 2 images attached, `7385b433` made featured, verified 4/4 matched. Variation gate proven on the 7-member catnip group. |
| `delist_all_shopify_listings(skus, allow_variation_parent_takedown=True, confirmed_count, dry_run=True)` | delegates to `delist_all_channel_listings(channels=["Shopify"])` | 10 | **Shopify-scoped slice of the above (v1.25.0; re-based in v1.32.0)** — unchanged behaviour, kept for back-compat and for when you deliberately want Shopify only. Take down EVERY Shopify listing for each item, across ALL stores it's on — the convenience wrapper over `unpublish_channel_listing` (one store at a time). Per SKU: resolve → read channel-SKU table → find every distinct SHOPIFY store → delegate each (sku,store) to the proven single-store tool. **Non-Shopify listings (Mirakl/eBay/Amazon/TikTok/Magento) are reported under `skipped_channels` and LEFT UP** — use `delist_all_channel_listings` to also retire the GLT channels (Amazon live-proven 5 Aug 2026; TikTok live-proven 7 Aug 2026). Built for archived-item cleanup: **unarchive in the UI → this → `archive_inventory_items`**. Items must be ACTIVE (archived SKUs can't resolve → `unresolved` with "unarchive first"). Dry-run tested 8 Jul 2026 (active `luma-harlyn-BlackBlack` → 3 stores planned + Mirakl skipped). Threshold 10 on (sku×store) take-downs. |

#### eBay listing description revise (issue #43) — NOT a GLT channel

eBay has no GLT template (`GetConfiguratorsInfoPaged` still 400s "Invalid parameter request" for it — unchanged), so `refresh_channel_listing`/`unpublish_channel_listing` correctly refuse it. But Linnworks exposes a SEPARATE, dedicated `Listings/` API family for eBay (live-probed 25 Aug 2026 — see the confirmed-endpoints table and the v1.48.0 note above), which this tool uses instead.

| Tool | Endpoint(s) | Threshold | Notes |
|---|---|---|---|
| `revise_ebay_listing_description(skus, store="EBAY0", confirmed_count, dry_run=True)` | `Inventory/GetInventoryItemChannelSKUs` (bulk) + `Inventory/GetInventoryItemDescriptions` (read) → `Listings/GeteBayConfigurators` + `Listings/GeteBayTemplates` (locate, every unblocked plan row — dry run included) → `Listings/ProcesseBayListings` (write) → `Listings/GeteBayTemplates` (config-scoped read-back, live run only) | 10 | **The first eBay write in this server (issue #43).** Per SKU: resolve → keep only channel-SKU rows with Source `EBAY` and SubSource matching `store` case-insensitively (`not_listed` otherwise) → group matched SKUs by `ChannelReferenceId` (the eBay item id) — **one plan row per listing**, deduping a multi-variation listing's many covered SKUs down to a single push instead of one per SKU. Each row's description is the item's own EBAY-channel description row for `store` (falling back to the default/blank row via `_effective_channel_value`, "the same rule used elsewhere"), plus an eBay-specific case-insensitive **prefix** fallback for the `"EBAY0"` (channel-SKU table) vs `"EBAY0_UK"` (descriptions table) sub-source mismatch (live-confirmed, same tenant/SKU). No usable description on any covered SKU → the row is `blocked` with a named reason, never pushed empty. **Every plan row carries `never_synced_skus`** — covered SKUs whose channel-SKU row's `LastUpdate` is the null date 0001-01-01 (or missing), i.e. never confirmed by the channel (the v1.42.1 TikTok mistake, repeated here on purpose to catch it) — with a `never_synced_warning` when non-empty; these SKUs are still planned, not force-blocked. **Stale-snapshot preview (QA round 1 fix):** for every UNBLOCKED row, the template is resolved during planning — including on a dry run, not only right before a live push — by sweeping `GeteBayConfigurators` + `GeteBayTemplates` (no direct by-SKU/listing-id lookup exists on this API — see the version note); the row gets `template_found` and a `staleness` block (`stored_title`, `stored_price`, `title_stale` vs the item's own current title, `warning`) so a human confirming a batch can see what the live push would actually send, not just the description. The found template is cached per listing id so a live run right after does not re-sweep. **Live run:** pushes the FULL cached template object with ONLY `Description` replaced, preserving Title/Attributes/Categories/Price/etc. and — per the live evidence in the version note — the seller's design wrapper; then re-checks the STORED template afterwards (config-scoped, cheap) and reports `post_push_description_matches` per result row — a read-back of Linnworks' own record, not proof the live eBay listing changed. **Also from that same re-read (QA round 2 fix): `post_push_status`/`post_push_error_message` are carried through verbatim onto every result row**, since eBay's own per-listing refusal (active bids, a recent sale, category restrictions) comes back as a stored condition on the template, not a call failure — the tool surfaces these but does not interpret or act on them. Every live-run row reports outcome `unconfirmed` (never `success`) — a 204 means Linnworks accepted the push, not that the listing changed. `rate_limited` is a separate bucket from `not_listed`/`unresolved` (a 429 is never mislabelled as either), with `complete: false` when non-empty. **(QA round 3 fix) A 429 while LOCATING the template — this tool's single heaviest quota consumer, since no by-listing-id lookup exists on this API — is also never folded into "template not found":** it buckets into `rate_limited` and blocks the row with reason `rate_limited_locating_template` (live-run outcome `rate_limited`, not `blocked`), and the same applies to the post-push read-back sweep (`post_push_read_back_rate_limited`). A row whose template genuinely can't be located (no quota issue) is now also `blocked` at plan level, so a dry run's "would be revised" count never promises a push the live run would then refuse. **`EBAY_CHANNELS` registry holds `revise_proven: False`**, surfaced on every response's `verification_note` **and (QA round 2 fix) on every plan row too** (`row["revise_proven"]`, matching `refresh_channel_listing`'s precedent of surfacing it at both levels), naming the description-frame verification URL (`https://vi.vipr.ebaydesc.com/itmdesc/<item_id>`) and warning the item page will not show a fresh push. **⚠️ FIRED LIVE 26 Aug 2026 (issue #47) — ACCEPTED, NOT OBSERVED TO PROCESS.** `Listings/ProcesseBayListings` was pushed live against a real listing (`ven-grip-cleaner`, item `286493672322`): Linnworks accepted it (204), but across a two-hour poll of the description-frame URL no channel-side effect was observed, while the identical edit pushed from the Linnworks listing UI landed within minutes — see the v1.48.1 entry above. `EBAY_CHANNELS["ebay"]` now also carries `push_observed_state`/`push_observed_reason` recording that observation as data (surfaced on the top-level response and on every plan/result row alongside `revise_proven`), so every message DERIVES from the registry rather than a hard-coded string. `revise_proven` stays False on that EVIDENCE, not caution; the route that works today is the Linnworks listing UI after reviewing this tool's dry-run manifest. See the Post-merge verification steps in the v1.48.0 version note for the human proof that would flip `revise_proven` to `True`. |

#### Channel listings (read) — issue #18

| Tool | Endpoint(s) | Notes |
|---|---|---|
| `get_channel_listings(sku)` | `Inventory/GetInventoryItemChannelSKUs` (GET, `?inventoryItemId=<guid>`) | Read-side companion to `list_to_shopify`. Resolves SKU → StockItemId, reads the channel-SKU link table. Returns `is_listed`, `listing_count`, distinct `channels` (Source) + `sub_sources` (store/region), and per-channel `listings` rows (source, sub_source, channel_sku, channel_reference_id, update_status, listed_quantity, max_listed_quantity, last_update, ignore_sync, is_multi_location, channel_sku_row_id). Non-empty = item is listed/linked on that channel. Covers Shopify (per store), eBay, Amazon (per region), Mirakl, Magento. No listing-URL field — `channel_sku`/`channel_reference_id` identify the listing. **Live-tested 18 Jun 2026** |
| `get_channel_listings_bulk(skus, stock_item_ids)` | `Inventory/BatchGetInventoryItemChannelSKUs` (POST, **unwrapped** `{"inventoryItemIds":[...]}`) | Batch dedupe companion — checks many SKUs at once (one batched call per 200 items). Returns `resolved_count`/`listed_count`/`unlisted_count`, per-SKU `results` (same listing shape as `get_channel_listings`), and `unresolved` rows for not-found SKUs. **Pass `stock_item_ids` instead of `skus` for large batches (v1.37.0)** — the batched read is ~1 call/200 but resolving SKUs costs 1 call each against a 150/min quota (5,391 items: 15.6s by id vs 187s by SKU). **`rate_limited` is now a SEPARATE bucket from `unresolved`, and `complete:false` flags a partial answer** — a 429 is never reported as "not found" (issue #34). Use before a bulk list to skip already-live SKUs instead of guessing from barcodes. **Live-tested 18 Jun 2026; rate-limit fix live-verified 6 Aug 2026** |

#### Inventory images (read) — issue #19

| Tool | Endpoint(s) | Notes |
|---|---|---|
| `get_inventory_item_images(sku)` | `Inventory/GetInventoryItemImages` (GET, `?inventoryItemId=<guid>`) | Pre-listing image gate — read-side companion to `add_inventory_item_images`. Resolves SKU → StockItemId, reads the image table. Returns `has_image`, `image_count`, `has_main_image` (spot items with *a* picture but no main set), and per-image `images` rows (image_id, is_main, sort_order, full_url, thumbnail_url) sorted by sort_order. **Live-tested 18 Jun 2026** |
| `get_inventory_item_images_bulk(skus, stock_item_ids)` | `Inventory/GetImagesInBulk` (POST, **WRAPPED** `{"request":{"StockItemIds":[...]}}`) | Batch image gate — checks many SKUs at once (one batched call per 200 items). Resolves SKU → StockItemId first (yields titles + an `unresolved` split, mirroring `get_channel_listings_bulk`), then maps bulk images back by id (the bulk row carries `StockItemId` but **not** `SKU`). Returns `resolved_count`/`with_image_count`/`without_image_count`, per-SKU `results` (same image shape as `get_inventory_item_images`), and `unresolved` rows. An item that exists with zero images is `has_image:false` (a real item to fix), distinct from `unresolved` (not in catalogue). **Also accepts `stock_item_ids` to skip resolution, and reports `rate_limited` / `complete` (v1.37.0, issue #34).** Use before a bulk list to skip/flag image-less SKUs. **Live-tested 18 Jun 2026** |

**GLT design notes (issue #16):** The issue referenced `Listings/…` but the endpoints are `GenericListings/…`. The configurator is applied at **CreateTemplates** time via `ConfiguratorId` — `SaveConfiguratorData`/`SaveConfiguratorFields` modify a *configurator's own definition*, not a template, so they're **not** part of the list-an-item flow. `_fetch_shopify_configurators()` normalizes the nested `Info.{Name,Id,ChannelId,SubSource}.Value` fields into flat dicts. v1 is Shopify-only by design (most forgiving channel); other channels (`Amazon`, `TikTok`, `Walmart`, etc. — also in the `ChannelType` enum) can follow once the write path is proven.

**GLT channel coverage (probed 8 Jul 2026 — "delist other channels" request).** The GLT (`OpenTemplatesByInventory`/`ProcessTemplates`) is the ONLY listing-management surface in the public API, and it only covers channels in its `ChannelType` enum (`CDiscount_OBSOLETE/Shopify/Magento/External/Walmart/TikTok/Amazon`). Per-type `GetConfiguratorsInfoPaged` on this tenant: **Shopify 67, Amazon 10 (1 account, ChannelId 2 "The Warehouse Group"), TikTok 5**; Magento 0, Walmart 0; **eBay → 400 "Invalid parameter request" (not a GLT channel at all)**. So the delist mechanism can, in principle, extend to **Amazon + TikTok** (same `ProcessTemplates Delete`, different `ChannelType`/`ChannelName` + channel-id from that channel's configurator catalogue) — but **eBay, Mirakl, and (this tenant) Magento cannot be delisted via the MCP at all** (no GLT, no dedicated public listing API — see the "does NOT work" table). ⇒ **"delist every channel" is not achievable**; a generalised tool can only clear GLT channels, and must report eBay/Mirakl/Magento as `skipped_channels` for manual take-down. **Built in v1.32.0 (issue #30)** — `unpublish_channel_listing(channel=…)` + `delist_all_channel_listings` now cover Shopify/Amazon/TikTok, with eBay/Etsy/Mirakl/CDiscount always skipped-with-a-reason. Re-probed 5 Aug 2026: same coverage, plus **Etsy → 400 "Invalid parameter request"** (not GLT either) and **CDiscount_OBSOLETE → 400 null-ref** / **External → 400 "Failed to create a channel"**. **Amazon Delete is now LIVE-PROVEN (5 Aug 2026, `MOB-GRP-3080` tpl 31703)** and **TikTok Delete is LIVE-PROVEN (7 Aug 2026, `ven-20-black-raw-core-complete-7.5` tpl 30006 — v1.42.0)** — both `delete_proven:true`. **All three GLT channels in use on this tenant are now proven**; only Magento/Walmart remain `false`, and they have 0 configurators here so they are untestable as well as unproven. **Amazon's GLT shape differs structurally from Shopify's** — one account (ChannelId 2) fronts 7 regional channel-SKU SubSources, and one item can hold SEVERAL templates (merchant + `.FBA`); see the v1.32.0 note. **TikTok differs from Shopify in the opposite direction** — the variation CHILD holds its own template rather than the parent, so a TikTok child can usually be retired on its own; see the v1.42.0 note. **The REVISE side (`refresh_channel_listing`) got the same channel generalisation in v1.47.0 (issue #42)**, tracked by its own `revise_proven` flag on `GLT_CHANNELS` — `Delete` and `Revise/Update` are proven independently per channel (a channel can have one proven and not the other), and only Shopify's `revise_proven` is currently `True`. **Correction (issue #43, 25 Aug 2026):** "no dedicated public listing API" for eBay above was about GLT specifically — it is still true that eBay has no GLT template and cannot be delisted via this mechanism, but eBay DOES have its own separate, non-GLT `Listings/` API that can REVISE (not delist) a listing's description — see `revise_ebay_listing_description` and its own `EBAY_CHANNELS` registry/`revise_proven` flag, tracked independently of `GLT_CHANNELS`.

---

## Confirmed working endpoints

| Endpoint | Method | Payload shape | Key notes |
|---|---|---|---|
| `Auth/AuthorizeByApplication` | POST | `{"ApplicationId","ApplicationSecret","Token"}` JSON body | Returns session `Token` + `Server` URL |
| `OpenOrders/GetOrdersLowFidelity` | POST | `{"request":{"LocationId":"..."}}` | Primary open-orders list |
| `OpenOrders/GetOpenOrdersDetails` | POST | `{"OrderIds":["pkOrderID-guid",...]}` **unwrapped** | Use GUID `pkOrderID`, not numeric |
| `Orders/GetOrdersById` | POST | `{"pkOrderIds":["guid",...]}` **unwrapped** | Bulk order detail; response includes `CustomerInfo.Address.EmailAddress`, `CustomerInfo.Address.FullName`, `CustomerInfo.ChannelBuyerName`; `OrderItem.RowId` = `OrderItemRowId` used by refund tools; `FulfilmentLocationId` used by `cancel_order`; **composite components nest under `OrderItem.CompositeSubItems`** (NOT `CompositeChild` — that field exists only on open orders via `GetOrdersLowFidelity`). Child `Quantity` is the resolved line total (e.g. 5 packs × 10 = 50) — **do not multiply by parent qty**. Children's `PricePerUnit`/`Cost` are `0.0` (money is on the parent line); `Level` is `0` on both parent and child, so detect composites by non-empty `CompositeSubItems`, not `Level`. `_batch_order_items` / `_flatten_order_item` preserve this nested array under `composite_sub_items`; `get_component_sales` consumes it. Confirmed 13 Jun 2026 |
| `Orders/GetOrderDetailsByNumOrderId` | GET | `?orderId=<numeric>` | Single order by human-facing number |
| `Inventory/GetInventoryItem` | POST | `{"sku":"..."}` or `{"stockItemId":"..."}` **unwrapped** | Exact match only; returns `StockItemId` |
| `Stock/GetStockItems` | GET | `?keyWord=<term>&entriesPerPage=<n>&pageNumber=<n>` | **Keyword search behind the UI inventory box** — matches title, SKU, and barcode (partial, case-insensitive). Response: `{"PageNumber","EntriesPerPage","TotalEntries","TotalPages","Data":[...]}`; each `Data` row has `ItemNumber` (SKU), `ItemTitle`, `BarcodeNumber`, `Quantity`, `Available`, `InOrder`, `RetailPrice`, `PurchasePrice`, `CategoryName`, `StockItemId`, `IsCompositeParent`, `IsVariationParent`. Optional `locationId` query param scopes stock figures to one location. **This is the working keyword-search endpoint** — the plural `Stock/GetStockItemsFull` and `Inventory/GetInventoryItems` both 400 here, but this singular GET does not. **Live-tested 16 Jun 2026** (issue #14) |
| `Stock/GetStockLevel_Batch` | POST | `{"request":{"StockItemIds":["guid",...]}}` | Returns all location rows — **always all 29 on this tenant, including zeros**, so it cannot be used to narrow which locations an item has ever been held at |
| `Stock/GetItemChangesHistory` | GET | `?stockItemId=<guid>&locationId=<guid>&entriesPerPage=<n>&pageNumber=<n≥1>` | **The stock audit trail** — every change to an item's level at one location. Response `{PageNumber, EntriesPerPage, TotalEntries, TotalPages, Data:[StockItemChangeHistory]}`, rows **NEWEST-FIRST**. **⚠️ `locationId` is REQUIRED** — the spec documents it optional ("If null then combined") but omitting it returns HTTP 400 "The request is invalid."; there is **no combined mode**, scan per location. **⚠️ `pageNumber=-1`** (spec: "if -1 then will return all pages") returns HTTP 400 "Value must be at least 1." **⚠️ The `StockItemChangeHistory` model has NO `ChangeSource` field** — the source is only in the free-text `Note` (the underlying `StockChange` table does have one, hence the custom-SQL export can filter on it); `_classify_change_source()` derives it. **⚠️ `Level` is the level AFTER the change** (spec says only "Change level") — verified on 15 consecutive pairs: `Level - ChangeQty` == the next-older row's `Level`. **⚠️ Every row's `StockItemId` is the ZERO GUID**, not the item's — don't key off it. No 200-row cap (`entriesPerPage=1000` works); a past-the-end page returns empty `Data` with a **200** (clean stop, unlike `GetStockItemsFull`'s 400). Rate limit 250/min. Wrapped by `get_stock_change_history`. **Live-tested 6 Aug 2026** (issue #33) |
| `Stock/GetItemChangesHistoryCSV` | GET | `?stockItemId=<guid>&locationId=<guid>` | CSV-link variant of the above (returns a `TempFile`). Rate limit 150/min. **Not wrapped** — the JSON endpoint is paged and directly consumable; noted so the CSV route isn't rediscovered as new |
| `Stock/GetStockItemsFullByIds` | POST | `{"request":{"StockItemIds":["guid",...]}}` | Item metadata by GUID |
| `Stock/GetStockItemsFullByIds` (with suppliers) | POST | `{"request":{"StockItemIds":[...],"DataRequirements":[1]}}` | **`DataRequirements:[1]` required** to populate `Suppliers[]` — default `[0]` returns empty array |
| `Stock/GetStockItemsByIds` | POST | `{"stockItemIds":["guid",...]}` **unwrapped** | Alternate metadata endpoint |
| `Inventory/GetInventoryItem` (variation parents) | POST | `{"sku":"..."}` **unwrapped** | For a variation **parent**, the response also carries a top-level `VariationGroupName` (absent/`None` for children + standalones). **`IsVariationParent` is UNRELIABLE** — observed `false` on a genuine parent — so don't trust the flag for role detection; use the relationship endpoints below. Confirmed 18 Jun 2026 (issue #17) |
| `Stock/GetVariationGroupByParentId` | GET | `?pkStockItemId=<parent-guid>` | Forward variation lookup. Returns `{VariationSKU, pkVariationItemId, VariationGroupName}` for a parent (the parent's `StockItemId` **==** `pkVariationItemId`), or HTTP 200 **`null`** for a non-parent (clean signal — same null-not-404 behaviour as `GetVariationGroupByName`). Confirmed 18 Jun 2026 (issue #17) |
| `Stock/GetVariationItems` | GET | `?pkVariationItemId=<guid>` | Returns the group's **child members only** (the parent is NOT in the list): `[{pkRowId, pkStockItemId, ItemNumber, ItemTitle}]`. Confirmed 18 Jun 2026 (issue #17) |
| `Stock/GetVariationGroupSearchTypes` | GET | — | Enum of `SearchVariationGroups` search types: `ItemSKU` ("Item SKU", matches member/child SKUs), `VariationName` ("Variation Name", default), `ParentSKU` ("Variation SKU", matches the parent/variation SKU). Confirmed 18 Jun 2026 (issue #17) |
| `Stock/SearchVariationGroups` | GET | `?searchType=<ItemSKU\|VariationName\|ParentSKU>&searchText=<text>&pageNumber=<n>&entriesPerPage=<n>` | Reverse variation lookup. Response `{PageNumber, EntriesPerPage, TotalEntries, TotalPages, Data:[{VariationSKU, pkVariationItemId, VariationGroupName}]}`. **`searchType=ItemSKU` SUBSTRING-matches member SKUs** (e.g. `"carver"` matches `carver-1-1-4-truck-hardware`), so a child→parent resolution must confirm **exact** membership via `GetVariationItems` to avoid false positives. Returns 0 entries for a standalone SKU. Confirmed 18 Jun 2026 (issue #17) |
| `Inventory/GetInventoryItemsCompositionByIds` | POST | `{"request":{"InventoryItemIds":["guid",…]}}` **WRAPPED** | **Bulk composition resolver** — the many-items form of `GetInventoryItemCompositions`. Returns `{"InventoryItemsCompositionByIds": {parent_id: [StockItemComposition,…]}}`, a MAP keyed by parent id. **Two gotchas, both live-confirmed 5 Aug 2026 (issue #31):** (1) **hard cap of 100 ids per call** — 200 returns HTTP 400 `{"Message":"The maximum items count for this call is 100"}` (the spec documents no limit); (2) items with **no compositions are OMITTED from the map**, not returned as empty lists (100 ids → 49 keys), so absence means "not a composite", not "call failed". Rate limit 250/min. Each `StockItemComposition` carries `LinkedStockItemId` (the COMPONENT), `SKU`, `Quantity`, `PurchasePrice`, and `StockItemId` (the PARENT — same trap as the singular endpoint). Wrapped by `find_composite_parents` |
| `Stock/GetStockItems` (composite parents) | GET | `?keyWord=&entriesPerPage=200&pageNumber=<n>` | The same active-catalogue sweep used by `_count_items_per_category`, reused by `find_composite_parents` to enumerate composite parents. **`IsCompositeParent` on these rows is RELIABLE** — unlike its sibling `IsVariationParent` (which is not; see `get_item_relationships`). Cross-checked live 5 Aug 2026 on a 200-item page: 96 flagged, 96 with components, zero disagreement in either direction. The sweep also yields a free SKU→StockItemId map. On this tenant: 34,023 active items / 171 pages → 4,343 composite parents. Still **active-only** (archived parents invisible) |
| `Inventory/GetInventoryItemCompositions` | GET | `?inventoryItemId=<parent-guid>&getFullDetail=true` | Forward composite lookup. Returns `[{LinkedStockItemId, ItemTitle, SKU, Quantity, PurchasePrice, ...}]` for a composite parent, or **`[]`** for a non-composite. **`LinkedStockItemId` is the COMPONENT's stock item id**; each row's own `StockItemId` field is the PARENT's id — do not use it as the component id. Confirmed 18 Jun 2026 (issue #17) |
| `OpenOrders/GetViewStats` | POST | `{"request":{}}` | Sanity check: returns order counts by view |
| `Inventory/GetStockLocations` | GET | — | All locations; Default location ID = `00000000-0000-0000-0000-000000000000` |
| `ProcessedOrders/SearchProcessedOrders` | POST | `{"request":{"DateField":"received","FromDate":"...","ToDate":"...","PageNumber":1,"ResultsPerPage":20}}` | `DateField` required (received/processed/payment/cancelled); min 20 results per page; flat response — no GeneralInfo/ShippingInfo nesting |
| `Inventory/GetInventoryItemExtendedProperties` | POST | `{"inventoryItemId":"guid"}` **unwrapped** | Field name typo in response: `ProperyName` not `PropertyName` |
| `Inventory/GetSuppliers` | GET | — | Not in public OpenAPI specs but confirmed working; returns flat supplier list |
| `Inventory/GetStockSupplierStat` | GET | `?inventoryItemId=<guid>` | **Per-item supplier links** — returns a JSON array of `StockItemSupplierStat` {IsDefault, Supplier, SupplierID, Code, SupplierBarcode, LeadTime, PurchasePrice, SupplierMinOrderQty, SupplierPackSize, SupplierCurrency, StockItemId} (`[]` when the item has no supplier links). NB: the 404 probe `GetInventoryItemSupplierStat` was just the wrong name. Wrapped by `get_inventory_item_suppliers`. **Live-tested 14 Jul 2026** (issue #25) |
| `Inventory/GetStockSupplierStatsBulk` | GET | `?inventoryItemIds=["guid","guid"]` | Batch form — **the array param must be a JSON-array STRING in the query string**; repeated `inventoryItemIds=` params and comma-joined lists both 400 "Invalid parameter inventoryItemIds". Returns a FLAT array across items (map back via each row's `StockItemId`). Not yet wrapped by a tool. **Live-tested 14 Jul 2026** (issue #25) |
| `Inventory/CreateStockSupplierStat` | POST | `{"itemSuppliers": [StockItemSupplierStat, ...]}` | Creates supplier links; **204 No Content** on success → use `call_linnworks_void`. **No client-generated GUID needed** (rows keyed by StockItemId+SupplierID — unlike the pkRowId sub-entity Creates). **Creating a row with `IsDefault:true` auto-flips the item's previous default row to false server-side.** Wrapped by `set_inventory_item_suppliers`. **Live-tested 14 Jul 2026** (issue #25) |
| `Inventory/UpdateStockSupplierStat` | POST | `{"itemSuppliers": [StockItemSupplierStat, ...]}` | Updates supplier links by (StockItemId, SupplierID); **204 No Content** → void call. **CLEARS any field omitted from the row** (live-proven: a cost-only update wiped `Code`) — carry the full existing row through, same gotcha as `UpdateInventoryItem`/PO header. Wrapped by `set_inventory_item_suppliers` (merge-on-update). **Live-tested 14 Jul 2026** (issue #25) |
| `PurchaseOrder/Search_PurchaseOrders2` | POST | `{"Status":"PENDING","EntriesPerPage":50,"PageNumber":1,...}` **unwrapped** | Wrapping in `{"request":{...}}` silently ignores all filters |
| `PurchaseOrder/Get_PurchaseOrder` | POST | `{"pkPurchaseId":"guid"}` **unwrapped** | Returns header + items + delivery records |
| `Orders/SetOrderCustomerInfo` | POST | `{"orderId":"guid","info":{"ChannelBuyerName":"...","Address":{...},"BillingAddress":{...}},"saveToCrm":false}` **unwrapped** | Address fields: FullName, Company, Address1/2/3, Town, Region, PostCode, Country, PhoneNumber, EmailAddress, CountryId, Continent; **not in public GitHub OpenAPI spec** but confirmed in apidocs.linnworks.net; returns OrderTotalsInfo |
| `PurchaseOrder/Update_PurchaseOrderHeader` | POST | `{"updateParameter":{...all fields...}}` | Must carry all existing fields — nulls clear values; blocks on DELIVERED |
| `PurchaseOrder/Add_PurchaseOrderItem` | POST | `{"addItemParameter":{"pkPurchaseId","fkStockItemId","Qty","Cost","TaxRate","PackQuantity","PackSize"}}` | Adds a new line; same endpoint used by `create_purchase_order`. **`Cost` is the tax-inclusive LINE TOTAL** per the spec: `(unitcost × qty) + tax` — NOT the unit cost. Sending the bare unit cost makes Linnworks back-derive a wrong unit (`unit/1.2/qty`) and wrong PO totals (issue #15). Tools convert via `_po_line_cost_inc_tax()`. The header `UnitAmountTaxIncludedType:0` is correct and unchanged |
| `PurchaseOrder/Update_PurchaseOrderItem` | POST | `{"updateItemParameter":{"pkPurchaseItemId","pkPurchaseId","Quantity","PackQuantity","PackSize","Cost","TaxRate"}}` | Note: uses `Quantity` (not `Qty` like Add); all fields required — carry unchanged values through. **`Cost` is the same tax-inclusive line total** as Add (issue #15) — the tool tracks the diff in ex-VAT unit terms and converts on write |
| `PurchaseOrder/Delete_PurchaseOrderItem` | POST | `{"deleteItemParameter":{"pkPurchaseItemId","pkPurchaseId"}}` | Removes a line item; `pkPurchaseItemId` from `Get_PurchaseOrder` response |
| `PurchaseOrder/Delete_PurchaseOrder` | POST | `{"pkPurchaseId":"guid"}` **unwrapped** | Deletes an entire PO (header + all lines + notes). Empty 2xx on success; subsequent `Get_PurchaseOrder` returns HTTP 400 "Purchase Order does not exist" (used as the read-back confirmation). **Wrapped by `delete_purchase_order` (v1.29.0)** — gated hard (irreversible, staging threshold 10, dry_run default). Also still used directly for test-PO teardown. **Live-confirmed 16 Jun 2026; tool live-tested 24 Jul 2026** |
| `RulesEngine/GetRules` | GET | — | Flat list of rule headers |
| `RulesEngine/GetRuleConditionNodes` | GET | `?pkRuleId=<int>` | Full IF/THEN tree for one rule |
| `ImportExport/GetImportList` | GET | — | List endpoint — reliable status |
| `ImportExport/GetExportList` | GET | — | Same shape as import list |
| `ImportExport/GetImport` | GET | `?id=<int>` | Config only — `ImportStatus` is null even for erroring imports |
| `ImportExport/GetExport` | GET | `?id=<int>` | Config only |
| `ImportExport/RunNowImport` | POST | `{"importId": <int>}` **unwrapped** | Returns **204 No Content** — use `call_linnworks_void`, not `call_linnworks`; puts import in queue immediately; not yet live-tested |
| `ImportExport/RunNowExport` | POST | `{"exportId": <int>}` **unwrapped** | Returns **204 No Content** — same pattern; not yet live-tested |
| `OpenOrders/SearchOrders` | POST | `{"LocationId":"...","SearchTerm":"...","IncludeProcessed":false}` **unwrapped** | Searches by ReferenceNum, ExternalReference, and related fields; response: `{"OpenOrders":[{"ViewId":n,"OrderIds":["guid",...]}],"ProcessedOrders":["guid",...]}` — open orders grouped in view objects (the SAME GUID can appear under multiple ViewIds — dedupe), processed orders as flat GUID list; **live-tested 15 Jun 2026: must be sent UNWRAPPED.** The `{"request":{...}}` wrapper returns HTTP 400 "Must provide a search term." even though the OpenAPI spec names the body parameter `request` identically to `GetOrdersLowFidelity` (which DOES require the wrapper). Same gotcha as `Search_PurchaseOrders2` |
| `Orders/CancelOrder` | POST | `{"orderId":"guid","fulfilmentCenter":"guid","note":"..."}` **unwrapped** | Cancels an open order; `fulfilmentCenter` = `FulfilmentLocationId` from order detail; optional `refund` (double) field for attached refund amount; returns a string |
| `Orders/LockOrder` | POST | `{"orderIds":["guid",…],"lockOrder":true/false}` **unwrapped** | Lock (true) / unlock (false) a list of orders. A locked order is barred from picking/dispatch (padlock in UI). **No readable lock field exists on the order model** (`lockOrder` is the only "lock" token in the whole orders spec) — so a lock CANNOT be read-back-verified via the API; confirm in the UI. **⚠️ Locking RELEASES the order's allocated stock** (park keeps it — but park has no endpoint). Rate limit 150/min. Wrapped by `set_order_status` (lock/unlock). **LIVE-PROVEN 28 Jul 2026** — reversible lock→unlock round-trip on open order 606371 (both empty 2xx) |
| `Orders/ChangeStatus` | POST | `{"orderIds":["guid",…],"status":<int>}` **unwrapped** | Set order (payment) status on a list of orders. **Enum (from the endpoint description): `0=UNPAID, 1=PAID, 2=RETURN, 3=PENDING, 4=RESEND`** — so paid→1, unpaid→0. This is the **`GeneralInfo.Status`** enum on `GetOrdersById` (a live PAID order reads back 1), DISTINCT from the GetOrdersLowFidelity display codes in `_ORDER_STATUS_LABELS`. Rate limit 150/min. Wrapped by `set_order_status` (paid/unpaid), read-back verified against `GeneralInfo.Status`. **Enum + read path confirmed 28 Jul 2026; the ChangeStatus write not yet live-fired** |
| `ReturnsRefunds/GetRefundOptions` | POST | `{"request":{"OrderId":"guid"}}` | Returns `RefundOptions` including `CanRefund`, `CannotRefundReason` enum; use as pre-flight check before CreateRefund; **read confirmed live 15 Jun 2026** (wrapped payload correct; returns full options object with `CanRefund`/`CanRefundShipping`/etc.) |
| `ReturnsRefunds/GetRefundHeadersByOrderId` | POST | `{"request":{"OrderId":"guid"}}` | Returns all existing refund headers for an order — useful to audit whether an order has already been (partially) refunded before calling CreateRefund; **spec-based, not yet live-tested** |
| `ReturnsRefunds/CreateRefund` | POST | `{"request":{"OrderId":"guid","ChannelInitiated":false,"RefundLines":[{"OrderItemRowId":"guid","RefundedUnit":"Item","Amount":10.00,"FreeTextOrNote":"..."},{"RefundedUnit":"Shipping","Amount":5.00}]}}` | Creates a refund record; `OrderItemRowId` = `OrderItem.RowId` from `GetOrdersById`; omit `OrderItemRowId` for Shipping/Additional lines; `RefundedUnit` enum: `Item`, `Shipping`, `Service`, `Additional`; returns `{RefundHeaderId, RefundReference, Status, CannotRefundReason, Errors}`; **spec-based, not yet live-tested** |
| `ReturnsRefunds/ActionRefund` | POST | `{"request":{"RefundHeaderId":42,"OrderId":"guid"}}` | Pushes an approved refund to the sales channel (Shopify/Amazon/eBay); returns `{SuccessfullyActioned, Status, Errors}`; note: `SuccessfullyActioned` can be true while individual `Errors` still exist — check both; **spec-based, not yet live-tested** |
| `Orders/GetOrderNotes` | GET | `?orderId=<guid>` | Returns a plain JSON array of `OrderNote` objects; **canonical fields per OpenAPI spec (confirmed May 2026)**: `OrderNoteId`, `OrderId`, `NoteDate`, `Internal` (bool, no `Is` prefix), `Note`, `CreatedBy`, `NoteTypeId`; **GUID required** — numeric IDs must be resolved first; **previously documented field names `pkOrderNoteId`/`IsInternal`/`NoteCreatedOn` were wrong** (fixed in issue #7) |
| `Orders/AddOrdersNote` | POST | `{"OrderIds":["guid",...],"NoteText":"...","IsInternal":true,"IsProcessingNote":false}` **unwrapped** | Accepts a list of order GUIDs; works on both open and processed orders |
| `ProcessedOrders/DeleteOrderNote` | POST | `{"pkOrderNoteId":"guid"}` **unwrapped** | Deletes a single note by its GUID; works on both open and processed orders; **no UpdateOrderNote endpoint exists** — use delete+add via `update_order_note` instead |
| `Inventory/AddInventoryItem` | POST | `{"inventoryItem": {StockItemId, ItemNumber, ItemTitle, BarcodeNumber, RetailPrice, ...}}` **unwrapped** | Creates a new inventory item; **`StockItemId` is REQUIRED and must be a client-generated GUID** (`uuid.uuid4()`) — omitting it returns HTTP 400 "StockItem StockItemId could not be empty"; returns an empty 2xx body on success (the generated GUID is the new item ID); **live-tested 15 Jun 2026** |
| `Inventory/UpdateInventoryItem` | POST | `{"inventoryItem": {StockItemId, ItemNumber, ItemTitle, ...all fields...}}` **unwrapped** | Updates an existing item; must carry ALL fields (nulls clear values — same gotcha as PO header update); empty 2xx body on success; **live-tested 15 Jun 2026** |
| `Inventory/DeleteInventoryItems` | POST | `{"inventoryItemIds": ["guid",...]}` **unwrapped** | Permanently deletes items by StockItemId GUID; empty 2xx body on success; wrapped by the `delete_inventory_item` tool (v1.14.0); **live-tested 15 Jun 2026** — create → delete → read-back-gone cycle confirmed |
| `Inventory/GetInventoryItemPrices` | GET | `?inventoryItemId=<guid>` | Returns array of price rows `[{pkRowId, Source, SubSource, Price, Tag, ...}]`; used as read-before-write for `set_inventory_item_prices`; **read confirmed live 15 Jun 2026** (one row per Source/SubSource channel) |
| `Inventory/CreateInventoryItemPrices` | POST | `{"inventoryItemPrices": [{pkRowId, StockItemId, Source, SubSource, Price}]}` | Creates new channel price rows; **`pkRowId` must be a client-generated GUID** (omitting it → PK collision on zero-GUID); empty 2xx body; **only for genuine channel rows** — the default price (empty Source+SubSource) is the implicit zero-GUID row not returned by the GET, so set it via Update (pkRowId `00000000-...`) instead; **live-tested 15 Jun 2026** |
| `Inventory/UpdateInventoryItemPrices` | POST | `{"inventoryItemPrices": [{StockItemId, pkRowId, Source, SubSource, Price}]}` | Updates existing price rows by pkRowId; **spec-based, not yet live-tested** |
| `Inventory/GetInventoryItemDescriptions` | GET | `?inventoryItemId=<guid>` | Returns array of description rows `[{pkRowId, Source, SubSource, Description}]`; used as read-before-write for `set_inventory_item_descriptions`; **read confirmed live 15 Jun 2026** |
| `Inventory/CreateInventoryItemDescriptions` | POST | `{"inventoryItemDescriptions": [{pkRowId, StockItemId, Source, SubSource, Description}]}` | Creates new description rows; **`pkRowId` must be a client-generated GUID**; empty 2xx body; no implicit default row (unlike prices); **live-tested 15 Jun 2026** |
| `Inventory/UpdateInventoryItemDescriptions` | POST | `{"inventoryItemDescriptions": [{StockItemId, pkRowId, Source, SubSource, Description}]}` | Updates existing description rows by pkRowId; **spec-based, not yet live-tested** |
| `Inventory/GetInventoryItemTitles` | GET | `?inventoryItemId=<guid>` | Returns array of channel-title rows `[{pkRowId, Source, SubSource, Title, StockItemId}]` (per-channel listing title overrides, distinct from the base `ItemTitle`); read-before-write for `set_inventory_item_titles`; **live-tested 23 Jun 2026** (issue #21) |
| `Inventory/CreateInventoryItemTitles` | POST | `{"inventoryItemTitles": [{pkRowId, StockItemId, Source, SubSource, Title}]}` | Creates new channel-title rows; **`pkRowId` must be a client-generated GUID** (same gotcha as descriptions); empty 2xx body; **live-tested 23 Jun 2026** — create + update verified by read-back against an isolated test SKU (issue #21) |
| `Inventory/UpdateInventoryItemTitles` | POST | `{"inventoryItemTitles": [{StockItemId, pkRowId, Source, SubSource, Title}]}` | Updates existing channel-title rows by pkRowId; **live-tested 23 Jun 2026** (issue #21) |
| `Inventory/DeleteInventoryItemTitles` | POST | `{"inventoryItemTitleIds": ["guid",...]}` | Deletes channel-title rows by pkRowId; exists in the spec but **not wrapped by any tool** (no delete request in issue #21); spec-based, not exercised |
| `Inventory/CreateInventoryItemExtendedProperties` | POST | `{"inventoryItemExtendedProperties": [{pkRowId, fkStockItemId, SKU, ProperyName, PropertyValue, PropertyType}]}` | Creates new extended property rows; **`pkRowId` must be a client-generated GUID**; note deliberate API typo `ProperyName`; **returns a 2xx with a NON-empty, NON-JSON body on success** (not an empty body like the sibling write endpoints) — `call_linnworks` now falls back to `{"raw": text}` on `JSONDecodeError` so the write isn't mis-reported as failing (issue #13, 15 Jun 2026) |
| `Inventory/UpdateInventoryItemExtendedProperties` | POST | `{"inventoryItemExtendedProperties": [{fkStockItemId, pkRowId, ProperyName, PropertyValue, PropertyType}]}` | Updates existing property rows by pkRowId; note deliberate API typo; **same non-JSON 2xx success body as the Create endpoint** — handled by the tolerant parse in `call_linnworks`; **create + update both live-tested 15 Jun 2026** against an isolated test SKU (persistence confirmed: the update path read back the value the create wrote) — issue #13 |
| `Inventory/AddImageToInventoryItem` | POST | `{"request": {StockItemId, ImageUrl, IsMain}}` | Adds a single image by URL; no bulk equivalent — iterate per item; **live-confirmed 17 Jul 2026** (issue #28 test bed — 3 images added, read back with SortOrder 0/1/2 in insertion order) |
| `Inventory/DeleteInventoryItemImageBulk` | POST | `{"request":[{"InventoryItemId":"guid","ItemNumber":"sku","ImageIds":["guid",…]}]}` | **Deletes images by IMAGE ID** — the one to use, since `get_inventory_item_images` returns ids. Returns **HTTP 200 with a body** (`BatchedAPIResponse`: `{Results:[{InventoryItemId,ItemNumber,ImageId}], TotalResults, ResultStatus:"SUCCESSFUL"/…}`) — **not** 204, so `call_linnworks` (not the void helper). `InventoryItemId` wins if both it and `ItemNumber` are sent. Wrapped by `delete_inventory_item_images`. **Live-tested 17 Jul 2026** (issue #28) |
| `Inventory/DeleteImagesFromInventoryItem` | POST | `{"inventoryItemImages": {"<stockItemId>": ["<image url>", …]}}` | Deletes images keyed by **image URL**, not id (a map of item id → url list); returns **204**. Rate limit 15/min. **Not wrapped** — `DeleteInventoryItemImageBulk` is the id-keyed sibling and fits our read shape; noted here so the url-keyed body isn't mistaken for the id-keyed one. Spec-based, not exercised |
| `Inventory/UpdateImages` | POST | `{"images": [StockItemImageSimple, …]}` | Updates image rows (`pkRowId`, `IsMain`, `SortOrder`, `ChecksumValue`, `RawChecksum`, `StockItemId`, `StockItemIntId`); **204**. **⚠️ PINS THE MAIN IMAGE TO SortOrder 0 — the result is always `[main] + [submitted order minus main]`, never what you asked if the main isn't first.** A **FULL set submitted main-first is honoured exactly**; a partial payload or a non-main-first order gets re-normalised (all live-proven 17 Jul 2026: `[img2,img3,img4,img1]`/img1-main → `[img1,img2,img3,img4]`; `[img1,img2,img3,img4]`/img3-main → `[img3,img1,img2,img4]`). Also **CLEARS any field omitted from a row** (nulls-clear family) — carry the whole row. **Field-name gotcha:** the GET returns the checksum as `CheckSumValue` but this model wants `ChecksumValue`. Wrapped by `set_inventory_item_image_order`. **Live-tested 17 Jul 2026** (issue #28) |
| `Inventory/SetInventoryItemImageAsMain` | POST | `{"inventoryItemId":"guid","mainImageId":"guid"}` **unwrapped** | Sets one image as the item's main image and clears the flag on the others server-side; **204**. **Does NOT reorder** — the new main keeps its SortOrder (live-observed sitting at sort 2 while main), until any later `UpdateImages` pins it to 0. Wrapped by `set_inventory_item_image_order`. **Live-tested 17 Jul 2026** (issue #28) |
| `Inventory/UploadImagesToInventoryItem` | POST | — | Raw image **file** upload (vs `AddImageToInventoryItem`'s URL). Exists in the spec; **not wrapped** (no request in issue #28) — relevant if images ever need pushing from local files rather than a public URL |
| `Stock/UpdateStockLevelsBulk` | POST | `{"Items": [{SKU, StockItemId, StockLocationId, StockLevel}]}` | Sets absolute stock levels; **returns an empty 2xx body — does NOT echo `Items`** on this tenant, so per-item success is inferred from the 2xx (verify with `get_stock_level`); **live-tested 15 Jun 2026** |
| `Stock/CreateVariationGroup` | POST | `{"template": {VariationGroupName, ParentSKU, ParentStockItemId, VariationItemIds: [guid]}}` | Creates a new variation group. **⚠️ `ParentSKU` must be a BRAND-NEW SKU that does NOT already exist** — passing an existing item's SKU → HTTP 400 "Chosen variation SKU already exists as a normal stock item." **`ParentStockItemId` = the zero-GUID `00000000-…`** (server MINTS the parent id, no client GUID); child GUIDs must resolve to existing items. Returns `{VariationSKU, pkVariationItemId, VariationGroupName}` (unwrapped) — `pkVariationItemId` is the minted group id. Wrapped by `create_variation_group`. **Live-tested 28 Jul 2026** (issue #29 — first-ever live run; corrected the parent-SKU model) |
| `Stock/CheckVariationParentSKUExists` | GET | `?parentSKU=<string>` | Returns a bare string: **`NotExists`** (SKU is free — usable as a new variation parent), **`Exists`** (already a normal stock item), or **`AlreadyVariation`** (already a variation parent). Used as the pre-flight in `create_variation_group`. **Live-tested 28 Jul 2026** (issue #29) |
| `Stock/AddVariationItems` | POST | `{"pkVariationItemId":"<group id>","pkStockItemIds":["<child guid>",…]}` **unwrapped** | **Adds child items to an EXISTING variation group.** `pkVariationItemId` = the group id (== the parent's StockItemId). Empty 2xx on success (read back with `GetVariationItems`). Wrapped by `add_variation_group_items`. **Live-tested 28 Jul 2026** (issue #29) |
| `Stock/DeleteVariationGroup` | GET | `?pkVariationGroupId=<group id>` | Deletes a whole variation group (and its virtual parent SKU). **Returns an EMPTY 2xx body** — `call_linnworks_get`'s `response.json()` raises on it, so the delete succeeds server-side but the client sees a JSON error; catch it (used only for test-PO/group teardown, not wrapped by a tool). Confirmed 28 Jul 2026 |
| `Stock/GetVariationGroupByName` | GET | `?variationGroupName=<string>` | **⚠️ UNRELIABLE — returns HTTP 200 `null` even for a group that demonstrably exists** (live-proven 28 Jul 2026, seconds after creating one). **Do NOT use for group lookup** — use `Stock/SearchVariationGroups` (searchType=VariationName, substring) with an exact-match confirmation instead (the `_find_variation_group_by_name()` helper). Kept here only to document the trap |
| `GenericListings/GetConfiguratorsInfoPaged` | POST | `{"request":{"ChannelType":"Shopify","ChannelName":"SHOPIFY","PaginationParameters":{"PageNumber":1,"EntriesPerPage":1000}}}` | Lists GLT configurators for a channel. **Other GLT channels use the same shape with their own pair — `"Amazon"`/`"AMAZON"` (10 configurators, ONE account: ChannelId 2, SubSource "The Warehouse Group"), `"TikTok"`/`"TIKTOK"` (5, ChannelId 30, "SKATEWAREHOUSE_UK"), `"Magento"`/`"MAGENTO"` (0 here), `"Walmart"`/`"WALMART"` (0 here) — live-confirmed 5 Aug 2026 (issue #30).** **`ChannelName` must be the Source string `"SHOPIFY"`, NOT a SubSource** — a SubSource ("SWH Shopify" etc.) returns HTTP 400 "Channel types mismatch on channel factory creation: Shopify - Shopify". Response: `{"ConfiguratorsInfo":[{"Info":{"Name":{Value},"Id":{Value},"ChannelId":{Value},"SubSource":{Value},"IsShowInInventory":{Value},...}}]}` — every field is wrapped as `{Type,Value,Errors}` (unwrap via `_glt_field`). 67 configurators in this tenant; `ChannelId` 18=SWH Shopify, 21=Venom Skateboards, 26=Icarus Eyewear, 29=Lobster Eyewear, 34=The Warehouse Group B2B. **Live-confirmed 18 Jun 2026** (issue #16) |
| `GenericListings/CreateTemplates` | POST | `{"request":{"ChannelType":"Shopify","ChannelName":"SHOPIFY","Parameters":{"SelectedRegions":[],"Token":"00000000-...","InventoryItemIds":["guid",...],"ChannelId":<int>},"PaginationParameters":{"PageNumber":1,"EntriesPerPage":<n>},"ConfiguratorId":<int>}}` | Creates GLT listing templates for the given items under one configurator. Response: `{"TemplatesInfo":[...],"AllCreatedIds":[<int>,...]}` — use **`AllCreatedIds`** (cleanly typed int array) for the template ids to process. The configurator is applied HERE via `ConfiguratorId`. **Spec-based, NOT yet live-run** (issue #16) — creates real templates |
| `GenericListings/ProcessTemplates` | POST | `{"request":{"ChannelType":"Shopify","ChannelName":"SHOPIFY","TemplateRequests":[{"TemplateId":<int>,"Action":"Create"}],"ClientContext":{"Activity":"...","Source":"..."}}}` | Pushes templates live to the channel. `Action` enum: `Create`/`Update`/`Relist`/`Delete`/`Revise`/`ChannelSpecific`/`NotAllowed` — `Create` for new listings (from `CreateTemplates`, `list_to_shopify`), `Revise`/`Update` to re-push an existing listing (from `OpenTemplatesByInventory`, `refresh_channel_listing`), `Delete` to END/take down an existing listing (from `OpenTemplatesByInventory`, `unpublish_channel_listing`). Empty 2xx response. **⚠️ CREATES ARE ASYNCHRONOUS (7 Aug 2026, issue #38): templates sit at `Status:"Creating"` and drain at ~50 per 7 minutes, so a 2xx means QUEUED, not listed — a read-back straight after a create proves nothing. DELETES are synchronous and confirm immediately.** **`Action="Delete"` LIVE-PROVEN on SHOPIFY 8 Jul 2026** (v1.25.0 — two listings taken down, confirmed by channel-SKU read-back) **and on AMAZON 5 Aug 2026** (v1.32.0 — `MOB-GRP-3080` tpl 31703 / ASIN B0B311TFG3: channel-SKU row gone, template gone, stock + other channels untouched). **On Amazon it succeeded despite `NextSuggestedAction:"NotAllowed"` — that flag does NOT gate a forced Delete.** **And on TIKTOK 7 Aug 2026** (v1.42.0 — `ven-20-black-raw-core-complete-7.5` tpl 30006 / listing `1729486505080953421`: channel-SKU row gone, `OpenTemplatesByInventory` on ChannelId 30 went 1 → 0, other channels + stock untouched) — **succeeding there despite `Status:"Errors while updating"`, confirming neither the status nor the suggested action gates a Delete.** **`Action="Update"` LIVE-RUN 14 Jul 2026 (v1.27.1) with a critical finding: it pushes the template's STORED field snapshot, NOT current item data** — a stale template (fields last built Feb 2026) overwrote the live selling price with a 5-month-old value; the listing had to be repaired via the Shopify Admin API. Only push templates known to be fresh, and read the live listing back after every push. `Create`/`Revise` remain spec-based, not yet live-run (issue #16). Changes/removes real customer-facing Shopify listings |
| `Listings/GetAllEbayConfigurators` | GET | — | **eBay-specific, NON-GLT** (issue #43, live-confirmed 25 Aug 2026). Returns `{"SourceConfiguratorData":[{"SourceName":"EBAY","SubsourceConfigurators":[{"SubsourceName":"EBAY0","Configurators":[{"ConfiguratorName":...,"ConfiguratorId":<guid>},...]}]}]}`. On this tenant: one source `EBAY`, one subsource `EBAY0`, 33 configurators. This is where the `"EBAY0"` account/store identifier (matching the channel-SKU table's SubSource) was confirmed. Wrapped by nothing directly; used to discover configurator ids for `revise_ebay_listing_description` |
| `Listings/GeteBayConfigurators` | GET | — | **eBay-specific, NON-GLT.** Returns a flat array of `EbayConfig` (no wrapper), each carrying `pkConfigId`, `ConfigName`, `EbayAccount`, `AssociatedTemplates`, `AssociatedVariations`, plus the full listing-policy shape (payment/shipping/returns/etc. — not used here). On this tenant: 33 configurators, 9 with `AssociatedTemplates > 0` (largest "(NEW) VARIATION - On Demand", 1049 templates/36 variations). Wrapped by `_find_ebay_template_for_listing()` (`revise_ebay_listing_description`) to discover which configurators are worth sweeping. **Live-confirmed 25 Aug 2026** |
| `Listings/GeteBayTemplates` | POST | `{"parameters":{"ConfigId":"<guid>","PageNumber":1,"EntriesPerPage":200,"TemplatesType":"Both"}}` | **eBay-specific, NON-GLT.** Returns `{"Items":[EbayListing,...],"TotalItems":<int>,"CurrentPage":<int>,"EntriesPerPage":<int>}`. **⚠️ `ConfigId` (non-zero) OR `TemplateIds` (non-empty) is REQUIRED** — an empty/zero `parameters` object, or `Source`/`SubSource` alone, returns HTTP 400 `{"Message":"Parameter 'parameters' has invalid value"}` (confirmed live: identical error whether `parameters` is `{}`, `null`, every-field-populated-with-defaults, or even a totally different top-level wrapper key — the validation genuinely requires ConfigId/TemplateIds, it is not a wrapping-shape issue). **⚠️ `InventoryItemIds` is documented in the schema but has NO server-side effect** — passing a real StockItemId alongside a real ConfigId returns the exact same first page as omitting it (live-confirmed). No visible page-size cap (500/page returned fine); a past-the-end page returns empty `Items` with a clean HTTP 200 (unlike `GetStockItemsFull`'s 400). Each `EbayListing` carries `TemplateId`, `InventoryItemId`, `SKU` (a group-style identifier on a variation template — see below), `ConfigId`, `ListingIds` (usually one entry, the live eBay item id — **confirmed equal to the channel-SKU table's `ChannelReferenceId`**), `Status`, `Title`, `Description` (plain inner content — **no seller-wrapper markers found across ~1,600 live templates read while probing this**), `Price`, `Attributes[]`, `Variations[]` (each with its own `StockItemId`/`SKU` — the REAL child SKUs on a multi-variation listing; the template's top-level SKU is the group identifier and is shared by every variation, along with one `ListingIds` entry). Wrapped by `_find_ebay_template_for_listing()`. **Live-confirmed 25 Aug 2026, read-only, across 9 configurators / ~1,600 templates** (issue #43) |
| `Listings/ProcesseBayListings` | POST | `{"items":[EbayListing,...],"force":true,"action":"Update"}` | **eBay-specific, NON-GLT write.** Per the spec: "Use this call to create templates in Linnworks and can also be used to push the template to a channel. This will create the template even if it returns null. This will also push the template to the channel depending on what the status is set as." Returns **204 No Content** (`call_linnworks_void`). `action` is documented as "for logging purposes" only (`"Update"`/`"Create"`/`"Process"`), not a behaviour switch. Wrapped by `revise_ebay_listing_description`, which sends the FULL existing `EbayListing` object (read via `GeteBayTemplates`) with only `Description` replaced. **⚠️ FIRED LIVE 26 Aug 2026 (issue #47), ACCEPTED, NOT OBSERVED TO PROCESS.** Called against a real listing (`ven-grip-cleaner`, item `286493672322`): the 204 response was received (so the call itself works as documented), but no channel-side effect was observed on the description-frame URL over a two-hour poll — the identical edit made through the Linnworks listing UI landed within minutes. See `EBAY_CHANNELS["ebay"]["push_observed_reason"]` and the v1.48.1 version note. `EBAY_CHANNELS["ebay"]["revise_proven"]` stays False on that evidence; a human must run the post-merge verification steps (see the v1.48.0 note) on ONE low-risk listing, if and when Linnworks confirms a fix, before it can be trusted |
| `Listings/CreateEbayTemplates` | POST | — | **eBay-specific, NON-GLT — tenant trap (issue #47, 26 Aug 2026).** REPLACES the item's template on EVERY call — a fresh `TemplateId` is minted each time, even when called again for the same item. **Never use it to poll status** — repeated calls silently destroy any in-flight operation on the template you were watching. To read a template's current state, page `Listings/GeteBayTemplates` instead (see above); this endpoint is not wrapped by any tool here |
| `Inventory/ArchiveInventoryItems` | POST | `{"parameters":{"InventoryItemIds":["guid",...],"SelectedRegions":[],"Token":"0000..."}}` | Archives items (hides from active catalogue; items NOT deleted, reversible via Unarchive). `InventoryParametersRequest` body. Empty 2xx on success; the archived SKU then **stops resolving** via `GetInventoryItem` (the read-back signal). Wrapped by `archive_inventory_items`. **Live-tested 8 Jul 2026** (v1.25.0 — re-archived sample `luma-harlyn-frosted`, empty 2xx, SKU no longer resolves) |
| `Inventory/UnarchiveInventoryItems` | POST | `{"parameters":{"InventoryItemIds":["guid",...],"SelectedRegions":[],"Token":"0000..."}}` | Restores archived items to active. Same `InventoryParametersRequest` shape as Archive. **Takes StockItemId GUIDs — NOT SKUs** (an archived SKU can't be resolved). Wrapped by `unarchive_inventory_items`. **Spec-based, not yet live-run by the tool** (the sample was unarchived via the Linnworks UI); shape identical to the live-proven Archive call |
| `Inventory/GetInventoryItem` (archived SKU) | POST | `{"sku":"..."}` | **Cannot resolve an ARCHIVED item by SKU** — returns HTTP 400 "Could not determine inventory item id from SKU". Active SKUs resolve fine. Confirmed 8 Jul 2026 (v1.25.0). Combined with "no archived enumeration", this means the only handle on an archived item is its StockItemId GUID from a UI/Data export |
| `GenericListings/OpenTemplatesByInventory` | POST | `{"request":{"ChannelType":"Shopify","ChannelName":"SHOPIFY","Parameters":{"SelectedRegions":[],"Token":"00000000-...","InventoryItemIds":["guid",...],"ChannelId":<int>},"PaginationParameters":{"PageNumber":1,"EntriesPerPage":<n>}}}` | **Opens the EXISTING GLT template(s)** for already-listed inventory on a store — the revise primitive (vs `CreateTemplates`, which *creates* and could duplicate). Same `Parameters` shape as `CreateTemplates` but **no `ConfiguratorId`** (the existing template carries its own). A **safe read** — live-probed with no side effects. Response: `{"TotalEntries":<int>,"TemplatesInfo":[{...}]}`; each template row has top-level (un-wrapped) `Id` (= `TemplateId` for ProcessTemplates), `StockItemId`, `ConfiguratorId`, `IsLocked`, `IsValid`, `IsAllowedToRevise`, `NextSuggestedAction` ("Update" for a Shopify item with a pending change), `IsNextSuggestedActionAllowed`, plus a nested wrapped `Info` (`ActiveListingId`=Shopify product id, `Status`="Listed", `SKU`, `Title`, `Price` — unwrap via `_glt_field`). A **not-listed** item returns **empty `TemplatesInfo`** (sometimes with `TotalEntries:1` — trust `TemplatesInfo`, not the count). **⚠️ `Info` MIXES VALUES WITH SUMMARIES (live-probed 19 Aug 2026, issue #40): `Title`, `Price`, `ActiveListingId`, `Status`, `LastModificationTime` and `LastUpdateTime` carry REAL values, but `Images`, `Attributes`, `MetaFields` and `Description` are `Type:"Action"` SUMMARIES — a count (`"1"`, `"21"`, `"42"`) or the literal `"Filled"`. So image URLs and description text are NOT retrievable here, which is why a snapshot-vs-item staleness check is structurally partial.** `LastModificationTime` = when the template BODY was last built (the snapshot date — catnip tpl 39076 reads 2026-02-16, matching the v1.27.1 stale-template incident); `LastUpdateTime` = when it was last PUSHED, and it **advances on a push that changed nothing** (it moved on the proven #40 no-op), so it cannot verify a revise. **⚠️ On a VARIATION template `Info.Price` is `0.0`** (prices live per-variant under `Variations`) — comparing it marks every variation group stale. **⚠️ `NextSuggestedAction` is effectively a CONSTANT for a listed Shopify template** — "Update" with `IsNextSuggestedActionAllowed: true` on **24/24** sampled, including seven-week-old snapshots; it does NOT mean a change is pending. Scope to one store via `ChannelId`. **⚠️ ONE ITEM CAN RETURN SEVERAL TEMPLATES on one channel (confirmed live 5 Aug 2026, issue #30): `vnm_bearings_gold` returns TWO Amazon templates for a single StockItemId — 32115 (`Info.ActiveListingId` = `vnm_bearings_gold.FBA`) and 32381 (`= vnm_bearings_gold`). Never key templates by StockItemId (last-write-wins drops one and leaves that listing live) and set `EntriesPerPage` above `len(chunk)`. On Amazon `Info.ActiveListingId` is the CHANNEL SKU, not a numeric product id; both Amazon templates read `NextSuggestedAction:"NotAllowed"` with `IsAllowedToRevise:true`.** Works unchanged for Amazon/TikTok with that channel's ChannelType/ChannelName + ChannelId. **Variation gotcha (confirmed 14 Jul 2026, issue #26):** for a GLT variation listing the template is keyed by the variation PARENT's StockItemId — the CHILDREN return empty `TemplatesInfo` even though they hold the channel-SKU mappings, and the parent holds the template while having NO channel-SKU row. Resolve child→parent (`SearchVariationGroups`+`GetVariationItems`, #17) and open with the parent's id. **Live-confirmed 18 Jun 2026** (issue #20) |
| `Inventory/GetInventoryItemChannelSKUs` | GET | `?inventoryItemId=<guid>` | **Read the channel-SKU link table for one item** — note the endpoint ends in lowercase `…SKUs` (the issue's all-caps `…SKUS` is wrong). Returns a JSON array of `StockItemChannelSKU` (empty `[]` when the item has no channel mapping); key fields: `Source` (SHOPIFY/EBAY/AMAZON/Mirakl MP/MAGENTO), `SubSource` (store/region), `SKU` (channel sku), `ChannelReferenceId`, `UpdateStatus` (""/"Confirmed"/"Notification"/verbose error blob), `ListedQuantity`, `MaxListedQuantity`, `LastUpdate`, `IgnoreSync`, `IsMultiLocation`, `ChannelSKURowId`, `StockItemId`. Rate limit 150/min. Wrapped by `get_channel_listings`. **⚠️ `ListedQuantity` IS NOT LIVE CHANNEL STOCK (7 Aug 2026, v1.42.1) — it is the last value stored on the mapping, and a row whose `LastUpdate` is the null date `0001-01-01T00:00:00` has NEVER synced, so its quantity is meaningless.** Live proof: 11 zero-stock TikTok SKUs read `listed_quantity: 30` and **not one had a live listing** (9 had no GLT template at all; 2 read `Status:"Not listed"` with an empty `ActiveListingId`). **A non-empty row proves a MAPPING exists, not that a listing is live** — for liveness use `OpenTemplatesByInventory`. **Live-tested 18 Jun 2026** (issue #18) |
| `Inventory/BatchGetInventoryItemChannelSKUs` | POST | `{"inventoryItemIds":["guid",...]}` **unwrapped** | Batch form of the above. Returns an array of `{"StockItemId":<guid>,"ChannelSkus":[StockItemChannelSKU,...]}` — one entry per requested id (empty `ChannelSkus` when unlisted). Echoes the StockItemId back; `_fetch_channel_skus_for_ids` lowercases keys defensively and chunks at 200. Wrapped by `get_channel_listings_bulk` + the `list_to_shopify` dedupe. **Live-tested 18 Jun 2026** (issue #18) |
| `Inventory/GetInventoryItemImages` | GET | `?inventoryItemId=<guid>` (or `?itemNumber=<sku>`) | **Read one item's images.** Returns a JSON array of `StockItemImage` (empty `[]` when the item has no images); key fields: `pkRowId` (image id), `IsMain`, `SortOrder`, `FullSource` (full-size URL), `Source` (thumbnail URL), `StockItemId`. Normalized by `_format_image_row`; wrapped by `get_inventory_item_images`. **Live-tested 18 Jun 2026** (issue #19) |
| `Inventory/GetImagesInBulk` | POST | `{"request":{"StockItemIds":["guid",...]}}` **WRAPPED** | Batch image read. **Must be wrapped** — sending `{"StockItemIds":[...]}` unwrapped returns HTTP 400 `{"Message":"request is empty"}` (also accepts a `SKUS` array inside the wrapper). Returns `{"Images":[GetImagesInBulkResponseImage,...]}`; each image carries `StockItemId`, `IsMain`, `pkRowId`, `SortOrder`, `FullSource` (full), `FullSourceThumbnail` (thumbnail) — but **NOT `SKU`**, so callers resolve SKU→StockItemId first and map back by id. `_fetch_images_for_ids` chunks at 200. Wrapped by `get_inventory_item_images_bulk`. **Live-tested 18 Jun 2026** (issue #19) |
| `Inventory/GetCategories` | GET | — | **List all inventory categories.** Returns a JSON array of `LinnworksCategory` `{CategoryId (guid), CategoryName, StructureCategoryId, ProductCategoryId}`. Flat list (StructureCategoryId is 0 for every row in this tenant). The zero-GUID row is the built-in **Default** category. Wrapped by `get_categories` + all category write tools (read-before-write). **Live-tested 7 Jul 2026** (v1.24.0) |
| `Inventory/CreateCategory` | POST | `{"categoryName": "..."}` **unwrapped** | Creates a category; **Linnworks mints the `CategoryId` server-side** — no client GUID needed (unlike the `pkRowId` sub-entity Create* endpoints). Returns the created `LinnworksCategory` `{CategoryId, CategoryName}`. Wrapped by `create_category`. **Live-tested 7 Jul 2026** (v1.24.0) |
| `Inventory/UpdateCategory` | POST | `{"category": {CategoryId, CategoryName, StructureCategoryId, ProductCategoryId}}` | Updates a category — **carry ALL fields** (omitted fields are cleared, same gotcha as PO header update). Empty 2xx body on success. Wrapped by `rename_category`. **Live-tested 7 Jul 2026** (v1.24.0) |
| `Inventory/DeleteCategoryById` | POST | `{"categoryId": "guid"}` **unwrapped** | Deletes a category by id; empty 2xx body on success (subsequent GetCategories no longer lists it). A **non-empty category's items are reassigned to Default** (items are NOT deleted). **Returns HTTP 400 `"...procedure 'Category is in use'."` when the category still holds ARCHIVED items or channel references** — this is the authoritative "not empty" signal (it sees archived stock the active-item sweep cannot). The delete tools detect it via `_is_category_in_use()` and classify as `skipped_in_use` (v1.24.1). Never delete the zero-GUID Default category. Wrapped by `delete_categories` + `delete_empty_categories`. **Live-tested 7 Jul 2026** (v1.24.0; in-use behaviour confirmed 7 Jul, v1.24.1) |
| `Inventory/GetInventoryItemsCount` | GET | `?includeDeleted=<bool>&includeArchived=<bool>` | Returns a single int — the **global** item count. `includeArchived=false` → **32,125** (active); `includeArchived=true` → **92,816** (~60k archived) in this tenant. Only takes deleted/archived flags — **no category filter**, so it can't count items per category (and thus can't detect archived-only categories). Used only as a sanity check; emptiness is derived by the full `Stock/GetStockItems` sweep, which is **active-only**. **Live-confirmed 7 Jul 2026** |
| `Stock/GetStockItems` (empty keyword) | GET | `?keyWord=&entriesPerPage=200&pageNumber=<n>` | The catalogue sweep behind `_count_items_per_category`. **Returns ACTIVE stock only — archived items never appear** (passing `includeArchived=true` is ignored; still returns 32,125). This is why the category emptiness check is active-only. **Confirmed 7 Jul 2026** (v1.24.1) |
| `Stock/GetStockItemsFull` | POST | `{"keyword":"","loadCompositeParents":true,"loadVariationParents":true,"entriesPerPage":<≤200>,"pageNumber":<n>,"dataRequirements":["StockLevels"],"searchTypes":[]}` | **The bulk active-item enumerator** — returns a **plain list** of full stock items incl. `CategoryId`/`CategoryName`. **Wrapped by `list_inventory_items` (v1.34.0)**; four behaviours live-confirmed 5 Aug 2026, each of which the older notes here got wrong. (1) **`loadCompositeParents`/`loadVariationParents` are INCLUDE flags** — `false` makes those parents **ABSENT** (the `false/false` payload previously documented here was NOT the full catalogue: `vnm-catnip` → 8 items both-true vs 1 both-false). With both true the sweep walks **38,684** items / 194 pages, vs the `Stock/GetStockItems` GET sweep's 34,023 (the GET omits variation parents). (2) **`dataRequirements` is a STRING enum** (`StockLevels`/`Pricing`/`Supplier`/`ShippingInformation`/`ChannelTitle`/`ChannelDescription`/`ChannelPrice`/`ExtendedProperties`/`Images`) — **NOT** the integer `[1]` of `GetStockItemsFullByIds`. `"StockLevels"` populates a **per-location** array (`Location{StockLocationId,LocationName}`, `StockLevel`, `Available`, `InOrders`, `Due`, `MinimumLevel`); `"Supplier"` populates `Suppliers[]`. (3) **No `TotalEntries` and no top-level `Quantity`** — sum the per-location rows (verified: Default 1 + Keen 6 == the GET's `Quantity` 7; `InOrders` == the GET's `InOrder`). A page past the end returns **HTTP 400 "No items found with given filter"** (an end marker — terminate on it); `entriesPerPage>200` → HTTP 400 "Number must be between 1 and 200". (4) **`"StockLevels": []` is returned both when levels weren't requested and when an item has none** — indistinguishable from the row alone. Also: **it DOES keyword-search** (empty `searchTypes` or `["SKU"]`/`["Barcode"]`), contradicting the old broken-endpoint note. Takes **no `locationId`** (an unknown key is ignored, all locations still returned) — scope client-side. Still **active-only** (no archived filter), so it does NOT help count archived-per-category. |

---

## ReturnsRefunds — `CannotRefundReason` enum reference

Both `GetRefundOptions` and `CreateRefund` return a `CannotRefundReason` field. `"None"` means no problem. Any other value means the refund cannot proceed:

| Value | Meaning | Action |
|---|---|---|
| `None` | No problem — refund can proceed | Proceed |
| `OpenOrderInLinnworks` | Order isn't processed yet | Use `cancel_order` instead |
| `OrderIsFullyRefundedInLinnworks` | Already fully refunded | Check with `GetRefundHeadersByOrderId` |
| `NotImplemented` | Channel doesn't support API-initiated refunds | Refund manually in the channel |
| `DisabledInConfig` | Refunds disabled in Linnworks workspace settings | Check workspace settings |
| `MissingOrderInLinnworks` | Order GUID doesn't exist in Linnworks | Verify the GUID |
| `Other` | Unclassified error | Surface the `Errors[]` array for details |

`PostSaleStatus.StatusHeader` enum (returned on refund lines): `OPEN`, `PENDING`, `PROCESSED`, `ERROR`, `ERROR_ACKED`.

---

## Endpoints that do NOT work

These have been probed and confirmed broken — don't waste time retrying them:

| Endpoint | Error |
|---|---|
| `OpenOrders/GetOpenOrders` | HTTP 400 null-reference |
| `OpenOrders/GetOpenOrderIds` | HTTP 400 null-reference |
| ~~`Stock/GetStockItemsFull`~~ | **RETIRED 5 Aug 2026 (issue #32) — this entry was wrong twice over.** It claimed the endpoint 400s and "does not support keyword search". It does neither: `keyword` works with empty `searchTypes` or `["SKU"]`/`["Barcode"]` (a SKU string with `searchTypes:["Title"]` correctly returns nothing, which is a match failure, not a 400), and with `keyword:""` it is the bulk active-item enumerator now wrapped by `list_inventory_items`. The original 400 was almost certainly a malformed body — see the confirmed-endpoints row for the working payload. `Stock/GetStockItems` (GET) remains the better *keyword* search (it has `TotalEntries` and both parent flags); `GetStockItemsFull` is the better *enumerator* (per-location stock in one pass) |
| `Inventory/GetInventoryItems` | HTTP 400 — no working payload shape found; use `GetInventoryItem` (singular) for exact SKU lookup, or `Stock/GetStockItems` (GET) for keyword/title/barcode search |
| `Stock/GetStockList` | HTTP 404 |
| `Inventory/SearchInventory` | HTTP 404 |
| `Stock/GetVariationGroupByName` | Not a hard error — returns HTTP 200 **`null` even for a group that exists** (live-proven 28 Jul 2026, seconds after creating one). Unusable for group lookup; use `Stock/SearchVariationGroups` (searchType=VariationName) + exact-match confirmation (`_find_variation_group_by_name()`) instead |
| `Inventory/GetInventoryItemSuppliers` | HTTP 404 — wrong name; use `Inventory/GetStockSupplierStat` (live-proven v1.27.0) or `Stock/GetStockItemsFullByIds` with `DataRequirements:[1]` |
| `Inventory/GetStockItemSuppliers` | HTTP 404 |
| `Inventory/GetItemSuppliers` | HTTP 404 |
| `PurchaseOrder/GetSupplierItems` | HTTP 404 |
| `Inventory/GetInventoryItemSupplierStat` | HTTP 404 — wrong name; the real endpoint is `Inventory/GetStockSupplierStat` (live-proven v1.27.0) |
| PO "Add delivery" booking | No public API equivalent — use `Add_PurchaseOrderNote` as workaround |
| Park / unpark an order | No public API equivalent (28 Jul 2026) — swept every 1.0 spec file: there is **no park endpoint**. Parking exists only as a **RulesEngine action (`ChangeOrderParkStatus`)**, and `GeneralInfo.IsParked` is **read-only** on `GetOrdersById`. `set_order_status` rejects `park`/`unpark` and points to `lock`. This matters operationally, not just for naming: **park is the only hold state that KEEPS stock allocated** (lock releases it), so it can't be replicated — the closest API hold (`lock`) frees the order's stock |
| ~~Composite component → parent reverse lookup~~ | **RETIRED 5 Aug 2026 (issue #31) — this entry was half wrong.** Still true: no endpoint maps a component SKU back to its composites. **No longer true:** that the enumerate-and-invert fallback is infeasible. `Stock/GetStockItems` (GET, `keyWord=""`) enumerates the active catalogue fine and carries a reliable `IsCompositeParent` flag, and `Inventory/GetInventoryItemsCompositionByIds` resolves compositions 100 at a time — so the reverse index is buildable in ~215 calls / ~105s. Built as `find_composite_parents` (v1.33.0). `get_item_relationships` still doesn't do it inline (too expensive per call) and now points at that tool |
| Per-category **archived**-item count | No public API equivalent (v1.24.1, 7 Jul 2026) — `Stock/GetStockItems` and `Stock/GetStockItemsFull` are both active-only (archived items never returned), `GetInventoryItemsCount` gives only a global archived total (no category filter), and there is no `GetArchivedItems`/`Get…ByCategory` endpoint. So "is this category truly empty (incl. archived)?" **cannot** be answered by reading — the delete tools instead rely on `DeleteCategoryById`'s own "Category is in use" 400 as the authoritative check (→ `skipped_in_use`) |
| Enumerate **archived** items | No public API equivalent (v1.24.1/v1.25.0) — no `GetArchivedItems`, and every list/search endpoint (`GetStockItems`, `GetStockItemsFull`) is active-only and ignores `includeArchived`. The only source of archived items is a Linnworks UI/Data export (include the "Stock Item ID" column) |
| Resolve an **archived SKU → StockItemId** | No public API equivalent (v1.25.0, 8 Jul 2026) — `Inventory/GetInventoryItem` 400s "Could not determine inventory item id from SKU" for archived items, and `GetStockItemIdBySKU`/`GetInventoryItemId` (all casings) 404. Archived items can only be addressed by StockItemId GUID (from an export). This is why `unarchive_inventory_items` is GUID-driven and the archived-delist workflow must unarchive in the UI first |
| Delist (END) **eBay** / **Etsy** / **Mirakl** / **CDiscount** listings | No public API equivalent for ENDING a listing (8 Jul 2026; re-confirmed with Etsy/CDiscount 5 Aug 2026) — **not GLT channels** (GLT `ChannelType` enum = `CDiscount_OBSOLETE/Shopify/Magento/External/Walmart/TikTok/Amazon` — **no eBay, no Mirakl**; `GetConfiguratorsInfoPaged` for eBay → 400 "Invalid parameter request"). So an eBay or Mirakl listing can only be ENDED in that channel's own admin — the MCP **cannot delist every channel**, only GLT-managed ones. Archived-item cleanup must clear eBay/Mirakl manually. ⚠️ **CORRECTION (issue #43, 25 Aug 2026): the old wording here — "no dedicated eBay/Amazon/channel listing-management spec in the public API (`ebay.json`/`amazon.json`/`channel.json` all 404)" — was misleading.** Those three FILENAMES genuinely 404, but a differently-named spec file, `listings.json`, DOES exist and defines a dedicated eBay `Listings/` API family (`GeteBayConfigurators`/`GeteBayTemplates`/`ProcesseBayListings`) — see the confirmed-endpoints table and `revise_ebay_listing_description`. It can REVISE an existing listing's description; it cannot create, end or relist one, so this row's core conclusion (delisting eBay via this server is impossible) still stands — only the reasoning was overstated. Same "stale note" lesson as assumption #10 |
| Refresh a GLT **variation** template's fields via API | No public API equivalent (14 Jul 2026, #27) — `ProcessTemplates` Update AND Revise both push the template's stored stale snapshot; `CreateTemplates` mints a new UNLINKED template instead of rebuilding; `SaveTemplateFields` only accepts top-level `ShopifyTemplateProperties` scalars (`Price`, `Title`) and rejects `Variations`/`Attributes`/`Description`. A stale variation template can only be refreshed in the Linnworks UI; stale-listing fixes go via the Shopify Admin API |
| Delist **Magento** listings (this tenant) | GLT `ChannelType="Magento"` is valid, but this tenant has **0 Magento GLT configurators** (`GetConfiguratorsInfoPaged` → 0), so Magento listings here aren't GLT-managed and can't be taken down via `ProcessTemplates Delete`. (Would work on a tenant that lists to Magento through the GLT.) Confirmed 8 Jul 2026 |
| `Listings/GetEbayListingOperations` | HTTP 400 "Parameter 'request' has invalid value" under BOTH a body payload and `request.*` query-string serialization (issue #47, 26 Aug 2026) — the operations queue for eBay pushes is unreadable on this tenant via the public API; not wrapped by any tool here |

---

## Autopaginating tools — run sequentially

These tools make hundreds of API calls internally (autopaginating + batched line-item fetches):

`get_top_skus` · `get_sales_by_supplier` · `get_category_report` · `get_revenue_summary` · `get_period_comparison` · `get_component_sales` · `find_composite_parents` (first call only — ~215 calls / ~105s to build its index, then cached 15 min) · `list_inventory_items(all_pages=True)` (~194 calls / ~132s — and ~215 more if `flag_composite_parents=True` builds the index) · `get_categories(with_counts=True)` / `delete_empty_categories` (full catalogue sweep)

Not autopaginating in the same sense, but cost-bearing enough to keep out of parallel
runs: `get_stock_change_history` costs ~1 call per (SKU, location) page, so a batch with
`all_locations=True` is ~29 calls per SKU (rate limit 250/min).

**Never fire two of these in parallel.** Concurrent calls hit Linnworks rate limits and the second call times out. Run them sequentially — wait for one to return before calling the next.

---

## Auth setup

1. Create an app in the Linnworks developer console — this gives you an App ID and App Secret
2. Visit `https://apps.linnworks.net/Authorization/Authorize/{ApplicationId}` while logged into Linnworks
3. The **Permanent token** is displayed on the installation page (not via the redirect URL)
4. If no token is shown, configure a Postback URL on the app and reinstall
5. Use the Permanent token as `LINNWORKS_INSTALLATION_TOKEN`
6. Call `Auth/AuthorizeByApplication` — this exchanges it for a session token; re-auth on 401

Credentials go in `.env` (local dev) or the Claude Desktop config `env` block (production). Never commit them.

---

## Testing

- **`python server.py --list-tools`** — offline smoke test, **no credentials needed**. Lists every registered MCP tool and a total count. Confirms the module imports cleanly and your new tool actually registered — catches decorator typos, duplicate tool names, and import-time errors that `py_compile` alone misses. This is the primary CLI build-loop check (the credential gate is now lazy, so the module imports credential-free).
- **`python server.py --check-auth`** — verifies credentials and the auth handshake without touching the MCP layer. Requires real credentials, so it only works where a `.env` (or env block) is present.
- **Bootstrap CLI credentials from the Desktop config (local Mac)** — a CLI session doesn't inherit Claude Desktop's `env` block, so by default `--check-auth` and in-process live tests can't authenticate. But the creds already live on this machine in `~/Library/Application Support/Claude/claude_desktop_config.json` under `mcpServers.linnworks.env`. Generate a gitignored `.env` from them once and the whole local CLI build loop can self-test:
  ```bash
  python3 -c "import json,os; e=json.load(open(os.path.expanduser('~/Library/Application Support/Claude/claude_desktop_config.json')))['mcpServers']['linnworks']['env']; open('.env','w').write(''.join(f'{k}={e[k]}\n' for k in ('LINNWORKS_APPLICATION_ID','LINNWORKS_APPLICATION_SECRET','LINNWORKS_INSTALLATION_TOKEN')))"
  ```
  `.env` is gitignored (never commit it). This only works on the local Mac with Desktop installed — **genuinely-remote routine builds on claude.ai/code have no Desktop config and still can't authenticate**. Keep the `.env` in sync if the Desktop creds are rotated.
- **In-process live testing** — once `.env` exists, `import server` and call any tool function directly against the live tenant (the running Desktop MCP won't hot-reload edits, so iterate in-process here, then restart Desktop to pick up the fix). For write tools, exercise `dry_run=False` against an isolated `ZZZ-MCP-TEST-*` SKU you create and delete (via `Inventory/DeleteInventoryItems`) — never a real catalogue SKU. NB: live production writes still require user approval (the auto-mode classifier blocks them otherwise).
- **Claude Desktop** — after registering in `claude_desktop_config.json` and restarting, ask conversational questions. Logs: `~/Library/Logs/Claude/mcp.log` (macOS) · `%APPDATA%\Claude\logs\mcp.log` (Windows)
- **Claude Code** — `claude mcp add` registers the server for a new session; useful for calling tools directly during development

---

## Build workflow (Claude Code CLI)

When asked to "build the latest approved issue" or "build issue #N", follow these steps exactly — do not ask clarifying questions, just execute:

### Step 1 — Find the issue
```bash
gh issue list --repo josephgurney/linnworks-mcp --label approved --state open --json number,title,body,labels
```
Take the lowest-numbered result. Read the full issue body carefully.

### Step 2 — Understand what to build
Read this CLAUDE.md (you're reading it now). Cross-check the request against:
- The existing tools table — does it already exist?
- The confirmed working endpoints — which endpoint does this need?
- The broken endpoints — is it blocked?

### Step 3 — Write the tool
Add the new tool function to `server.py` following these rules:
- Place it in the correct section (Orders, Purchase Orders, Reporting, etc.)
- Add `@mcp.tool()` decorator
- Follow the read-before-write pattern for any write tool
- Default `dry_run=True` for all write tools
- Write the docstring carefully — it's the UX description Claude sees
- Match the payload wrapper pattern of nearby tools (some endpoints need `{"request":{}}`, some don't — check the confirmed endpoints table)

### Step 4 — Verify syntax
```bash
.venv/bin/python3 -m py_compile server.py && echo "Syntax OK"
```
Fix any errors before proceeding.

### Step 5 — Verify the tool registered (offline)
```bash
.venv/bin/python3 server.py --list-tools | grep <new_tool_name> && \
  .venv/bin/python3 server.py --list-tools | tail -1
```
This is the real CLI build-loop check — it needs no credentials. It confirms the
module imports cleanly **and** your new tool registered (catching decorator typos,
duplicate names, and import-time errors `py_compile` misses). The `grep` must
match and the total count should have gone up by the number of tools you added.

`--check-auth` and live testing need credentials. On the local Mac you can
bootstrap them: generate a gitignored `.env` from the Desktop config's
`mcpServers.linnworks.env` (see the **Testing** section for the one-liner), then
`--check-auth` passes and you can live-test tools in-process. On a genuinely
remote build (claude.ai/code, no Desktop config) creds are unavailable — fall
back to `--list-tools` only and defer live testing to Claude Desktop.

### Step 6 — Update CLAUDE.md
- Add the new tool to the Tools table
- Add any newly confirmed endpoints to the confirmed endpoints table
- Add any newly discovered broken endpoints to the broken endpoints table

### Step 7 — Commit and push
```bash
git add server.py CLAUDE.md
git commit -m "Add <tool_name> tool (closes #N)"
git push origin main
```

### Step 8 — Comment on the issue
```bash
gh issue comment N --repo josephgurney/linnworks-mcp --body "## Built

Added \`tool_name\` in this commit. To use it, pull the latest \`server.py\` and restart Claude Desktop.

**What was built:** [one sentence]
**Endpoint used:** \`Endpoint/Name\`
**Dry run default:** yes/no"
```

### Step 9 — Label the issue
```bash
gh issue edit N --repo josephgurney/linnworks-mcp --add-label "built" --remove-label "approved"
```

### Important notes for CLI builds
- The Linnworks MCP isn't connected as MCP tools in the CLI session, but on the local Mac you **can** authenticate: bootstrap a gitignored `.env` from the Desktop config (see **Testing**), then `--check-auth` passes and you can `import server` and call tool functions in-process against the live tenant — including `dry_run=False` on an isolated `ZZZ-MCP-TEST-*` SKU. Always run `python server.py --list-tools` (offline) as the baseline registration check. Live production writes still require user approval (the auto-mode classifier blocks them). On a genuinely remote build with no Desktop config, defer live testing to Claude Desktop.
- Do not ask the user for confirmation between steps — execute the full workflow and report back at the end.
- If an endpoint is unknown, check `https://apidocs.linnworks.net` or fetch the relevant spec file from `https://raw.githubusercontent.com/LinnSystems/PublicApiSpecs/master/1.0/<name>.json` before guessing. Key spec files: `orders.json`, `openorders.json`, `returnsrefunds.json`, `purchaseorder.json`, `inventory.json`, `stock.json`, `processedorders.json`, `importexport.json`, `rulesengine.json`.

---

## Out of scope for Phase 1

- OAuth, Dynamic Client Registration — Phase 2
- Hosting, deployment, public URLs — Phase 2
- Multi-tenancy, per-user token storage — Phase 2

Flag any request that pushes toward these and discuss before proceeding.
